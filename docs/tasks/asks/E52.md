# E52

**Status:** done (2026-09-23) — X1 learned LUT, X2 Blender cove (default island), X3 TRELLIS.2 + Hunyuan3D-2 + CC0 assets (board: art/driftwood-isle/round-8-assets/trellis-vs-hunyuan-board.jpg), X4 painted horizon, X5 WebGPU stage 1 (parked behind Settings); continuing in DRIFTWOOD-REMASTER-V2 (V-B1, V-G1/G2, V-M1) (live in `0c886df-muestvsn`)
**Ask:** Whatever it takes to close the mockup gap: install GPU torch / anything; implement GI + AO; better assets (CC0, Hunyuan3D-2 / TRELLIS.2 image-to-3D, Blender sculpts + textures); a Blender-built island prototype with an in-game toggle vs the TypeScript island; learned colour LUT; painted horizon; upgrade three.js to latest + WebGPU renderer, TSL, raw WebGPU where it pays; break the no-textures rule


Routed as DRIFTWOOD-REMASTER Track X (X1–X5). three.js: npm latest is 0.186.0 = what we pin, so the "upgrade" is the
WebGPU renderer + TSL that r186 already ships (staged behind `?gpu=webgpu`). The no-textures rule is lifted (D9).
