# E80

**Status:** needs pick (2026-09-23) — 5 mockups on the board, which letter moves HOVER?
**Ask:** I don't like the hover position create 5 mockups for new positions

**Board:** `art/hud/round-11-hover-position/board.jpg` (BEFORE + A–E). They are codex edits of Jake's iPhone
screenshot (`before-iphone.jpg`). The HOVER pill leaves its spot above the bottom bar in every variant.

- **A** `A-top-row-pill.jpg`: a third pill in the top row, after "30 fps 33 ms".
- **B** `B-disc-above-jump.jpg`: a round disc above JUMP in the right-thumb cluster.
- **C** `C-bar-tab-above-move.jpg`: a folder tab on the bottom bar's top edge, above MOVE.
- **D** `D-under-objective.jpg`: a small tab right-aligned under the GLYPH SHARDS / SEA CAVE strip.
- **E** `E-disc-left-edge.jpg`: a round disc on the left edge above MOVE, for the left thumb.

Re-rolled: B twice (HUD drift) and D once (squashed 2:3 frame). The index entry is in `art/README.md`.
Next: Jake picks a letter, then `.ws-touch-pill.hover` (src/ui/styles/touch.css) and TouchControls move there.
