# E173

**Status:** parked (2026-09-26, wrapped up by the user) — built on branch `worktree-agent-ac10dd3f03afa09c9` (tip `dccaa7fa`, worktree .claude/worktrees/agent-ac10dd3f03afa09c9; NOT landed): desktop KTX2 baked + Auto on desktop + boards progress/279–281 (on the branch). Its scorecard (progress/scorecard/e173.md on the branch) missed 4 budget rules: the resident return Nalati → Driftwood 0.05 → 1.7 s (phone) / 1.94 s (desktop), and Driftwood cold 4G 23.7 → 26.7 s (phone) / 23.1 → 27.7 s (desktop). Next: find why (KTX2 re-upload on resume? the transcoder on the cold path?), fix, re-run the scorecard, land
**Ask:** bake desktop KTX2 too (GPU-compressed textures on desktop, not only the phone)

