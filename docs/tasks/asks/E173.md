# E173

**Status:** in flight (2026-09-26 — the user: "I want it to be finished, I don't want it parked") — its subagent is fixing its two regressions (Driftwood desktop cold 4G +4 s, Pine desktop main-thread +1–2 ms/frame); the look change is KTX2's (boards 279–281). The slower resident return is main's, E194 (a second subagent)
**Ask:** bake desktop KTX2 too (GPU-compressed textures on desktop, not only the phone)

