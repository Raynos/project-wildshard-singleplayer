# E173

**Status:** parked (2026-09-26, the user wrapped up) — built on branch `worktree-agent-ac10dd3f03afa09c9` (rebased on 33c629af, NOT landed): desktop KTX2 (Pine Hollow desktop GPU 1205 → 619 MB, Driftwood + Pine resident 1212 → 626 MB), Auto on desktop, boards progress/279–281, +91 MiB repo. Its own regressions vs main measured alone: Driftwood desktop cold 4G 23.6 → 27.7 s (same bytes, cause unknown), Pine Hollow desktop main-thread +1–2 ms/frame (cause unknown), desktop look SSIM vs the image goldens (needs a re-budget / new goldens), cache 124.5 → 271.5 MiB (under the 300 cap). The slower resident return is NOT E173's — it is on main (E194)
**Ask:** bake desktop KTX2 too (GPU-compressed textures on desktop, not only the phone)

