# P5

**Status:** done (2026-09-22) — live `911bad9`+
**Ask:** Cold first-launch bytes 30 MB → ~10 MB (baked PMREM instead of the 4 MB HDR, model textures to the phone tier, KTX2 for GPU memory)

Pine Hollow tier=phone cold **37.47 → 10.01 MB** (boot, skipintro); Driftwood (the default shard) **2.38 MB in 17 requests** incl. the title screen (bench:ci, E4). Commits:
`1c4d87b` prop textures q85 · `1374a41` sky HDR → 0.3 MB gain-mapped pair + .phone copies (replaces the 4 MB HDR) · `50b9326` SW skips hero images · `81fdb22` phone tier WebP for every texture set / prop / LOD GLB · `1da15cd` per-shard boot manifest, Driftwood clouds + gas giant baked, bench-load refuses a foreign port · `8b57364` baked-sky decode in a Worker, resizeQuality medium · `8362d8a` PMREM split into three tasks · `911bad9` LOAD-PERF rows + progress/149–150.
Terrain layers stay 1024 on the phone (512 visibly soft). KTX2 measured and rejected: 2.4× WebP's error at equal size (the high-quality variant 4× bigger), a ~0.5 MB transcoder, and it saves GPU memory, not bytes. Baked PMREM rejected: more bytes on top of the sky image the background still needs. Open risk handed to E4: `/assets/baked/**` is cached for a year but re-baked at the same path. Request pack (101 → 17) was E4's `e0f7cfc`; title heroes (−1.7 MB) E4's `62f66b8`.

_Moved from the legacy table in docs/tasks/ASKS.md on 2026-09-22._
