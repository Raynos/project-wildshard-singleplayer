# E65

**Status:** in flight (2026-09-23, Driftwood integrator session, solo) — Look Lab live `312a4f3`; sheet sent; the user is trying lighting / sky / post in-game (LUT + horizon: keep on) (2026-09-23)
**Ask:** Taste is the user's call: a debug / variants menu (or screenshot sheets of every variant) to toggle every stylistic change — toon vs original lighting, stylized vs realistic sky, fog, grade, etc. — so the user decides each one; only obvious fixes / asset upgrades ship without a pick


Plan: a **Look Lab** group in main menu ▸ Settings — one switch per taste axis, each with the pre-remaster look kept
as an option (lighting, sky, fog, grade / LUT, haze + grain, painted horizon, cloud shadows, terrain palette, water,
island); every axis also a URL param via `setting()` so the harness can shoot every variant; then per-axis variant
sheets at fixed poses sent to the user for the pick. Fixes and asset upgrades stay on without a pick.
