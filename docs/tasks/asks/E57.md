# E57

**Status:** open (2026-09-23)
**Ask:** Listen on the phone: veto the MiniMax music auto-picks (round-2 page) and the re-scored trailers

- From E33 (MUSIC plan, archived `project/archive/2026-09-23-music.md`). The MiniMax Music 3 score shipped as auto-picks
  (the top-scored take per style × slot). The pick and 2 alternates per slot are on `art/music/round-2-minimax/index.html`;
  a vetoed slot is re-cut from an alternate with `scripts/music/gen/stems.py --pick <style>/<slot>=<take>` (new file
  names, the service worker caches audio). Trailers `/trailer-15.mp4` and `/trailer-30.mp4` carry the piano title.
