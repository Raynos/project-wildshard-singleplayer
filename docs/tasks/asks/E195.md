# E195

**Status:** needs you (2026-09-26) — run these, one per prompt:
**Ask:** needs you — cleanup the E154/E155 session could not do (dcg blocks it): a stray self-symlink node_modules/node_modules made by mistake, four build-output stashes, and temp dirs


```
! unlink ~/projects/games/wildshard-singleplayer/node_modules/node_modules
! git -C ~/projects/games/wildshard-singleplayer stash list
```
Then drop, by tag (re-read the list first: the stash is shared) `e179-generated-1790400249`, `e174-build-outputs-a623e8`,
`e174-build-outputs-a623e8-2`, `e174-build-outputs-a623e8-3` (each holds only build-regenerated files:
`src/boot/*.generated.ts`, `public/assets/baked/*/terrain.json`) with `! git -C ~/projects/games/wildshard-singleplayer stash drop stash@{N}`.
Temp dirs to remove: /tmp/e194, /tmp/e172-*, /tmp/e160-before, /tmp/e160-after.
The self-symlink came from a mis-chained `ln -sf node_modules` run in the main checkout (2026-09-25 22:53). The gates run
fine with it, but a tool that walks node_modules could loop on it.
