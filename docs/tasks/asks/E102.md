# E102

**Status:** done (2026-09-24)
**Ask:** add links to ~/projects/weights and ~/projects/localai in the md of this repo (AGENTS.md) — then, same
session: "Update the main md in this repo to make it really obvious how we use local LLMs for music sfx mockups
and 3d models"

AGENTS.md gets a new section near the top, **Local models: music · SFX · 3D · mockups**. It has:
- links to `~/projects/weights` (MODELS.md, AGENTS.md) and `~/projects/localai` (music-models, 3d-models, engines,
  LANDMINES);
- one table saying which engine, weights, runner and licence each job uses;
- the shared-lock rule, the evict.sh warning and the licence rule.

"Audio engines" and "Mockups" point to it. Codex stays the mockup path; Qwen-Image-2.1 is listed as the E100 trial.
Evidence: the commit that carries this file.

Live build: `b8c91b4-mufni1hn` (deploy run 36015850575 green).
