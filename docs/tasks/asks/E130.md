# E130

**Status:** done (2026-09-25) — live in build 5de4ef9-mugvhywt
**Ask:** Item 7 (E126): Driftwood HUD + map. B1 locked in (the Map tab draws structures + paths as flat shapes, undiscovered stay ?). C ok (shorter labels, ◆ status, auto-nudge collisions). D ok (M opens/closes Map, I Inventory, Esc pause/Settings). E ok (hide settings that don't apply to the current weapon). A (pop-up placement) pending: board art/hud/round-10-toast-placement/board.jpg, A1 vs A2


**2026-09-25, A picked: A1.** Toasts stack under the quest chip (top-right) and the chip stays visible. Board: `art/hud/round-10-toast-placement/board.jpg` (iPhone portrait).

**Done 2026-09-25 (E130 subagent), live build `5de4ef9-mugvhywt`.** Before / after (iPhone 16 Pro): `progress/243-e130-map-before-after.jpg`.
- **D** `c2fe91a` — M opens / closes the Map, I the Inventory (same key again closes, the other switches), Esc pauses on
  Settings; one key listener in `src/ui/Menu.ts`, gated by the HUD (`keyGate`). Root cause of "M lands on Settings": opening
  the menu releases the pointer lock (`menu.onOpen`), and the HUD's `pointerlockchange` answered every release with
  `setPaused(true)` → `menu.open('settings')`. Now a release while the menu is up is ignored (and one within 400 ms of a
  close: the browser's own Esc handling drops the lock the close had just re-taken). "M again" never closed because
  main.ts had a second M listener that re-opened it — removed. **E32 fixed on the way** (the HUD's nolock Esc listener is gone).
  Checked headless: pointer-lock play and `?nolock`, all three shards; touch paths untouched.
- **E** `c2fe91a` — Settings rows carry a data-driven "applies when" (weapons held, shard), re-read on every open: Tracer
  bolts only with crossbow / rifle, Hunter's eye only with a bow, Swing turn speed only with a blade; labels hide with
  their last row. Driftwood no longer lists Tracer bolts.
- **A1** `34eccf5` — toasts right-aligned under the quest chip on every tier (HUD reads the chip's box into
  `--ws-toast-top/right`), two at most, the chip's dim fade removed; Nalati layout D right-aligned under its stack.
- **C** `d096514` — quest markers labelled by short name behind a cyan ◆; a marker at a place of its name tags that
  place's label; every label laid out clear of the others / markers / your arrow (all shards).
- **B1** `5de4ef9` — `ChunkDef.map` (Driftwood's paths + registry piece ids) → sand paths, palm crowns and the pieces'
  collider footprints (`src/ui/mapShapes.ts`) painted into the minimap layer and the full map's cached tiles.
- Left for the user: the ◆ shows "a shard waits here" only (a taken shard's marker hides, so there is no hollow ◇ for
  "taken"); silhouettes show dimmed under the fog of war even before a place is discovered (its label stays "?").
