# E44

**Status:** done (2026-09-23) — `e391e58`, live as build `e391e58-mudyi4vi`: everything (all audio + the title / explore art) loads at the loading bar, 0 requests after "playable" on both shards, the PWA reloads and plays offline (music + SFX, style / set switches included); DOWNLOAD phone 14.8 MB Driftwood / 23.5 MB Pine Hollow.
**Ask:** "Preload music — all audio files should be preloaded and pre-downloaded; open the download budget higher in the loading menu. In fact everything should be loaded at the loading bar and the PWA should work offline. No cheating and background loading."

The user's picks:
- Scope: the launched shard + all audio.
- Audio decode: download all of it, decode only the selected style and SFX set.
- Priority: completeness wins over a small first launch.

The plan was docs/plans/PRELOAD-OFFLINE.md, archived as project/archive/2026-09-23-preload-offline.md (audit list, DOWNLOAD totals, before / after bench, offline results).
