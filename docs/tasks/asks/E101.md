# E101

**Status:** needs pick (2026-09-24). The weights check is done; the captain licence question below is Jake's.
**Ask:** update all the music, sfx and local 3d art models in ~/projects/weights too

## Done
- **Upstream check (2026-09-24)** of every music, SFX and 3D repo in `~/projects/weights` (HF `lastModified` and sha,
  org listings, LICENSE files). **Nothing changed upstream since it was fetched, and no newer open-weight successor
  exists** for MiniMax-Music3, CLAP, htdemucs, MOSS-SoundEffect v2.0, Stable Audio 3 Medium, TRELLIS.2-4B, DINOv3 or
  BiRefNet. Nothing to re-download.
  - The official SA3 Medium repo moved after our mirror, but only a training flag in `model_config.json` changed.
    The weights are byte-identical.
- `~/projects/weights/MODELS.md` gets the missing `tencent/Hunyuan3D-2mini` row (8.50 GB, on disk, never listed), the
  check's results, and the licence debt below. weights commit `cce080e`, local, not pushed.

## Needs Jake
- **The Drowned Captain ships from Hunyuan3D-2**: `public/assets/models/driftwood-hero/captain/captain.glb`, `3cac2d3`.
  - The licence of every Hunyuan3D repo (2, 2mini, 2.1, Omni, Part) says it "does not apply in the EU, UK and South
    Korea", and that includes using its output there. A public web game is served in all three.
  - The MIT path is TRELLIS.2: regenerate the captain from the same `ref-captain.jpg` and re-rig it on his bones.
  - That changes a shipped look, so it is Jake's pick, not done on spec.
