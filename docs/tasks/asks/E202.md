# E202

**Status:** done (2026-09-26, Codex)
**Ask:** What are all these uncommitted files, how can we commit them in many small commits ? That’s unrelated hygiene work.

Expanded `git status --porcelain=v1 -uall` inventory: **724 dirty paths** (the ordinary status collapses untracked directories, making the count look much smaller). Main groups are 309 `art/nine-dragon-stack/` images/reference files, 267 `art/sfx/` generated candidate takes, 28 `art/music/` candidate cues, 42 `progress/scorecard/` files, 26 `progress/bench/` files, 12 `scripts/steam-trailer/` files, and the Nine Dragon F1 code/physics routes. Other changes include `src/audio/`, three baked terrain manifests, `.github/workflows/deploy.yml`, the art index, and E5/E57/E62 ask files. Some unrelated paths are **already staged** (`MM` or `D ` in status), so a shared-index commit would mix work.

Commit by feature and ownership with explicit pathspecs: (1) F1 bounds/physics routes after its walk gate, (2) small dome visual changes, (3) trailer pipeline edits with Steam regression checks, (4) Nine Dragon shot/cut/audio metadata, (5) approved Nine Dragon images and trailer masters/pick notes, (6) F2 grapple when ported, (7) music and SFX selections per sound family after listening and licensing notes, (8) unrelated audio/workflow/docs changes in their own reviewed commits. Scorecard/bench raw files and candidate takes need an intentional keep/discard decision; do not sweep them into a commit.

The bug fix (`8062b4ef`) and the empty-state UI (`7eb0f72c`) were committed separately with explicit pathspecs. The remaining dirty files were not swept into either deployment.

Audit/inventory commits `988c28ea` and `28c6202c`; GitHub deploys passed, inventory build `28c6202-muieyobz`.
