# E202

**Status:** done (2026-09-26, Codex)
**Ask:** What are all these uncommitted files, how can we commit them in many small commits ? That’s unrelated hygiene work.

Audit: about 301 dirty paths on handoff, mostly 211 `art/` assets and 42 `progress/` captures/bench files. The code WIP belongs chiefly to Nine Dragon F1 bounds/respawn and F3 trailer tooling; other art/docs/bench files are separate work. Do not sweep the shared index. Commit by ownership and feature with explicit pathspecs: (1) F1 bounds/physics routes after its walk gate, (2) small dome visual changes, (3) trailer pipeline edits with Steam regression checks, (4) Nine Dragon shot/cut/audio metadata, (5) approved masters/pick notes, (6) F2 grapple when ported. Captures and bench scratch files need an intentional keep/discard review, not a blanket commit.

The bug fix (`8062b4ef`) and the empty-state UI (`7eb0f72c`) were committed separately with explicit pathspecs. The remaining dirty files were not swept into either deployment.
