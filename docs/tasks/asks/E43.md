# E43

**Status:** in flight (2026-09-23, Driftwood integrator session, E7) — round 1 **done** `ac9b87c` (9 captures + 9 mockups + 3×3 sheets sent to the user, README with every camera); round 2 = the look-agent + model-agent looping on the TOP-10 gaps → art/driftwood-isle/round-5-loop/
**Ask:** One small area (spawn cove), 9 angles (4 first-person + 5 god-mode: top, diag front/left/right/back) → 9 parallel codex remaster mockups from the concept art → loop the engine until it matches; 3×3 in-game + 3×3 mockup sheets for Claude iOS


**Plan:** part of DRIFTWOOD-REMASTER (E7). Area = the spawn cove (pier landward end + beach + plateau stair). Captures,
mockups, 3×3 sheets and a per-angle gap list → `art/driftwood-isle/round-4-remaster/` (README.md there records each
camera so later rounds re-shoot identical frames). The gap list's top 10 is routed to the look / model / ground-cover
agents; each loop re-shoots the 9 in-game frames and compares against the mockups until they match.

Round 1 findings (README.md in round-4-remaster): wall-clock ~70 min (captures 8 min; nine parallel codex image_gen
runs 30–45 min each). Next rounds: capture + ONE concept ref, lower effort, 4–5 runs at a time. The pier ends ~33 m
short of the beach (deck to z −190, dry sand from ~z −158) → the model-agent extends it. `?explore=world&cam=` ignores
the camera (Explore.ts:232 re-homes it) → E14's owner.
