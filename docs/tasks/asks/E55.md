# E55

**Status:** done (2026-09-23) — `2ec9412`, live in `6c3ffc7-muefx2at`. Leftover: a LUT on/off row waits on the look-agent's LUT3DEffect (src/world/lut.ts + Game.ts still uncommitted); touch controls offer Auto / Always only (an Off needs TouchControls, HUD agent)
**Ask:** Every experimental toggle in Settings, no query parameters: main-menu settings (reload-to-apply, e.g. renderer WebGL/WebGPU, island procedural/Blender) separate from pause-menu settings (runtime-only), to reduce chaos


Plan: main-menu Settings = reload-to-apply (renderer WebGL/WebGPU, island procedural/Blender, quality tier) with
APPLY & RELOAD; pause-menu Settings = live toggles only (painted horizon, LUT, time of day / clock, audio, look).
Dev/test query params (skipintro, nolock, x/z/yaw, showcase …) stay for the agent harnesses; a param, when present,
still wins for that load, otherwise the saved setting. One `setting()` helper in Settings.ts.

Landed (`2ec9412`): `setting(k)` / `saveSetting` / `onSettingChange` in src/ui/Settings.ts — URL param › saved pick ›
default. Main menu ▸ SETTINGS (title link → src/ui/BootSettings.ts, APPLY & RELOAD): quality tier, render scale,
anti-aliasing, renderer WebGL / WebGPU / WebGPU·GL (experimental), island procedural / Blender (experimental), touch
controls. Pause menu ▸ Settings (live): Time of day (live / midday / golden / sunset / night) and Painted horizon, plus the
existing gameplay / controls / audio rows; the reload-only rows left it. Evidence: progress/settings-*.jpg (both menus at
1600×900 and 390×844, horizon on / off and golden hour switched live from the pause menu).
