# E28

**Status:** done (2026-09-23) — Jake: "E28 yes". Herdr message sent to the three other live wildshard agents (remaster, pop-in / graphics, Nalati) pointing them at AGENTS.md; the colliding legacy row E24 (commit guard) is renumbered E92.
**Ask:** (from E21) Switch the already-running agents to the new rules; fix the legacy E24 id collision

Left over from E21 (`63afe18`):

- Sessions started before `63afe18` read the old AGENTS.md: they may still add rows to the frozen
  `docs/tasks/ASKS.md` and try a bare `git push` (now blocked by the hook, which points them at
  `scripts/push-main.sh`). Offered to the user: one herdr message to every live pane — "ask files via
  `scripts/ask-new.sh`, push via `scripts/push-main.sh`, commits with `-- <paths>`".
- Id collision: `docs/tasks/asks/E24.md` is the app-store ask (enrol Apple / Play, moved from the legacy
  table), but `ce30632` added a legacy ASKS.md row `E24` for "fix the commit guard" (done) after the freeze.
  Give that one a fresh id with `scripts/ask-new.sh` (as a done file) and note the rename in its legacy row.
