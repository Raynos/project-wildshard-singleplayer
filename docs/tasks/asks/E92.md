# E92

**Status:** done (2026-09-22) — renumbered from the legacy ASKS.md row `E24`, which collided with docs/tasks/asks/E24.md (the app stores); E28
**Ask:** lol fix the commit guard if you can't do multi-line commits


`.claude/hooks/guard-git-add-all.sh`: not multi-line — an apostrophe inside a double-quoted `-m` paired with a later quote
and ate the ` -- <pathspec>`; now one perl pass strips both quote kinds left to right; 10/10 shape tests (5 allow, 5 still
block). (Moved from the legacy table's second `E24` row.)
