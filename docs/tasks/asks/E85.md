# E85

**Status:** in flight (2026-09-23, main session)
**Ask:** Permanently color grade on remove it from settings

Built: the LUT is always on; the pause-menu Colour grade switch and Game.ts' live opacity subscription are gone, a
saved `lut: off` is ignored. Only `?nolut` (the LUT fit's own captures, scripts/fit-lut.py) builds without it.
