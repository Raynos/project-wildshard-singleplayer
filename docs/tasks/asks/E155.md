# E155

**Status:** in flight (2026-09-25, a build subagent in its own worktree; merged onto main by the E154/E155 session) — the user picked **4B with subagent**: build the whole thing now, capped at 4 (docs/plans/SHARD-CACHE.md); E159 made the cap 2

**Ask:** why do shards reload? keep shards in memory (last four, evict on the fifth) so I can go to the main menu and switch between the three shards without the loading screen

- 2026-09-25, the subagent: built (M1–M5) on branch `worktree-agent-a28ef91886e36c097`, rebased on main 4ff49d2; not merged, not deployed. Headless desktop + phone runs PASS (scripts/e155-shard-switch.mjs; progress/268-e155-shard-switch-desktop.jpg, progress/269-e155-shard-switch-phone.jpg). Open: the memory of two resident shards on a real iPhone (SHARD-CACHE M0).

