# E155

**Status:** in flight (2026-09-25, a build subagent in its own worktree; merged onto main by the E154/E155 session) — the user picked **4B with subagent**: build the whole thing now, capped at 4 (docs/plans/SHARD-CACHE.md)
**Ask:** why do shards reload? keep shards in memory (last four, evict on the fifth) so I can go to the main menu and switch between the three shards without the loading screen

