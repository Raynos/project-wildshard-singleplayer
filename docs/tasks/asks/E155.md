# E155

**Status:** done (2026-09-25) — in-page shard switching, landed `85cc9e57` (13 commits), live `85cc9e5-muheee1q`. Prod run (phone, KTX2, scripts/e155-shard-switch.mjs --lose --debugcard): PASS, 6 steps, 1 document load, 0 page errors; a return to a resident shard 70 ms (Driftwood) / 96 ms (Pine Hollow), no loading screen, the frame the player left (Δ 1.6–2.9 / 255); a parked shard losing its GL context is evicted; Debug ▸ Memory readout + Shards in memory 2 → 1 evicts at once. First builds / the rebuild on a never-cached headless profile: 8.4–12.8 s (downloads); on a warm cache locally 2.4–3 s. Not yet measured on a real iPhone (docs/plans/SHARD-CACHE.md)

**Ask:** why do shards reload? keep shards in memory (last four, evict on the fifth) so I can go to the main menu and switch between the three shards without the loading screen

- 2026-09-25, the subagent: built (M1–M5) on branch `worktree-agent-a28ef91886e36c097`, rebased on main a5cf0da6 (E157 A + B); not merged, not deployed. Headless desktop + phone runs PASS, Images and KTX2 (scripts/e155-shard-switch.mjs, scripts/e155-tex-auto.mjs; progress/271-e155-shard-switch-desktop.jpg, 272-…-phone-images.jpg, 273-…-phone-ktx2.jpg). On the iPhone: pause ▸ Settings ▸ Debug ▸ Memory (Shards in memory 2 / 1 + a readout). Open: two resident shards' memory on a real iPhone (SHARD-CACHE M0).

