# E99

**Status:** needs pick (2026-09-24) — three branded looks built and switchable; A is the default until Jake names one

Pause ▸ Settings ▸ Debug ▸ Resume screen ▸ Look: A Title · B Glass · C Plate (Settings option `resume`, `?resume=a|b|c`).
Picking one previews it for 1.4 s. Board: art/resume/round-1-branded/board.jpg (live captures, dev server, 402×874 @3×).
- A · Title: the shard's portrait title art full-bleed under the title's scrim, the PROJECT WILDSHARD wordmark top left
  where the title has it, RESUMING + hairline + DRIFTWOOD ISLE low.
- B · Glass: the blurred still of the paused game under a navy glass card (corner brackets) holding the wordmark, RESUMING, the
  hairline and the shard name.
- C · Plate: brand navy `#0d1b26`, the wordmark, the shard mark, RESUMING, the shard name and the title's tagline.
Built: src/ui/Resume.ts (brand(), preview(), look classes), resume.css, index.html (markup + the recovery reload's first
paint reads the look and the shard's name / art), Settings `resume`, Menu Debug picker, main.ts brand at boot.
Verified: each look through the real hide → show path in the game; the picker saves `resume` and previews.
Not done: art/README.md index row (another agent has uncommitted edits in it).
**Ask:** I don't want a black resuming screen I want a branded resuming screen lol

