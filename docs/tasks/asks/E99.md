# E99

**Status:** needs you (2026-09-24) — live in `4dadb24-mufh1yk8`; Jake checks an app switch on the iPhone. Jake picked **A Title** ("A title looks best to me and simplest"); B / C and
the Debug picker removed

The RESUMING screen is the shard's title screen: its portrait title art full-bleed under the title's scrim, the PROJECT
WILDSHARD wordmark top left, RESUMING + hairline + the shard's name low. Board of the three looks:
art/resume/round-1-branded/board.jpg; the pick on each playable shard: art/resume/round-1-branded/picked-a-per-shard.jpg.
Per shard (Jake: "The resume title should change for all three shards"): the art and name come from the shard being
played (`getActiveChunk()` at boot; picking another shard on the title reloads with `?chunk=`). Verified on Driftwood Isle
and Pine Hollow, then on Nalati Grasslands once it landed on main (live `b3d4e00-muga27bg`, prod capture):
art/resume/round-1-branded/picked-a-three-shards.jpg.
Built: src/ui/Resume.ts (brand()), resume.css, index.html (markup + the recovery reload paints the shard's name / art
from sessionStorage), main.ts brand at boot.
Indexed in art/README.md (`a729579`).
**Ask:** I don't want a black resuming screen I want a branded resuming screen lol

