# E88

**Status:** done (2026-09-23, main session) — `c92bccb`, live in `c92bccb-mueuqkp7`
**Ask:** Post clean by default and remove toggle

Built: Driftwood builds only the clean L5 post chain (Game.buildComposer) — the cinematic pass is no longer built beside
it, so the unused pass costs nothing; the Post picker and the Debug card's Look lab section are gone, a saved `cinematic`
is ignored. Only `?post=cinematic` still builds the haze + grain + fringe chain there.
