# E34

**Status:** done (2026-09-22) — `6660585`, live in `6660585-mudfi3mz`
**Ask:** (HUD agent) "The black bar at the bottom is a huge black bar — 25 % transparency, 75 % solid, render a bit of the world behind it"

`src/ui/styles/touch.css` `.ws-touch-bar`: `rgba(6,10,18,0.75)` and the 10 px backdrop blur removed. The canvas was
always full-screen under the bar; the blur + 0.78 tint smeared the world to flat grey (and cost a full-width blur pass on
phones). 390×844 `?touch=1` check: pier planks + sword arm read through the bar.

_Was row "E26" in the legacy docs/tasks/ASKS.md — that id is taken by another ask's file (docs/tasks/asks/E26.md), so this
ask was re-claimed as E34 at the E21 migration._
