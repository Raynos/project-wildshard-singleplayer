# E124

**Status:** needs pick (2026-09-25, review of round 12): the corner BAG button now touches the minimap (NE tick gone, bigger glyph); live, ON by default, `?bagbtn=0` = off. Board art/hud/round-12-corner-touches-rim/board.jpg; commit 188fc54, live build 188fc54-muguhb85
**Pick (2026-09-25), the user:** "None really. I wanted a button that fits in cleanly with the minimap. I want a button that squares out the minimap, so that button should be having a circular edge, and it should [make] the top right corner of the minimap into a square, and we don't touch the pause button. You can build it and show it to me lol. The button even has top and right straight edge, and on the minimap the button has like a reverse moon edge or something."
**Built:** src/ui/BagButton.ts: a corner piece inside `.ws-minimap`. Its top and right edges are straight on the square around the circle; the third edge is a concave arc ~2.5 px outside the tick ring. It has a glass fill, cyan hairlines, a corner bracket, a backpack glyph, a pressed state, and a hit area ≥ 44 px out into the margin. On a phone the circle steps ~7 px down and left so the square lines up with PAUSE. The menu is split (Menu.ts `split`): BAG = Map · Inventory · Achievements (the button opens Inventory; the minimap / M open Map), PAUSED = Settings (+ Feedback when unlocked). The PAUSE chip is untouched.
**Round 12 (2026-09-25), the user on round 11:** "Make it bigger by touching the minimap. The little blue things that stick out from minimap in this corner can go away and the button can touch." Built: the NE tick is gone with the button on; the piece fills the whole corner of the square outside the circle, and its concave edge is the minimap rim itself (no stroke of its own: no gap, no doubled line). Short end caps stop it ~3 px past the N and E ticks. The glyph grew from 12×14 to 17×20 px on a phone. No BAG label: ~5 px text would not read. Round 11 was commit 3bf267b, build 3bf267b-mugu226k.
**Open:** the user's look at the board / on the phone. Not done: the art/README.md index row (that file carries another agent's uncommitted hunks); a separate trophies screen (Achievements is the trophies tab for now).
**Ask:** Show me a mockup (qwen 2.1) of an inventory button top right of the minimap; we can also move the minimap 5% to the left. Basically I want two menu buttons, inventory and pause. Pause takes you to settings and feedback. Inventory to map / inventory / trophies.

