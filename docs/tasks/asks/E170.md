# E170

**Status:** needs pick (2026-09-25). Jake: for each of the four, keep as is, restyle, or fix. They are taste and design calls, so none was changed.
**Ask:** (found in E168 while shooting the Steam trailer) four visual rough spots the trailer had to frame around: Driftwood's 1 px chunk-boundary lines read as scratches; the Storm Titan's storm wall reads as a flat grey slab from inside the arena; Pine Hollow from above looks bald (the forest thins with distance); the Driftwood lagoon is 1.5–3 m deep and underwater is a flat cyan wash — keep, restyle or fix each?


- Where each one lives:
  - `src/world/Boundary.ts`: `LineBasicMaterial` lines, always 1 device pixel.
  - `src/nalati/stormTitan.ts`: the `titan-storm-wall` cylinders, FX.stream material.
  - Pine Hollow's forest draw distance.
  - `src/world/Seabed.ts` / Driftwood's lagoon depth.
- The stills are in the E168 scouting passes. The trailer hides the boundary and the storm wall in its shots, and has no
  Pine aerials and no dives.
