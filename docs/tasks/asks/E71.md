# E71

**Status:** done (2026-09-23, main session) — build id below once live
**Ask:** build R3 sprint lock (HUD-REFINEMENTS): push the stick past its ring ~0.25 s and sprint latches with a lock icon; touching the stick again / pulling back cancels


Built: held past the ring (forward) 0.25 s → the sprint latches (SPRINT LOCK tag, a padlock on the ring's top); lift the
thumb and you keep running forward (knob parked at the top); touching the stick again, pulling back, the water or the board
end it. Headless drive: held 0.45 s → latched at 7.2 m/s, released → still 7.2 m/s after 0.8 s, touched → stopped
(progress/216). Also fixed: the SPRINT tag's `+` sibling rule, broken when E50 put the ORBIT arcs between stick and tag.
