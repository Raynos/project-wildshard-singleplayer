# E46

**Status:** done (2026-09-23, HUD build agent)
**Ask:** iterate on the live build of touch layout E to make it more polished


**Evidence:** commit `a281f4b` (live in build `9287cef-mueegpmm`, CI deploy run 35898409538 green).
- iOS (Jake's bugs): the touch layer cancels its own touch events (non-passive touchstart / touchmove / touchend), pinch
  gestures, dragstart / contextmenu / selectstart; every element draggable="false" + touch-action / user-select /
  touch-callout / user-drag none; ATTACK captures its own touch, only a lift or cancel ends a hold. iOS 26.5 Safari
  simulator with real XCUITest touches — live E42: a 1.6 s hold never charged (heavy 0), a double tap zoomed 1.6×;
  E46: the hold charges and chops (heavy 1, 0 pointercancel), no zoom, no selection (`progress/198`).
- Black strip (home-screen app): `src/core/viewport.ts` — the real height (`--ws-vh`) for #game / #hud / the canvas and
  the screen projections. Not reproducible in a simulator Safari tab: **Jake to confirm in the home-screen app.**
- Layout: divider 45 % / 55 %; ATTACK on the divider on the bar's centre line; LOOK pad = ATTACK's width, centred;
  stick 16 px clear of ATTACK at 390 (24 at 430); SWAP | HOVER on the divider above ATTACK, clear of the ring.
- Polish: DODGE / JUMP one size, even diagonal; AIM up-right of FIRE; 44 px hit areas; ATTACK swing flash; LOOK pad
  lights while dragging; SPRINT tag above the bar over the stick.
- Shots: `progress/202` rest sword, `203` rest ranged, `204` hold-heavy, `205` 430×932, `206` before / after vs 192.
