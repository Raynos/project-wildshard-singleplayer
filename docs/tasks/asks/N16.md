# N16

**Status:** done (2026-09-24) — the plan built and archived: project/archive/2026-09-24-nalati-merge.md; leftovers N10, N11, N20, N21, N22
**Ask:** main has moved a lot — review everything the other agents built in the last 24 h: physics (see how Pine Hollow and Driftwood did it, implement physics in Nalati), World Explorer, Model Explorer (add to Nalati), the HUD improvements + HUD mockups (where the new Nalati HUD elements go on top; iterate on the Nalati HUD), the Driftwood remaster techniques (tell me what we should do to Nalati)


**Decisions (the user, 2026-09-24):** merge commit (not a rebase) · touch HUD = main's HUD + Nalati extras, then HUD mockup
rounds · a Nalati quest line: yes · physics: FULL (every POI a registered collider, the horse on the motor, navmesh, ragdolls,
physics projectiles) · music: Nalati's own steppe theme (MiniMax Music 3, adaptive like Driftwood's) · sfx: the full Nalati set
regenerated with MOSS-SoundEffect v2 + Stable Audio 3 Medium, the better take per sound · order: in parallel with subagents
(≤ 3 at once, disjoint files).

**Decisions, wave 2 (the user, 2026-09-24):** far-background seam (N19): BLEND the painting (the panorama's lower band dissolves into fog / terrain colour, no new far geometry) · quest: three chained chapters (TULPAR → THE GOLDEN KING → FATHER OF THE WIND) · models: both pipelines (main's Blender + TRELLIS / Hunyuan), best per model in-engine, all through main's registry · a Nalati Look Lab (every look change a switchable variant + a comparison sheet; the user picks).

**Decisions, wave 3 (the user, 2026-09-24):** ride stick: the stick steers the horse, the camera follows (RDR2 / KCD first person), free-look drifts back · bow on touch: hold FIRE to draw, release to loose, early release = let down; AIM a toggle (zoom down the arrow) · Nalati lands on main when this plan is finished — the user merges it · on main Nalati is in the shard picker with an EARLY ACCESS tag.

**Decisions, wave 4 (the user, 2026-09-24):** HUD mockup round: all four layouts A–D (on foot + in the saddle each) · HOVER on Nalati: on foot only (hidden in the saddle) · lock-on: sabre + spear now against every Nalati enemy, the bow later (with N18) · Nalati phone target: 30 fps, max graphics (re-measure with main's 2× phone render scale and 80 m animal shadows).

**Decisions, wave 5 (the user, 2026-09-24):** DISMOUNT: the HORSE right-edge tab turns into DISMOUNT in the saddle (one toggle) · score: one Kazakh-folk score whatever the style setting, 3 zone variants (Nalati Grasslands / Sky Grassland / Snow Lotus Valley) crossfaded at the zone lines, calm / alert / combat / night / storm + stings · camp people: procedural painterly figures now (the elder is the quest giver), generated + rigged models later · bosses: open any time, the quest catches up from the saved boss state.

**Decisions, wave 6 (the user, 2026-09-24):** day clock: a sun / moon glyph on the minimap rim, no text · throat singing: a drone in the night + boss cues only, after a 6-take MiniMax instrument test · boss music: one track each (the Golden King; Jel Ata = the storm cue) · Look Lab, first variants: terrain in the shadow bake, baked terrain AO / bounce, the generated-model shading fix (the learned LUT not now).

**Decisions, wave 7 (the user, 2026-09-24):** the horse on its own CharacterMotor in the fixed step (after the hotfix), the rider's capsule off while mounted · climb: 40° on foot (main's rule), the horse its own limit (~35° at a gallop, steeper at a walk) · WebGPU: Nalati always runs WebGL · stampedes run through a player on foot, collide with a rider.

The user, 2026-09-24: "Ok I think that's enough lol you can finish the plan and mega build the plan with subagents" → project/archive/2026-09-24-nalati-merge.md, in progress.

**Decisions, wave 8 (the user, 2026-09-24):** clocks: one small interface over DayNight + DayClock now (Settings ▸ Time of day, Explore's light presets, the HUD glyph on every shard), one engine clock later · chapters: TULPAR → THE GOLDEN KING → FATHER OF THE WIND · Explore for Nalati: in the same change as the physics registration (one registry: drawn, colliding, explorable) · phone render scale: keep 2×, measure the camp at 30 fps, drop to 1.5× only if 2× can't hold it.

**HUD pick (the user, 2026-09-24):** D — status left (art/hud/round-12-nalati-merge/board.jpg).
