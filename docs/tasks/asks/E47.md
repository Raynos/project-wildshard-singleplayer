# E47

**Status:** done (2026-09-23, main session) — `6073b55`, live in `f6cc18c-mueneknw`
**Ask:** compact FPS pill next to the PAUSE button showing only e.g. '59 fps 17 ms'; tap it to expand and show calls + tris, expanding on top of the minimap; frees a row so the health bar moves up


Built (HUD build agent, finished + verified by the main session): on phones the frame meter is a glass pill on PAUSE's row
("60 fps 17 ms"); a tap opens a details panel over the minimap (frame p50 / p95, draw calls, triangles, tier · DPR, GL), a
tap anywhere else closes it; VITALS moved up to directly under PAUSE and the left column (✎ NOTE, boss bar, feed, toasts)
up with it. The pill cancels its own touches (no iOS zoom / callout) and never starts a look drag. Desktop keeps the
one-line readout. Verified 390×844 (progress/210); gates green on the exported tree.
