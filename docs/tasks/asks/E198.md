# E198

**Status:** open (2026-09-26) — parked: start only if Jake reports the warm phone below a steady 30 in Driftwood's grass (E189 closed on his "Call it done")
**Ask:** (leftover of E189, parked by Jake's 'Call it done') if the warm iPhone drops below a steady 30 in Driftwood's grass: the next debug cuts (near ground cover, bloom) with before/after boards; and the shadow pass re-deriving three's shared depth program 21x a frame (instanced vs non-instanced casters; V8 garbage)


## Where to start (from E189)

- Live since `881efe7-mui0qja5`: E142's depth slices + off-screen god-ray skip on Driftwood's phone tier, the far cascade
  (22–80 m) redrawn every other frame, FXAA instead of SMAA, no god rays (Driftwood phone only). Mobile is locked at 30 (E193).
- Jake's rules for cuts: "cut blindly, but show me before and after photos & videos … DEBUG CUTS, not commit and ship"
  (Debug ▸ Performance rows, default off; he picks, then the row goes and the cut becomes the default). He kept both so far.
  Allowed to give if needed: "far shadows softer". Ruler: the in-game probe (RUN PROBE → COPY, with a baseline per row);
  the Mac (M5) cannot rank phone costs. Capture scripts in E189's session scratchpad pattern: cutshots.mjs (4 variants,
  stills + 6 s canvas videos), then a labelled board + hstack video.
- Next cut candidates: the near ground cover (GroundCover.ts, E186 handed it over; keep E156's no-bounce / kites-matched /
  no-pop rules), bloom (levels / resolution).
- The shadow pass re-derives three's shared MeshDepthMaterial program 21× a frame: instanced vs non-instanced casters
  alternating (enemies, rope bridge, instanced props), not the shadow pieces (a piece-only depth material took it 22 → 21,
  reverted). Fix: a depth material per (instanced, instanceColor, receiveShadow) combination. CPU / garbage, not the GPU.
