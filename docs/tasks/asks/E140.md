# E140

**Status:** needs pick (2b: dead-items board) — items 1c 3a 4a 6a 7a shipped, 5b 8a no change; the dead items wait for the verdict
**Ask:** Item 6 (E126, dev screens): 1c a visible Developer switch in Settings turns dev mode on; 2b show me a board of the dead items first; 3a perf meter dev only; 4a player loading screen, details in dev; 5b Explore World stays a player feature; 6a build pill only when a new build waits, the id in dev; 7a Settings Debug card dev only; 8a Experimental / Early access stripes stay until release


**2026-09-25:** the Hover tab (the hoverboard) stays a player feature. The user: "Hover tab player feature".

**Phase 1 (2026-09-25, E140 subagent):**
- 1c: `src/core/devMode.ts` (`isDev` / `setDev` / `onDev`, localStorage `ws.dev`, `?dev` / `?dev=0` for one load, `<html data-dev>` for CSS,
  set by index.html before the loading panel paints). The switch: Settings ▸ DEVELOPER ▸ Developer mode in the pause menu and the
  title's Settings (`src/ui/devSwitch.ts`); live, no reload.
- 3a: the frame meter (desktop line + phone pill / panel) shows only in developer mode; `?perf=0` hides it, `?perf=1` shows it (budget check).
- 4a: players see the shard's name, one bar (download + setup, half each) and a short line; the tracks, step log, tier line, clock and
  "Loading chunk · <slug>" are developer mode's. index.html and src/boot/shell.ts stay identical.
- 6a: the build pill shows for players only when a new build waits ("New version · tap to update"); developer mode shows the id as before.
  The update mechanics are unchanged.
- 7a: the Settings DEBUG card is developer-mode only; Review (password, Quick note) moved out of it into Settings.
- 5b, 8a and the Hover tab: no change.
- Evidence: `progress/250-e140-dev-mode.jpg` (iPhone 16 Pro, player vs developer mode). Commit 468a1f3e, live build 468a1f3-mugwx8eu.

**Phase 2 (2b) board:** `art/dev-screens/round-1-dead-items/board.jpg` — 13 tiles. Waiting for "delete all" / "delete all except N".
Already gone before this ask: 4 (Pine Hollow step names; only the empty "Tree cards" / "Trees · 0 trees" rows are left) and 9 (the Island row, E136).

**2026-09-25, the user's verdict so far:**
- **1:** delete the chunk panel.
- **2:** the chunk boundary warning shows in developer mode only.
- **3:** keep the staging boundary for everyone, permanently ("permanently keep the staging boundary for everyone, number three"). This also means E36's cyan rectangle is intended.
- **4:** "loading step names is dev only" — phase 1 already did it; the empty rows ("Tree cards", "Trees · 0 trees") stay.
- **5:** delete the loading footer text; the slot stays for boot errors.
- **6:** the tagline "A world that does not exist yet, arriving one chunk at a time." shows in developer mode only.
- **7:** "Driftwood Isle · Shard 1" → "Driftwood Isle" (every shard).
- **8:** keep "Shown under your name in multiplayer" — titles only mean something in multiplayer, the line explains them.
- **9:** already removed by E136; nothing to do.
- **10, 11, 12:** delete the dead code (the in-game `askReload(…, 'game')` variant, the hidden `.ws-game-fps` readout + CSS, Explore's
  "Model Explorer lands next" toast).
- **13 and the wordmark:** the name stays. "Let's keep the name Project Wildshard … we don't have to rebrand" — tab title, manifest
  and the PROJECT WILDSHARD wordmark unchanged.
- **New ask:** "add the dev toggle to the main menu somewhere, like the title screen … a little toggle, compact, small … I definitely
  want to be able to not have to go into the settings to toggle dev on and off."

**Phase 2 (2026-09-25, E140 subagent):**
- 1: the desktop chunk panel is gone (markup, CSS, the per-frame pos writes).
- 2: the boundary warning shows only in developer mode (text unchanged; live — main.ts sets it every frame and HUD reads `isDev()`).
- 3: no change; E36 closed as the staging boundary gate, kept on purpose.
- 5: the footer strings are gone from `index.html` and `src/boot/shell.ts` (still identical); `data-el="foot"` stays for boot errors.
- 6: the tagline is hidden unless `<html data-dev>` (loading.css; the markup stays so dev mode keeps it).
- 7: the menu subtitle is the shard's display name alone.
- 10–12: deleted (`ReloadPrompt.askReload(host, what)` / `reloadWithPicks()` are title-only now; HUDState lost its `fps` field
  and every caller its `fps:`; `.ws-game-fps` and its 6 CSS rules; the Explore toast branch).
- The DEV toggle: title screen bottom row "SETTINGS · SOUND ON ■ · DEV ■" (styled as SOUND's sibling, ~40 px tall tap target
  inside the row), and a small "DEV ■" chip left of CLOSE in the pause menu header (it fits at 402 px). Both and the Settings ▸
  Developer switch share `devMode.ts`; the title's build pill flips between "<sha> · reload" and hidden live.
