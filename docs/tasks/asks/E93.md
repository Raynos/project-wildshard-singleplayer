# E93

**Ask:** iPhone check of E89 + E90: walk the Driftwood cove (palms / shadows / ground cover should not pop, shadows should hold still) and one run with Low Power Mode off for fps

**Status:** needs you — Jake's iPhone reading (the E89 / E90 fixes are live since 29c9e69-mueuyxwm)

What to do on the phone (Safari, Driftwood Isle):
1. Walk around the Blender cove (the south half: pier → beach → palms). Palms, their shadows and the ferns / bushes
   should no longer pop in; shadows should stand still (a faint shift about every 1.5 s is the 0.25° sun step).
2. Once with **Low Power Mode off**: does it hold 60 fps? E90 added +18–31 % triangles on the phone tier (headless
   still 60 fps).

Then, depending on the reading:
- frames drop → lower `LOD_D` in `src/world/BlenderIsland.ts` (110 m) until it holds;
- the 1.5 s shadow step is still visible → cross-fade two shadow maps between steps (costs a second shadow map on the
  phone; not built);
- a pop is still seen → where: past 110 m the palms still swap to the far copy, and animals go 3 → 2 → 1 draws at 45 /
  100 m (`animalEyeDist` / `animalOneDrawDist`, tier.ts) — neither was checked on the phone.
