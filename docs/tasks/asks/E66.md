# E66

**Status:** done (2026-09-23) — Pine Hollow only (the user: "build E67 and E66 ( pine hollow only )"); b95079e, live `b95079e-muena3vr`
**Ask:** (leftover from EXPLORE-WORLD, archived) Switch Explore World on for Pine Hollow / Nalati: set ChunkDef.explore + pois and register the shard's models (src/explore/registry.ts) — no Explore code needed (X10)


- Pine Hollow: `explore: true` + 7 map pins; `registerPineHollowModels` (src/explore/catalog.ts): 3 cabins + pond live, Scots pine (on a real tree), boulder / stump / log; tap-to-select on forest + props.
- Fixes found on the way: forest LOD / grass / mist followed the parked player, not Explore's camera; the Model Explorer showed the whole cabin group; the terrain splat shader's 0/0 NaN (bloom → black squares).
- Nalati: not switched on (its branch lives in ../wildshard-nalati-grasslands; the same two steps apply there: ChunkDef.explore + pois, and a register call in main.ts).
