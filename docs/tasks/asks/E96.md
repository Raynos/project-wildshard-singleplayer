# E96

**Status:** needs you (2026-09-24) — built and live (build id below); only Jake's iPhone can say whether the overnight case is gone

Read of the screenshots: the WebGL canvas still drew (the gulls fly) but nothing the DOM paints showed. The HUD, the
pause menu that `ws:background` puts up on hide, and the RESUMING screen were all missing, with white (unpainted) strips
top and left round a canvas shifted a few px. It looks like WebKit handing back a page after a night suspended without
repainting its DOM layer. No event fires for that, so it can't be caught and repaired in place.
Fix: `src/core/GpuRecovery.ts`: coming back after more than 10 min hidden (wall clock) does the recovery reload instead
of waking the page. The player gets the RESUMING screen, then lands where they stood under the pause menu (or on the title
if that is where they were), on the newest build when its worker is waiting (`sw.ts adopt(to)`).
Verified in Chromium on the dev server: a faked 2 min away does not reload; 11 min away reloads to `?at=…&glreload=1` and
lands under the pause menu. Not reproducible headless: the overnight WebKit state itself.
**Ask:** Went back to the game after not playing since yesterday: a still frame with a white border, the controls are gone, only the flying birds move. Backgrounding / resuming is still broken. (screenshots: white status-bar strip on top, white strip down the left edge, frame shifted right, no HUD)

