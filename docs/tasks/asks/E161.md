# E161

**Status:** in flight (2026-09-25) — the E158 subagent (SW cache GC + an unused-asset audit that deletes what nothing references); the E155 subagent's eviction frees an evicted shard's GPU / JS memory
**Ask:** cull and GC stale / unused stuff — stale SW caches and old-build entries, assets nothing references, what an evicted shard leaves behind

