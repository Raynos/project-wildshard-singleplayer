# E161

**Status:** done (2026-09-25) — the SW frees what the build no longer names, scripts/unused-assets.mjs (8 orphans, 0.66 MB) (`176021f0`, `94da474a`); an evicted shard releases its GL context, physics, listeners and world (six leak chains + Pine Hollow's NPC materials fixed; heap snapshots) (`85cc9e57`)
**Ask:** cull and GC stale / unused stuff — stale SW caches and old-build entries, assets nothing references, what an evicted shard leaves behind

