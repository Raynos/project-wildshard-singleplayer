# E161

**Status:** in flight (2026-09-25) — the cache + asset half landed (`176021f0`, `94da474a`; live `a9a60e5`): the SW frees every entry the build no longer names; scripts/unused-assets.mjs deleted 8 orphan files (0.66 MB) and warns in the pre-push gate. The evicted-shard memory half is the E155 build's
**Ask:** cull and GC stale / unused stuff — stale SW caches and old-build entries, assets nothing references, what an evicted shard leaves behind

