# E36

**Status:** open (2026-09-22)
**Ask:** (found by the HUD agent, E11 verification) a faint cyan rectangle outline floats over the sea near the horizon on Driftwood (390x844 ?touch=1, spawned 3.5 m from a boar on the beach, yaw 0.3) — not a HUD element; unidentified

Seen once in a 390×844 capture during E11's lock-on check (the HUD agent's session): a thin cyan rectangle outline, about
55×20 px, over the sea near the horizon, left of centre. Probing `#hud` found no element there, so it is drawn in the 3D
scene (a debug helper, a shrine / POI marker, or a collider box?). Not investigated further. To resume: open
`/?chunk=driftwood-isle&nolock=1&skipintro=1&touch=1` at 390×844, spawn next to a beach boar
(`__world.player.spawn(a.x + sin(0.3)·3.5, a.z + cos(0.3)·3.5, 0.3)`), pitch −0.15, and look for it. Then name the object,
and fix it if it shouldn't be there. **Status open, owner: none.**
