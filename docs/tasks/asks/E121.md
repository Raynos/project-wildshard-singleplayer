# E121

**Status:** done (2026-09-24)

**Fix:** src/main.ts: `hud.onExitToMenu` sets `fromTitle`; the next ENTER WORLD puts the player back on the spawn (`toSpawn`, the respawn without its death sting). A plain Resume from the pause menu does not (the same `enter` handler).
**Measured:** player at (40, 40) → Exit to main menu → ENTER WORLD: live build stays at (40, 40); the fix → (0, −235), the pier spawn.
**Ask:** When you exit to the main menu and enter the world again, it should reset the spawn position. It shouldn't have a persistent location in the world; it should just reset the spawn.

