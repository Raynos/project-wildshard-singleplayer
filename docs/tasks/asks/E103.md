# E103

**Status:** needs pick (2026-09-24). Optional: Jake sends guesses for any of the 6 blind rounds ("1A 2B …"), and the
next agent reveals the key and his score. Round → key: 1 Qwen-2.1 `~/ml/imagegen/out/blind-key.json` · 2 turbo
`blind-turbo-key.json` · 3 klein-4B `blind-klein4b-key.json` · 4 SenseNova `blind-sensenova-key.json` · 5 klein-9B
`blind-klein9b-key.json` · 6 Edit-2511 `blind-qwen2511-key.json` (all in `~/ml/imagegen/out/`).
**Ask:** send the codex vs Qwen images again for Claude iOS as a completely blind test (left/right, unlabelled) so I can judge them myself; research-only licence is fine for mockups, drop the licence talk


Sent 2026-09-24: 10 unlabelled A/B pairs (`~/ml/imagegen/blind.py`, out/blind/), codex side is a coin flip per pair, pairs shuffled, same crop / size / JPEG quality. Answer key: `~/ml/imagegen/out/blind-key.json` — reveal when Jake sends his guesses.
