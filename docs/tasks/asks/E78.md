# E78

**Status:** in flight (2026-09-23, main session)
**Ask:** Painted horizon: lock it in, on by default, no toggle

Built: `HorizonMatte` is always shown; the pause-menu Painted horizon switch and main.ts' live subscription are gone, a saved
`matte: off` from the old switch is ignored. Only `?matte=0` hides it (the before / after captures).
