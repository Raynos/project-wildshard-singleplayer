# E33

**Status:** done (2026-09-23) — `7821d7b`, live in `7821d7b-muefqh08`
**Ask:** "Yeah clean up disk lol. Minimax m3 is the winner everything else nah. Remaster all the music and audio in wildshard using minimax m3 audio"

- Disk cleanup: **done**. Every music model but MiniMax-Music3 was deleted (ACE-Step ×2 copies, HeartMuLa + its MLX conversion, both Stable Audio Open mirrors, t5-base, larger_clap_music), about 158 GB freed. Weights repo `258eb5b`.
- Remaster: project/archive/2026-09-23-music.md v3 (rows 4–10). MiniMax generates the music in all three styles, which stay behind the in-game switch. Demucs stems give the calm and tension layers. Sound effects get a MiniMax feasibility pass, keeping the synth as the fallback. The UI shows a "MiniMax-Music3" credit (licence).
- The user's picks (2026-09-22): title screen plays the title theme; ship auto-picks, veto later; piano is the default + trailer style; SFX that MiniMax can't do get a local SFX-model bake-off (not the synth).
- Game code live: `03ed269` / `c84345d` (stem player, Music style picker, MiniMax-Music3 credit, shrine hum); synth plays until the generated files land.
- Done 2026-09-23 (`7821d7b`, build `7821d7b-muefqh08`). Music: MiniMax Music 3, three styles, piano the default, demucs calm /
  tension stems, title themes, stings, re-scored trailers. Sound effects (AGENTS.md "Audio engines"): ONE generated set
  `public/assets/sfx/best/`, per sound the lower CLAP rank of MOSS-SoundEffect v2 vs Stable Audio 3 Medium (MOSS 30,
  SA3 Medium 20, `jump` stays synth); Settings ▸ Sound effects = Generated · Synth. Plan archived:
  project/archive/2026-09-23-music.md. Leftovers: E56 (Stability AI registration), E57 (phone listen / vetoes).
