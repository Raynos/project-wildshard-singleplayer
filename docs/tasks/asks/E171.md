# E171

**Status:** done (2026-09-25) — commits 32375b8e, a3922b58; live build `a3922b5-muhtwg32`
**Ask:** delete pine wood branch and nalati branch in git

- `nalati-grasslands` (b3d4e00e) and `pine-hollow-remaster` (dff063ff) were both fully merged into main
  (0 commits ahead, local and origin).
- Their worktrees were dead (folders gone / only a `.DS_Store`); `git worktree prune` cleared them.
- Jake ran the branch delete and the origin delete himself (dcg refuses agent branch deletes): both are gone
  locally and on origin, and a `git fetch --prune` shows no nalati/pine refs left.
