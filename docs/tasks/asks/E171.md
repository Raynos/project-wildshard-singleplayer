# E171

**Status:** needs you (2026-09-25). dcg refuses agent branch deletes; Jake runs the command below.
**Ask:** delete pine wood branch and nalati branch in git

- `nalati-grasslands` (b3d4e00e) and `pine-hollow-remaster` (dff063ff) are both fully merged into main
  (0 commits ahead, local and origin).
- Their worktrees were dead (folders gone / only a `.DS_Store`); `git worktree prune` cleared them.
- To finish: `git branch -d nalati-grasslands pine-hollow-remaster && SKIP_PUSHLOCK=1 git push origin --delete nalati-grasslands pine-hollow-remaster`
