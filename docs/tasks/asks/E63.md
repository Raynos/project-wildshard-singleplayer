# E63

**Status:** needs pick (2026-09-23) — both built and live (`f6cc18c`, `f6cc18c-mueneknw`); Jake plays T DODGE vs V DODGE and says which to delete
**Ask:** implement dodge variants T (lean + smear) and V (roll-dip) side by side as two buttons, 'T DODGE' and 'V DODGE', so Jake can play both and say which one to delete


Built (main session) from docs/plans/DODGE-FEEL.md: the same dash, two feels, two buttons (V DODGE left of T DODGE; desktop
Left Alt = T, Shift + Left Alt = V), one shared 0.8 s cooldown (E59).
- T "lean + smear": camera roll 7° into the side on the shared envelope (load 0–40 ms, peak 120, spring overshoot ~390),
  4 cm lead, 7 cm dip, +5° FOV built over 40–120 ms; the blade flung against the dodge on a lateral spring and whipping
  back; horizontal streaks slide across the outer thirds (CSS, no WebGL cost). A backstep pitches up 1.5° instead.
- V "grounded roll-dip": a 22 cm dive by 150 ms and a rise with a small overshoot, the view tipped 4° down, 3° roll, +10°
  FOV, the blade tucked flat (TUCK), a darker vignette pulse (CSS).
- Not built yet (either variant): V's dust / splinters (a new particle program), the per-variant whoosh / foot-plant
  sounds and haptics, a Reduce-motion setting (DODGE-FEEL D1–D3, D5). Slow-mo frames: progress/213.
