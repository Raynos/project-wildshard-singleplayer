# E131

**Status:** needs you (2026-09-25): a defensive fix is live (036b225 + 683c765, in build `e9970c1-muh9w0ps`) but the bug never reproduced off the user's phone (Chromium, iOS 26.5 simulator Safari). The user: on the title screen in the home-screen app, rotate to landscape and back; say whether the shard card stays centred (if not: a screenshot)
**Not reproduced** in Chromium (390×844 ↔ 844×390, slow and instant flips: the selected card stays centred, offset 0 px) nor in the iOS 26.5 simulator's Safari (iPhone 17 Pro, 5 rotations incl. 0.6 s flips; landscape shows the rotate gate). The user's screenshot is the home-screen (standalone) app; the card sits ~half a landscape width right of centre, i.e. `place()` ran with the landscape strip width after the turn back.
**Fix:** src/ui/HUD.ts: a ResizeObserver on the deck strip re-centres the selected card (and zeroes any native scrollLeft) whenever the strip's box really changes, after layout — not only on `resize`, which iOS can fire before the new layout settles.
**Ask:** On main menu when you rotate to landscape and back the hero is scrolled and positioned not center

