# E115

**Status:** done (2026-09-24)

**Cause:** on the phone tier the whole island shares one 1024 px shadow cascade reaching 80 m (src/core/tier.ts), with a 0.14 m normal bias (Sky.ts, against deck acne). On the turntable a 6 m model got a hand's width per shadow texel: blocky blobs, a map that re-fits (swims) as the camera orbits, and light leaking past every edge.
**Fix:** src/explore/ModelExplorer.ts `fitShadows` / `releaseShadows`: while a model is on show the cascade ends just past it (camera distance + the disc radius × 1.4), the map is 2048, the normal bias ~2.5 texels; everything restored on leaving the model.
**Evidence:** progress/224-e115-turntable-shadows-before-after.jpg (lookout + hut, live vs fix, phone tier). In-world phone shadows are the same coarse cascade: E122.
**Ask:** (Explore Models turntable, the hut + the lookout stairs) something's off: the graphics are pixelated, bleeding, not focused, flickering, blurry, not crisp. The shadows are jumping around. The shadow of the stair looks like a blurry, blobby mess.

