# E105

**Status:** needs pick (2026-09-24) — the user says yes / no: draw Driftwood's structures + paths on the MAP tab (not built; Minimap.paintRegion's ocean branch returns after the pier)
**Ask:** (from E97, the user: "I think the main map is stale as well … I can't tell") Driftwood's full map draws only ground, sea and the pier — no hut, sand path, palms, rope bridge, wreck — so it reads as out of date. Draw the island's built things on the map?

