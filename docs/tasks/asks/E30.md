# E30

**Status:** open (2026-09-22) — waits on the native apps (docs/plans/NATIVE-APPS.md, E23 / E24)
**Ask:** (leftover F7 of the feedback inbox, E26) prove a review note sent from the **native iOS / Android build** lands in the inbox

The code is in `1e46950`. In the native build (`import.meta.env.MODE === 'native'`), `src/ui/review.ts` posts to
`https://wildshard-singleplayer.vercel.app/api/inbox`, and `api/inbox.ts` answers CORS for `capacitor://localhost`,
`https://localhost` and `http://localhost` only. Still to do: on the simulator / emulator build, unlock in Settings →
REVIEW, send a note, and check that `pnpm inbox:pull` shows it. If the WebView's origin is not one of the three above,
add it to `NATIVE_ORIGINS` in `api/inbox.ts`.

From: project/archive/2026-09-22-feedback-inbox.md (F7)
