# E76

**Status:** done (2026-09-23, fixed by the E7 session on the user's word) — `b421a8a`, live in `b421a8a-muet7s50`: test/physics-navmesh.test.ts: a 30-query warm-up before the timed pass (JIT + lazy tables out of the mean), and the wall-clock bounds (parse < 1000 ms, mean < 0.3 ms, cross-map < 5 ms) get 4× headroom when `CI` is set; path counts and correctness unchanged
**Ask:** (found at exit, E7 session) test/physics-navmesh.test.ts is flaky on the CI runner: a path-timing assertion (mean < 0.3 ms) failed at 0.317 ms on a docs-only push (run 35939202501), passed a minute earlier — a red deploy for everyone when it trips

