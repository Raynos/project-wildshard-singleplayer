# E178

**Status:** done (2026-09-25) — `fdba4b6e`: PAUSE header [RESUME] PAUSED ·DEV [EXIT TO MAIN] (pinned; the full-width Resume / Exit buttons gone), BAG header [CLOSE] BAG ·DEV (top-left always = back to the game, so a bag-closing thumb never hits EXIT). progress/278
**Ask:** pause menu redesign: top banner [RESUME] PAUSED {dev} [EXIT TO MAIN] — both main actions fixed at the top; remove the full-width Resume / Exit to main menu buttons (Close = the same as Resume)

