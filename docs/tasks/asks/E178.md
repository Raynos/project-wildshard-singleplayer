# E178

**Status:** in flight (2026-09-25, a build subagent: [RESUME] PAUSED ·dev [EXIT TO MAIN] pinned; the bag menu keeps CLOSE)
**Ask:** pause menu redesign: top banner [RESUME] PAUSED {dev} [EXIT TO MAIN] — both main actions fixed at the top; remove the full-width Resume / Exit to main menu buttons (Close = the same as Resume)

