# E142

**Status:** needs you (2026-09-25) — both lanes shipped (density + phone knobs a4afbe53; the heavy GPU lane: point-light skip a80cda9, the on-device probe 04f7f4b, card sort d51c3d8; live `33554ce-muh0fcot`). **Dynamic resolution (4b87508) is REMOVED** — Jake: "Dynamic resolution is a hack … a complete bullshit hack. We should not be going back from 2 to 1.25. We should just be doing performance optimizations necessary for hitting 30 FPS at 2." The phone renders at a fixed 2×; the 30-fps-at-2× lane below cuts real cost only. The Mac's GPU frame at the phone's size is ~2 ms, so most of Jake's 71 ms is unexplained: **Jake runs the probe once** — at the stones, tap the fps pill ▸ RUN PROBE, hold still ~60 s, screenshot the panel (details below).
**Ask:** Pine Hollow on Jake's iPhone runs at 14 fps / 71 ms (target: a steady 30) — 'performs really badly'

Evidence: `progress/pine-hollow-iphone-14fps-dense.jpg` (Jake's iPhone, live build 18b3d6b+, the old-growth by the King's
standing stones: the FPS pill reads **14 fps 71 ms**; charging, not Low Power Mode). Headless phone-tier runs at 4× CPU held
30 fps — they measure the CPU; the phone is GPU / fill-bound (2× render scale on a 3× screen, layered alpha-tested foliage
cards, shrubs × 12 and the denser Hollow from look round 3, the Blender species set's card clusters, shadows, the volumetric
march). Fix lane started 2026-09-25 by the Pine Hollow session (wA:p1).

## 30 fps at 2× lane (2026-09-25) — real cost cuts only, the render scale stays 2×

Jake (after the density cut + the first levers, on his iPhone): "25 to 30 FPS. It's a lot better, but it's not perfect."
And: "Dynamic resolution is a hack … a complete bullshit hack. We should not be going back from 2 to 1.25. We should just
be doing performance optimizations necessary for hitting 30 FPS at 2." So the dynamic resolution is **removed**
(ee63606d) and every lever here keeps the phone at 2× and the picture as it is — a same-moment A/B per lever (the frame
frozen, drawn with the lever off / on, diffed); anything visible would go to a board instead of shipping. Levers are
Pine Hollow phone tier only (`tier.ts phoneCut`), each with its own `?<lever>=0`, `?at2x=0` = all off.

Ruler: `scripts/pine-hollow-gpu.mjs` on a clean export (M5 Max, 1206×2622 — at the game's own 804×1748 the M5 sits at
its CPU submit and the subtractions saturate), each lever switched at runtime, base / lever rounds alternating, p20.
GPU ms per frame before → after (the saving):

| lever | commit | gate | hollow | stones (Jake's) | old-growth | pond | lookout | A/B |
|---|---|---:|---:|---:|---:|---:|---:|---|
| baseline (HEAD ee63606d) | — | 2.00 | 2.06 | 2.38 | 2.78 | 2.45 | 2.13 | — |
| **depth slices**: the viewmodels draw into near slices of the depth range instead of clearing, so the world's depth needs no mid-pass copy + merge; the depth readers read the scene's own depth (`worldDepth.ts`, `?depthslice=0`) | see git log | −0.13 | −0.13 | −0.11 | −0.18 | −0.15 | −0.15 | the world identical; on the crossbow the arrow-on-rail intersection rounds the other way on ~0.01–0.03 % of the frame's pixels (zoomed below) |
| **god rays skipped off-screen**: the rays' four passes (a full-size depth copy, the sun mask, the blur, the march) only while the sun's bounds are in the view; out of it they are black and the SCREEN blend is the identity (`Game.ts skipRaysOffscreen`, `?rayskip=0`) | see git log | −0.07 | −0.08 | −0.09 | −0.08 | −0.12 | −0.09 | identical (the diff = the frame's own noise); the sun was out of view at all six poses |

| **the IBL refresh in steps**: every 2 s the sky was re-rendered and PMREM'd (r186's GGX prefilter: ten 256-sample passes) in ONE frame — ~2.3 ms of the M5's GPU, more than a whole phone frame at 2×, i.e. a hitch every 2 s the frozen-frame table never sees. Now one step a frame (the top level, then one GGX level; 11 frames), in place (`PineDayNight.stepEnvironment`, `?envsteps=0`) | see git log | the hitch frame: **+2.3 ms → ≤ +0.5 ms** a frame over 11 frames (stones, pond) | | | | | | a whole stepped cycle = the whole refresh (0.001 % of px > 2/255, the frame's noise); mid-cycle (new top levels, old rough ones) vs the new sky: mean 0.22–0.29/255, the same as the 2 s-old IBL itself |
| **terrain noise only where its layer is**: the ground shader's value noises (the trail verge, the two-sampling blend, the open floor, the heath, the moss) are evaluated only where their layer weight is > 0 / a near sampling is read — a 0 weight zeroed them anyway (`Terrain.ts BOREAL_MAP`, all tiers: the same program output) | see git log | −0.03 | −0.01 | −0.02 | −0.02 | −0.04 | −0.03 | identical (the diff = the frame's own noise) |

Evidence: `progress/pine-hollow-E142-at2x-depth-rays-ab.jpg` (pond, crossbow out: lever off | on | diff ×8, and the
arrow zoomed). Why these first: on a tile GPU a mid-pass copy ends the scene pass (colour + depth stored, copied,
reloaded), and the phone's memory bandwidth is ~10× narrower than the M5's — the M5 under-reads them.

## Heavy GPU lane (2026-09-25, the structural GPU work; the fix lane keeps density + the tier knobs)

**The ruler: `scripts/pine-hollow-gpu.mjs`** (serve a clean export with `vite preview`; one muted Metal Chromium). The iPhone
16 Pro's real screen (402×874 @3), Pine Hollow paused at each pose, the frozen frame drawn by the tool:

- **GPU ms/frame** = 12 frames back to back, then a real GPU sync (a fresh 1-px write + readPixels), ÷ 12; the 20th percentile
  of 24 rounds (other GPU clients on the box only ever add time). Stable to ~2 %, scales with the pixel count.
  EXT_disjoint_timer_query_webgl2 exists on ANGLE Metal but over-reads 2× whole and 10× once cut per pass, and gl.finish()
  does not wait for the GPU at all — so no per-pass timeline: per-pass costs are **subtractions** (the frame with that pass
  or group switched off, base and toggled rounds alternating; `null` = the A/B's own noise floor, ±0.08 ms).
- **Overdraw** = every material swapped to add 1/255 per fragment, no depth test (R opaque, G alpha-tested, B transparent);
  `raster` = alpha test off, i.e. every fragment the GPU shades before it can discard.
- Measured at pixel ratio 3 (1206×2622, `--scale=3`): at the game's 2 the M5 is within ~0.5 ms of its CPU submit and the
  subtractions saturate. The game's own pixel ratio 2 (804×1748) reads 1.9–2.2 ms.

Baseline, HEAD 468a1f3 (M5 Max, ms per frame at 1206×2622; each column = what the frame saves with that thing off):

| pose | GPU ms | cpu submit | shadow | depth copy | colour chain | vol | god rays | bloom | LUT | SMAA | all alpha-tested | forest cards | far impostors | bark | twigs | grass | undergrowth | terrain | water | sky |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| gate | 2.57 | 1.60 | 0.50 | 0.16 | 0.43 | 0.03 | 0.11 | 0.21 | 0.01 | 0.07 | 0.60 | 0.41 | 0.03 | 0.09 | 0.17 | 0.10 | −0.02 | 0.47 | −0.08 | 0.16 |
| hollow | 2.75 | 2.50 | 0.61 | 0.12 | 0.47 | 0.05 | 0.13 | 0.16 | 0.03 | 0.05 | 0.95 | 0.52 | 0.06 | 0.05 | 0.07 | 0.11 | 0.07 | 0.58 | −0.06 | 0.26 |
| stones (Jake's) | 3.13 | 2.10 | 0.51 | 0.05 | 0.39 | 0.04 | 0.28 | 0.42 | 0.13 | 0.33 | 0.88 | 0.72 | −0.13 | 0.00 | 0.02 | 0.12 | −0.13 | 0.84 | 0.05 | 0.19 |
| old-growth | 3.07 | 1.70 | 0.68 | 0.18 | 0.55 | −0.03 | 0.02 | 0.25 | −0.02 | −0.09 | 1.03 | 0.69 | 0.28 | −0.14 | 0.09 | 0.05 | 0.05 | 0.62 | 0.02 | 0.22 |
| pond | 2.97 | 1.70 | 0.67 | 0.16 | 0.38 | 0.08 | 0.20 | 0.17 | 0.00 | 0.12 | 0.60 | 0.27 | 0.01 | 0.11 | 0.02 | 0.14 | 0.07 | 0.92 | 0.03 | 0.44 |
| lookout | 2.75 | 1.80 | 0.57 | 0.16 | 0.38 | 0.08 | 0.08 | 0.00 | −0.04 | 0.10 | 0.74 | 0.43 | 0.08 | 0.07 | 0.07 | 0.00 | 0.02 | 0.70 | 0.03 | 0.25 |

Overdraw (layers per pixel, mean; p95 in brackets) — `progress/pine-hollow-gpu-overdraw-baseline.jpg` (gate · hollow ·
stones · old-growth · pond · lookout; black 0 · blue 1 · cyan 2 · green 4 · yellow 8 · red 16 · white 32+):

| pose | opaque | alpha-tested kept | alpha-tested rasterised | transparent | all rasterised | px > 4 layers |
|---|---:|---:|---:|---:|---:|---:|
| gate | 2.23 | 2.20 (16) | 3.27 (25) | 1.25 | 6.76 (29) | 20 % |
| hollow | 2.35 | 2.45 (14) | 5.58 (35) | 1.81 | 9.74 (40) | 43 % |
| stones | 1.82 | 1.76 (9) | 4.08 (25) | 1.19 | 7.09 (28) | 27 % |
| old-growth | 3.12 | 6.91 (20) | **20.54 (60)** | 1.16 | 24.81 (64) | 66 % |
| pond | 2.37 | 1.56 (10) | 3.22 (22) | 1.35 | 6.93 (26) | 35 % |
| lookout | 2.18 | 1.93 (12) | 2.67 (20) | 1.21 | 6.06 (24) | 26 % |

What it says: the frame is spread wide — alpha-tested foliage 20–35 % (the needle cards most of it; a crown is 16–32+
card layers deep), **the terrain's splat shader 17–31 %** (one full-screen layer, but a dear one), the shadow cascade
~20 %, the colour chain ~15 % (bloom + god rays; the volumetric march is only ~0.05 ms = 2 %, cheap to keep), the sky
5–15 %, the depth copy ~5 %. No single switch halves it; the levers below stack.

**Calibration (low confidence).** Jake's 71 ms at the stones against ~1.9 ms here at the game's own 804×1748 is ~37×; the
A18 Pro's GPU is ~7–9× narrower than the M5 Max's (cores × clock, bandwidth). So the WebGL frame alone should be ~15–20 ms
on his phone: the rest of the 71 ms is likely not this GPU work — iOS compositing (the HUD's ~15 `backdrop-filter: blur`
panels and discs re-blurred over a canvas that changes every frame), the main thread, thermal (charging). The HUD blur is flagged
for a phone A/B.

## Fix lane: the density cut + the phone knobs (2026-09-25)

**Built.**
- **E143's forest cut** (`pine-hollow.ts`): trees 1 666 → 962 (918 on the baked grid); within 100 m of the Hollow
  413 → 126; within 100 m of the old-growth 217 → 115; shrubs 18 000 → 3 750; ferns 9 000 → 6 000. The numbers are in
  E143.md.
- **Pine Hollow's phone knobs** (`tier.ts PINE_HOLLOW_PHONE`, only on the phone tier with `?chunk=pine-hollow`;
  `?phknobs=0` = the phone row as before):
  - hi → lo tree cards and the shadow cascade at 60 m, not 80 (E94 keeps them together);
  - animal shadows to 60 m;
  - the grass carpet at 40 slots per cell, not 56.

**Per-lever A/B** (scratch `gpu.mjs`: the frame rendered 12× back to back + a GPU sync, base / toggled alternating ×3, median
Δ; drawn at 4× the pixels (1608×3496) so the M5 is fill-bound like the phone; runtime toggles, HEAD `d0fbfb8`):

| lever (runtime toggle) | stones Δ | Hollow grove Δ | applied? |
|---|---:|---:|---|
| render scale × 0.75 (dpr 2 → 1.5) | **−34 %** | **−35 %** | no — **resolution is not a lever** (E70: 1.5 "really bad and blurry"; the dynamic resolution that stepped it is removed, Jake: "a complete bullshit hack") |
| whole forest hidden | −26 % | −14 % | as the density cut (E143) |
| shadow map not re-drawn | −13 % | −19 % | cascade 80 → 60 m. The runtime A/B of that alone read 0 on the M5 (it draws triangles, not pixels); −0.1–0.2 M phone tris |
| animals hidden | −8 % | ±noise | no |
| grass hidden | −7 % | −2 % | slots 56 → 40 |
| SMAA off | −4 % | — | no (the foliage cards alias badly without it) |
| bloom / god rays / volumetric march off | −3 % each | −2 to −5 % | no (the look); see the heavy lane's table |
| understory hidden | 0 % | 0 % | cut anyway (E143 look) |

**The builds** (the heavy lane's `pine-hollow-gpu.mjs`, `--subtract=0`: GPU ms/frame at the game's own 804×1748, p20 of 24
rounds; overdraw = every fragment rasterised per pixel; phone tris). Before `d0fbfb8` → after (the cut + the knobs); the
knobs alone = after vs `?phknobs=0`:

| pose | GPU ms (M5) | all layers / px | alpha-tested layers / px | phone tris | knobs alone: tris · layers |
|---|---|---|---|---|---|
| gate | 1.72 → **1.46** | 6.77 → 4.55 | 3.28 → 1.12 | 1.41 → 1.05 M | 1.21 → 1.05 M · 4.57 → 4.55 |
| Hollow | 2.15 → **1.71** | 9.76 → 4.66 | 5.58 → 0.60 | 1.72 → 1.22 M | 1.40 → 1.22 M · = |
| stones (Jake's) | 2.01 → **1.68** | 7.09 → 5.56 | 4.08 → 2.64 | 1.20 → 0.84 M | 1.10 → 0.84 M · 6.91 → 5.56 |
| old-growth | 2.18 → **1.82** | 24.82 → 12.67 | 20.54 → 9.04 | 1.72 → 1.23 M | 1.43 → 1.23 M · 12.77 → 12.67 |
| pond | 2.03 → **1.72** | 6.93 → 4.43 | 3.22 → 0.81 | 1.16 → 0.90 M | 1.06 → 0.90 M · 4.49 → 4.43 |
| lookout | 1.97 → **1.47** | 6.06 → 4.59 | 2.67 → 1.22 | 1.34 → 1.07 M | 1.28 → 1.07 M · 4.72 → 4.59 |

Draw calls barely move (88–136 → 89–124): the forest is instanced/batched, so the cut shows up in triangles and overdraw.

**What this means for the iPhone (low confidence).** The M5's GPU frame drops 15–25 % at every pose. The overdraw halves
in the Hollow and the old-growth; at the stones it drops by 22 %, since the arena itself is bare and it is the grass there.
Scaled linearly from Jake's 71 ms at the stones, that is **~55–60 ms, i.e. still ~17–18 fps at his spot** if the phone
were purely GPU-bound. It gets more in the forest poses. It does not reach a locked 30 at the stones. The 71 ms is ~37×
the M5's GPU frame, and the A18 Pro's GPU is only ~7–9× narrower (see the heavy lane's calibration above). So much of the
phone's frame is probably not this GPU work. The candidates are the HUD's blur panels, the main thread, and thermal state
while charging; the Ghost Stag event was also up in his photo. Only the phone can say which: the re-check above.

Evidence: `progress/pine-hollow-E142-before-after.jpg` (phone FP, iPhone 16 Pro, gate · Hollow · stones · pond ·
old-growth, before | after, the numbers under each pair).

### Heavy lane — levers shipped (M5 ms per frame at 1206×2622 unless said)

| lever | commit | live build | before → after |
|---|---|---|---|
| the GPU ruler `scripts/pine-hollow-gpu.mjs` + this table | 70fef4e / 8b2a557 | 4b87508-mugya2u0 | — |
| ~~dynamic resolution~~ — **REMOVED** (Jake: "a hack … a complete bullshit hack"; the phone stays at 2×): the file, its Game hook, the Settings ▸ Debug row, `?dynres` / `?dynresmin`, its test | ~~4b87508~~ | — | headless GPU-bound 7680×4320: 30 fps held at 3.6 of 4 (settles, doubling hold-off); at a normal load it never moves |
| **point lights skip their BRDF where they add 0** (the 5-light pool, dark by day / past 10 m) | a80cda9 | a4cb559-mugytazr | the pool's cost 0.26–0.30 → 0.07–0.11 ms (~7 % of the frame); night A/B by the cabin byte-identical |
| **on-device probe**: fps pill ▸ RUN PROBE, or `?probe=1` | 04f7f4b | 04f7f4b-mugzt5yj | — (the phone's own numbers, below) |
| **needle cards front to back** (BatchedMesh sort) | d51c3d8 | d51c3d8-muh0b1l7 | −0.08 ms stones, −0.12 old-growth; same image |

**The non-3D suspects (the fix lane's relay), measured:**
- *Main-thread JS*: the iOS Simulator (Safari's JSC, iOS 26.5, iPhone 17 Pro, the Ghost Stag out at 19 m) runs a frame's
  JavaScript in **3.6–3.9 ms** (2.5 of it three's draw submission) — not the 71 ms, even at 2× slower on the A18.
- *HUD backdrop blur*: the build's CSS minifier keeps only `-webkit-backdrop-filter`, so Chrome (every headless ruler) never
  draws it; Safari does. In the Simulator, 40 full-screen blur layers at opacity 0 (the pause menu, journal, trader / board
  panels sit in the DOM like that) load the GPU **exactly like none** — WebKit does not composite them. Visible ones cost
  ~0.12 ms each on the M5 per full screen; the live HUD shows only ~7 small blurred discs / chips (~3 % of the screen).
  So the blur is not the 71 ms either; the probe's `no HUD blur` / `no HUD` rows will say what it costs on the phone.
- **Jake: one run of the probe settles it.** Developer mode on, at the stones: tap the fps pill ▸ **RUN PROBE**, hold
  still ~60 s, screenshot the panel (or open `…/?chunk=pine-hollow&elite=ghost-stag&from=19&probe=1`). Each row is
  uncapped with one thing off, all at the fixed 2× (the `scale 1.0` row is gone: resolution is not a lever); the row whose
  `frame` drops most is the bottleneck (`flat terrain` → the ground's splat shader, `no cards` → the needle cards,
  `no shadows` / `no post` → those passes, `no HUD blur` / `no HUD` → iOS compositing, `no scene` → the floor, all rows ≈
  equal with a high `js` → the main thread).


After the fix lane's density cut + these (HEAD d51c3d8, M5, 1206×2622; `progress/pine-hollow-gpu-after-density-s3.json`):
gate 2.57 → **2.01**, hollow 2.75 → **2.05**, stones 3.13 → **2.41**, old-growth 3.07 → **2.65**, pond 2.97 → **2.47**,
lookout 2.75 → **2.08** ms (−17…−25 %). The old-growth's alpha-tested layers rasterised per pixel 20.5 → 9.0. What is left,
largest first: the terrain's splat shader 0.37–0.85, the colour chain 0.34–0.43 (god rays + bloom 0.23–0.29), the shadow
map 0.25–0.43, the needle cards 0.12–0.92 (old-growth). Untried / look-changing levers: PINE-HOLLOW-FOLLOWUPS.md "GPU levers
not taken" (F-G1…F-G10).
