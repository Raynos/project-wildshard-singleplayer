# E40

**Status:** open (2026-09-22) — E5's lane (src/audio/**)
**Ask:** v1 synth fallback: the kick stays on after the theme loops back (found by the D42 music agent); fix + unreviewed Driftwood / citadel synth themes parked in project/parked/d42-synth-themes.patch

The E1 music agent (D42, stopped when the user redirected music to MUSIC v2 / E5) found that in the v1 synth score
the kick layer stays on when the theme wraps back to its start. Its uncommitted work — the fix, a composer pass on
wildshard-theme.ts, Driftwood + citadel synth themes, flute / steel-pan patches — is parked, **never type-checked or
listened to**, in `project/parked/d42-synth-themes.patch` (reverse-applies at `bc6d4d4`; src/audio has moved since, so
cherry-pick by hand). Also from its analysis: 60–74 % of the v1 theme's energy is under 200 Hz (drone / pad / bass pile
up at 70–335 Hz), ~1 % above 2 kHz. Only matters while the synth is the fallback; drop this if MUSIC v3 retired it.
