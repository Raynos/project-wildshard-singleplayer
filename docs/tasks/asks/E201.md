# E201

**Status:** in flight (2026-09-26, Codex)
**Ask:** Couple of things in the Explorer worldview. The model explorer is empty, which is surprising to me. The empty state of the model explorer doesn't have a good UI in the model explorer, so come up with suggestions, like mockups for the empty state of the model explorer, and ask yourself why is the model explorer empty? Are there no models here? Is everything procedural? Is there any Blender? Is there any Trelis assets? Is there any QuanHuan, the other model? There's two model generators, right? Two asset generators: the Chinese one and Trelis. Yeah, why is the model explorer empty?

Nine Dragon has GLBs in `public/assets/nine-dragon/`, including TRELLIS.2 lion, crowd, and guard assets and Blender-finished rig/hook parts. Most city geometry is procedural and batched. Its five world-registry pieces have no `model` metadata, and `Explore.ts` previously skipped the Model Explorer pane for zero catalog entries, leaving an entirely blank screen.

`art/nine-dragon-stack/round-17-model-empty/` has the real 390×844 empty capture and two code-native portrait mockups. Option A, a full catalog state with a World Explorer route, is implemented in `Explore.ts`/`explore.css`; option B is a compact alternative. A local portrait browser check confirmed the message and button, and the button opens World Explorer. Curated specimen registration remains open; the UI truthfully reports zero registered models until that work lands. Hunyuan3D-2 is present in the local weight store but excluded from this game by the project's licence policy; Qwen Image is for mockups, not shipped 3D.

Commit `7eb0f72c`; clean-export gate and GitHub deploy passed; production build `7eb0f72-muier0q6`.
