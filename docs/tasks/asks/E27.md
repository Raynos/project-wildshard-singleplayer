# E27

**Status:** in flight (2026-09-22, prepare-to-exit session)
**Ask:** Copy the prepare-to-exit skill from ~ game demos trials gauntlet, make it specific to this repo

Wildshard already had the skill (ported from trials-gauntlet in `34efff4`, re-cut for CD in `e550ab4`); trials-gauntlet's
current copy has nothing wildshard lacks (its CD step was ported *from* here). So E27 = re-cut the wildshard copy against
today's AGENTS.md: pathspec commits as the default (private index only for shared-file hunks), gates on a clean export of
HEAD before the push, `scripts/push-main.sh`, ask files + plan State lines / archive, the 3-browser lane + emulator kill,
the real checkout / memory paths (`~/projects/games/wildshard-singleplayer`), SKIP_* escapes. Banners byte-identical.
Also fixed `scripts/ask-new.sh`: the apostrophe in its `${1:?…user's words…}` usage string made bash refuse to parse the
script at all, so no ask id could be claimed.
