# E172

**Status:** done (2026-09-25) — one Debug registry (E162's) in both the title's Settings and the pause menu, plus Loading & memory ▸ Clear downloads (two taps; deletes every cache + the SW + the KTX2 markers, keeps saves / settings / review login, reloads cold). `d666cbbd`, `e074ce98`. Proof: after Clear downloads the server re-sent 21.1 MB (= a first visit), saves intact. progress/276. Not yet tried on the iPhone
**Ask:** a Dev / Debug section in the MAIN MENU settings (the title screen has none, only the pause menu) with a button to nuke the cache, so I can test cold loads myself

