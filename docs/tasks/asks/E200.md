# E200

**Status:** in flight (2026-09-26, Codex)
**Ask:** My Dragon Shot appears to have a double loading bar bug. And when I try to load it, it, you know, obviously pulls up the loading bar. But then once it completes, it pulls up the loading bar again. It has to load twice, like there's some kind of bug or glitch here.

Earlier theory, now ruled out as a full explanation: the portrait PWA starts at `/`, builds Driftwood, and builds Nine Dragon on selection. Jake clarified that he sees the **Nine Dragon** loader complete, then a page restart and a **second Nine Dragon** loader. This is consistent with an iOS WebContent restart or GPU recovery, but neither cause is confirmed. The screenshot's Download and Setup bars are two measures within one build, not the reported second load.

Change so far: persist the last successfully selected shard and use it for the PWA root launch when no explicit harness `chunk` is supplied. The GPU texture-mode resolver uses the same saved slug. This helps a recovery reload land on Nine Dragon but does not establish or fix the restart's cause.

Commit `8062b4ef`; clean-export gate and GitHub deploy passed; production build `8062b4e-muieoa4u`. On 2026-09-26, the installed portrait iOS 26.5 Simulator PWA loaded Nine Dragon once (27 MB download, setup completed), returned to the title with LOADED, and entered gameplay on the next tap without another loading bar. The Simulator has 8 virtual cores and did **not** reproduce Jake's 4-core phone failure. Keep this ask open: obtain the phone's existing Debug ▸ Loading & memory ▸ Last reload evidence if it recurs, or reproduce under a lower-memory device/profile, then fix the demonstrated cause. A first-ever launch also still builds Driftwood before selecting another shard; a lightweight title shell is separate follow-up work.

Code audit after that run: `ShardHost.switchTo()` parks Driftwood and builds Nine Dragon with a two-resident-shard cap; a parked renderer keeps its scene textures. Switching shards itself does not call `location.reload()`. Only explicit update adoption and boot-setting application do so in the inspected boot/UI paths. Holding Driftwood's textures during the Nine Dragon build is a plausible peak-memory pressure point on the physical iPhone, but the Simulator result and code alone do not prove it caused Jake's restart. The existing `lastEndLine()` debug record can distinguish an intentional navigation from an unexpected page death on the phone.
