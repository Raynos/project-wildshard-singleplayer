# E21

**Status:** in flight (2026-09-22, the E13 / E21 session)
**Ask:** "What **rules can we set up in AGENTS.md for 10 agents committing to the same main branch**? Prior art in game-demos trials gauntlet, the final-fantasy gauntlet demo, and kami-kakushi" → the user: "copy kami kakushi hook; add push-main.sh and a hook that blocks bare git push; one file per ask sure; update AGENTS.md; turn the hooks on lol" + "1, 2, 3, 7, 8"

Picks, from the ranked list relayed in chat (prior art: kami-kakushi `.claude/hooks/guard-git-add-all.sh`,
`guard-bash-safety.sh`, `src/scripts/push-main.sh`, `.githooks/post-commit`; FF15 gauntlet `CLAUDE.md` shared trunk):

1. sweep guard: `.claude/hooks/guard-git-add-all.sh` (PreToolUse Bash) + `.githooks/post-commit` ledger
2. push lock: `scripts/push-main.sh` (lockf on `.git/push.lock`, a held lock leaves commits local, the pusher loops
   until origin/main caught up) + the bare-`git push` block in `.claude/hooks/guard-bash-safety.sh`
3. tree-wide destructive git blocked (restore / checkout `.`, bare stash, reset --hard, clean -f), no escape
4. one file per ask: `docs/tasks/asks/<ID>.md`, ids claimed by `scripts/ask-new.sh`; the legacy table is history
7. push cost: `.gitattributes` (binaries `-delta`), `.githooks/pre-commit` refuses progress/ images > 500 KB
8. hooks on: `git config core.hooksPath .githooks`; the session brief warns when it is off

Not picked: foreign-hunk check (5), private-index rule (6).

_Moved from the legacy table in docs/tasks/ASKS.md on 2026-09-22._
