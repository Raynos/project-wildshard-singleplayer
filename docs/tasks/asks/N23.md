# N23

**Status:** done (2026-09-25) — the user after the board: "Edge ok" / "Edge on" → the Edge setting defaults to ON (the berm + spruce + granite along the slab's low edges; the edge terrain + navmesh bakes load by default); the full cleanup (one bake, no switch) is NALATI-FINISH B7
**Ask:** Skybox thing still wraps badly lol (the painted hills fade into haze, then the 3D field ends in a hard straight line in front of them — iPhone screenshot from the camp at golden hour, after the N19 blend)

**What ON is:** a grassy berm along the slab's low edges (7–21 m; a 3–9 m bank by the camp + pasture so the painted range still shows over it), a convex face with a > 40° band, spruce lines + granite on the crest, a notch the road's width at each entry road (y = 0 kept), the Kunes leaving through a narrowing gorge; the crags keep their own skyline. A second terrain + navmesh bake (terrain.edge.bin / navmesh.edge.bin); OFF is byte-identical to before. src/chunks/nalatiEdge.ts.
**Checks (edge on):** physics-baseline walk 12 legs + trails 16 legs, 0 stuck (progress/nalati-merge/n23/*.json); navmesh `--check` up to date; a 1 m flood fill of the ≤ 40° ground finds no climb up the face onto the crest.
**Still shows / follow-ups:** at the W / E / S gates the road's notch still frames the gate panel + the cloud deck (the road must stay straight at y = 0); from high ground (the bowl's west rim, Eagle Rock) you look over the berm onto the cloud deck; the crest can be reached by walking along it from the shoulders / crags (as the edge can be today), where the CHUNK BOUNDARY wall and the cloud deck are what you see.
