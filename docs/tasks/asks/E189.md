# E189

**Status:** in flight (2026-09-25, wildshard-singleplayer-d0) — cause found (thermal throttle), Jake picked a locked 30; first cut shipped, ground cover + shadow cuts next (see Next). Lanes agreed with the E186 owner (wildshard-singleplayer-df): E186 keeps GroundCover.ts, BlenderIsland.ts, stutter-run.mjs and the JS garbage trace. E189 takes E165 (the rope hitch and the altitude fps), E174 (phone shadow-map variants) and the steady-frame GPU budget pass (shadows, Sky.ts, shadowFade/Chunks/Filter, cascadeCull, Game warm-up).
**Ask:** You are the FPS perf mega agent for driftwood


## Findings (2026-09-25)

- **Jake's 60 → 20 fps drop is the iPhone 17 Pro throttling its GPU under sustained load**, not a leak: standing still on the
  pier the same frame (111 calls, 395 k tris) went 17 → 19 → 21 → 24 ms; a 3-min Mac soak (scratchpad soak-still.mjs) held
  the frozen frame at 1.0–1.2 ms GPU with every GL object, program and the heap flat. Research: the 17 Pro keeps 64–73 %
  in 3DMark's 20-loop stress test; Apple (WWDC18 612): lock 30 if 60 can't hold 10 min; Safari gives a page no thermal signal.
- **Jake's pick: a locked 30 fps on the hot phone** (the phone default cap stays on; cut GPU until it holds in the grass).
- **Where the GPU goes** (`node scripts/pine-hollow-gpu.mjs --chunk=driftwood-isle`, progress/driftwood-gpu-e189-base.json;
  interior grass pose, 804×1748): ground cover 42 % (639 k tris, 31 k instances), shadows ~30 %, island terrain ~7 %, the
  sword's mid-pass depth copy ~6 %, the whole post chain < 10 %.
- **Shipped (no pixel change):** E142's depth slices + god rays skipped off screen on Driftwood's phone tier too
  (`phonePictureCuts`, tier.ts / Game.ts): M5 interior 1.72 → 1.54 ms, flank 1.40 → 1.24 ms; more on the phone's bandwidth.

## Next (open)

1. Ground cover GPU cuts as Debug variants (GroundCover.ts, after the E186 agent's upload rewrite lands — it hands the file
   over). Keep E156's rules: no grow / shrink / sink, far = near's own leaves (silhouette-matched), no part-grown inserts.
   Cheap levers: phone near cap + density, far keep ratio, far pebbles / shells, far kite tris (fern 18, hibiscus 21, bush 14).
2. Shadows: the far cascade redrawn every other frame; a cheaper fade; the shadow pieces' receiveShadow flipping three's
   depth program 23–30× a frame (≈ 8 MB/s garbage, E186.md "For d0") — set it to match their neighbours.
3. perfProbe.ts rows for Driftwood ("no ground cover", "no viewmodel") so one RUN PROBE on the hot phone ranks the levers.
4. Proof: a hot-phone reading after 10 min in the grass at the 30 cap.

## 2026-09-26

- **E193 is live** (`52f4967-muhy9dzk`): every shard at a locked 30 on mobile. Jake's ruling on the cuts (via the E186
  session): "Hold 30 fps, keep the look": invisible cuts only, shipped as the default until the warm phone holds 30 in
  the grass (his 00:27 reading, warm, grass: 38 ms frame, gpu~ 32 ms: ~15–20 % to cut).
- **Probe rows live** (`fe21667-muhyr6wr`): RUN PROBE on Driftwood adds no cover / no near cover / no far cover / no terrain
  / no viewmodel. Waiting on one warm-phone run (Jake) to rank the costs; the Mac (M5, shared with other agents' GPU work)
  reads the same frozen frame 1.67 then 2.12 ms and cannot split cascades or cover kinds.
- Mac ratios that held across runs: shadow map redraw ~35–40 % of the grass frame, the post chain ~18 %, a sun-fade frame
  +~8 %; ground cover per kind 0.01–0.12 ms each (no single offender).
- **Not fixed, measured:** the shadow pass re-derives three's shared depth program 21–22× a frame (V8 garbage). It is the
  instanced / non-instanced casters (enemies, rope bridge, instanced props) alternating on the shared MeshDepthMaterial,
  not the shadow pieces: giving the pieces their own depth material took it 22 → 21 only (reverted). The fix would be a
  depth material per (instanced, instanceColor, receiveShadow) combination for every caster. CPU / garbage, not the GPU
  bottleneck: parked.

## 2026-09-26 (later) — the probe says it is the post chain; debug cuts

- Jake's warm-phone probes (00:50, 01:04) with the new rows: "no post" 48 → 17 ms, any one scene system 3–8 ms; but the
  second run swung 17 ↔ 45 ms row to row on the same frame (and "no post" read 40 there): the warm phone flips between a fast
  and a slow state on its own. So the probe now takes an uncapped baseline before every row and reports the row against it.
- **Probe upgrade** (Jake: "you need a copy button … lots and lots of data"), live `0d46808-mui0e6y6`: COPY beside RUN
  PROBE (the whole report, kept across a reload): a header (build, device, settings, where, draws), every row with p50 /
  p95 / min / max / js / wait / its baseline / its saving, and the run second by second.
- **Debug cuts** (Jake: "cut blindly, but show me before and after photos & videos … DEBUG CUTS, not commit and ship"),
  Debug ▸ Performance, Driftwood only, both default to today: Anti-aliasing SMAA | FXAA (one FXAA pass on the graded frame
  instead of SMAA's three), God rays On | Off. Boards progress/282 (grass) + 283 (cove), videos sent (scratchpad
  e189-cuts-*.mp4). Found and fixed on the way: FXAA inside the colour pass threw the grade away (a dark disc in the sky,
  dark patches on the water): it has its own pass now. Visible difference: with the god rays off the faint warm glow round a
  palm crown in front of the sun goes.
- Far cascade every other frame: live (`51c8785`), Jake's "far shadows softer" pick.
