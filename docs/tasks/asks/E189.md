# E189

**Status:** in flight (2026-09-25, wildshard-singleplayer-d0) — cause found (thermal throttle), Jake picked a locked 30; first cut shipped, ground cover + shadow cuts next (see Next). Lanes agreed with the E186 owner (wildshard-singleplayer-df): E186 keeps GroundCover.ts, BlenderIsland.ts, stutter-run.mjs and the JS garbage trace. E189 takes E165 (the rope hitch and the altitude fps), E174 (phone shadow-map variants) and the steady-frame GPU budget pass (shadows, Sky.ts, shadowFade/Chunks/Filter, cascadeCull, Game warm-up).
**Ask:** You are the FPS perf mega agent for driftwood


## Findings (2026-09-25)

- **Jake's 60 → 20 fps drop is the iPhone 17 Pro throttling its GPU under sustained load**, not a leak: standing still on the
  pier the same frame (111 calls, 395 k tris) went 17 → 19 → 21 → 24 ms; a 3-min Mac soak (scratchpad soak-still.mjs) held
  the frozen frame at 1.0–1.2 ms GPU with every GL object, program and the heap flat. Research: the 17 Pro keeps 64–73 %
  in 3DMark's 20-loop stress test; Apple (WWDC18 612): lock 30 if 60 can't hold 10 min; Safari gives a page no thermal signal.
- **Jake's pick: a locked 30 fps on the hot phone** (the phone default cap stays on; cut GPU until it holds in the grass).
- **Where the GPU goes** (`node scripts/pine-hollow-gpu.mjs --chunk=driftwood-isle`, progress/driftwood-gpu-e189-base.json;
  interior grass pose, 804×1748): ground cover 42 % (639 k tris, 31 k instances), shadows ~30 %, island terrain ~7 %, the
  sword's mid-pass depth copy ~6 %, the whole post chain < 10 %.
- **Shipped (no pixel change):** E142's depth slices + god rays skipped off screen on Driftwood's phone tier too
  (`phonePictureCuts`, tier.ts / Game.ts): M5 interior 1.72 → 1.54 ms, flank 1.40 → 1.24 ms; more on the phone's bandwidth.

## Next (open)

1. Ground cover GPU cuts as Debug variants (GroundCover.ts, after the E186 agent's upload rewrite lands — it hands the file
   over). Keep E156's rules: no grow / shrink / sink, far = near's own leaves (silhouette-matched), no part-grown inserts.
   Cheap levers: phone near cap + density, far keep ratio, far pebbles / shells, far kite tris (fern 18, hibiscus 21, bush 14).
2. Shadows: the far cascade redrawn every other frame; a cheaper fade; the shadow pieces' receiveShadow flipping three's
   depth program 23–30× a frame (≈ 8 MB/s garbage, E186.md "For d0") — set it to match their neighbours.
3. perfProbe.ts rows for Driftwood ("no ground cover", "no viewmodel") so one RUN PROBE on the hot phone ranks the levers.
4. Proof: a hot-phone reading after 10 min in the grass at the 30 cap.
