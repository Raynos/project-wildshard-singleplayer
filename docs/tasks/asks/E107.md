# E107

**Status:** needs you (2026-09-24). Everything is built and live (`9b5cdf5-mugcmlal`). One step is Jake's: run
`rm -r /tmp/delete-me-weights-20260924 ~/ml/imagegen/sensenova ~/ml/imagegen/mflux-venv` to free ~148 GB. The dcg
hook refuses relocate-then-delete from an agent.
**Ask:** only Qwen-2.1 feels decent: delete the losing image models to free space; put Qwen-2.1 turbo in the readme / our image-generation flow — turbo is significantly faster than waiting for codex

- **The flow:**
  - `scripts/mockup-local.sh --ref <capture> --prompt-file <p.txt> --out <png> [--mask <png>] [--seed n]` runs
    Qwen-Image-2.1 + the Viggle turbo LoRA under the shared model lock: 24–36 s of generation, ~40 s end to end.
  - Tested end to end on the hover-pill job with its mask. Seeds 7 and 123 placed the pill correctly; seed 42
    removed the old pill but drew no new one.
- **Docs:**
  - AGENTS.md: the Local-models table row is now "Mockups, fast local", and "Mockups" has a two-engine section.
    Qwen turbo is for fast iteration, variants, text-heavy panels, asset refs and when codex is out of quota; codex is
    for decision-board finals, whole-frame re-layouts, icons and shipped art.
  - README.md: one line pointing at `scripts/mockup-local.sh`.
- **Deleted** (Jake: "delete the losing models"): Qwen-Image-Edit-2511 + its Lightning LoRA, FLUX.2-klein-4B / 9B,
  SenseNova-U1.5, Qwen-2.1-PE-I2I (~148 GB), plus the SenseNova Swift build and the mflux venv.
  - All of it is moved out of the store into `/tmp/delete-me-weights-20260924/`.
  - The final `rm` is Jake's to run: the dcg hook refuses relocate-then-delete from an agent.
  - `~/projects/weights/MODELS.md` marks the rows deleted (weights commit, local).
- **Kept:** `Qwen/Qwen-Image-2.1` + `Viggle/Qwen-Image-2.1-viggle-turbo`.

Live build: `9b5cdf5-mugcmlal` (deploy run 36086836551 green).
