# E114

**Status:** needs pick (2026-09-24): board `art/rocks/round-1-directions/board.jpg` — NOW / A chiselled / B smooth painted / C layered slabs / D ink outline (mockup only)
**Ask:** The low poly rocks of the shipwreck look like shit. Most low poly rocks look like shit. We need to try something else here. Don't know exactly what, but what we have now looks bad.


**2026-09-24, the user (Boulder turntable):** "Boulders have all kinds of glitches here. The edges are not aligned. So you can see through the boulder, the faces of the boulders are not touching. There are all kinds of gaps you can see through. Also, I don't like this boulder. These low poly stones don't look good. Shitty lack of detail. Way too basic."
Cause of the gaps: Boulders.ts jitters IcosahedronGeometry vertices believing it is indexed; it is not (every face owns its corners), so the copies drift apart. Fix: mergeVertices before the jitter (sent to the rocks subagent).

**Follow-up (the user, Explore → Models → Boulder):** "Boulders have all kinds of glitches here. The edges are not aligned.
So you can see through the boulder that the faces of the boulders are not touching … Also, I don't like this boulder …
Way too basic."

**Log**
- Crack fix (default look): `src/world/Boulders.ts` jittered `IcosahedronGeometry`'s vertices "on the indexed sphere",
  but PolyhedronGeometry is non-indexed — each face's copy of a corner took its own jitter and the faces split apart
  (see-through cracks). Now `mergeVertices` welds it before the jitter. Same bug fixed in `src/world/Props.ts` bushes
  (Pine Hollow, off by default). Checked clean: `lowpolyKit.rock()` (welds via a position map — the wreck, the cove,
  the shrine, ground cover), `Seabed.ts` coral (per-position hash). Shipped e953a5a, live build `e953a5a-mugezqft`;
  before/after `art/rocks/round-1-directions/boulder-crack-fix-before-after.jpg`.
- Directions (taste — the user picks; the current look stays the default): `src/world/rockKit.ts` builds three looks
  behind `?rocks=a|b|c` — A chiselled planes, B smooth painted (smooth normals, +1 draw), C layered slabs — wired into
  the wreck's reef rocks, the shore boulders + Explore's Boulder, the cove's loose rocks and the ground-cover pebbles.
  D (ink outline) is a mockup only. Board, mockups, captures and tri counts: `art/rocks/round-1-directions/` (README).
  Try: `https://wildshard-singleplayer.vercel.app/?explore=model&model=wreck&rocks=a` (or `b`, `c`). Shipped 458f5e1
  (+ art index 9efea91), live build `9efea91-mughi98b`.
