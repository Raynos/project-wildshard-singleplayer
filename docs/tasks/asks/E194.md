# E194

**Status:** done (2026-09-26) — no game regression: since da2566d4 (Shards in memory = 1) the scorecard's step 2 was a REBUILD, timed against the baseline's resident return. The switch route now saves Shards in memory = 2 and warns when a step is not the kind it expects (`03883c38`). On main with 2 resident: the resident return 0.06 s to playable, 0.14–0.16 s to first frame (baseline 0.05 / 0.12–0.13; +0.04 on the phone = E193's 30 fps lock). E179's release costs ~0.02–0.04 s on desktop and saves ~80 MB (phone) / ~178 MB (desktop) — kept. Driftwood phone cold 4G on main alone +0.65 s (noise band ±2.58 s): the Nine Dragon Stack teaser card's art at the bar (+0.41 MB phone)
**Ask:** regression on main (scorecard, found by E173's side-by-side run): the resident return Nalati → Driftwood went 0.05 s → 1.7 s (phone) / 1.94 s (desktop), and Driftwood phone cold 4G slowed — bisect since the 45fa37b baseline (suspects: E179's parked render-target free / re-alloc, E174 shadow variant C, E186 / E189)

