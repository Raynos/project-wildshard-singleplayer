# E196

**Status:** open (2026-09-26) — run `node scripts/scorecard.mjs --export=HEAD --tag=baseline --runs=2 --goldens --retouch` on a quiet machine (~1 h) and commit progress/scorecard/baseline*. Evidence: progress/scorecard/e173-noise.md
**Ask:** re-record the scorecard baseline now that desktop draws KTX2 (E173): the desktop look goldens are image-based, so the desktop pose SSIM rows flag the intended KTX2 look

