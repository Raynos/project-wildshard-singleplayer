# E41

**Status:** done (2026-09-23) — `b63aa0e`, live in `236099e-mudwxink` (CI run 35844204518); the gate ran on the push itself (6 s, passed)
**Ask:** use herdr to tell the other agents on main that deploys are failing; figure out why; make the pre-push hook fail instead of the upstream deploy

Why deploys failed:
1. The Vercel free-plan quota (100 deployments/day, `api-deployments-free-per-day`) ran out on 2026-09-22, and every push was refused for hours. It reset overnight.
2. f94c5da and e507b24 went red *on Vercel* while every CI gate was green. `.vercelignore`'s unanchored `art` matched `src/explore/art/` at any depth, so Vercel's remote build never saw the hub art (hot-fixed by a rename in 0cc05de).

Done:
- herdr broadcast to the three other wildshard-singleplayer agents on main (w2:p3, w7:p1, w8:p1), 2026-09-23.
- `.vercelignore`: every line anchored with `/`.
- `scripts/vercel-tree-gate.sh` + `.githooks/pre-push`: builds the pushed tip from exactly the files Vercel uploads, and refuses the push on red. Proven on f94c5da (FAILED: `src/explore/art/*.webp` ignored) and HEAD (passed, ~6 s).
- AGENTS.md: the rule is written down.
