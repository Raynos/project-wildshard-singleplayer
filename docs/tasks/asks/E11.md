# E11

**Status:** done (2026-09-22) — `6eafcb4`, live in `6eafcb4-mudcvh0k` (CI run 35801491661); mockups `58e4bdb` (art/hud/round-7-sword-touch/)
**Ask:** (HUD agent) "Use a subagent to **research the AIM and movement mechanisms of Call of Duty Mobile**; see how that might apply to our **driftwood sword gameplay**" → "How do we improve the twin stick touch controls … ask me any questions" → "show me the HUD / UI codex mockups (parallel)"

research relayed in chat. The user's picks: **tap LOOK pad = swing + a HEAVY disc** in the old AIM spot (tap charge / tap release); **CODM lunge** (≤ 4 m, ±25° cone → ~1.4 m in ~0.13 s, heavy 5 m, walkable ground only) on every input, camera turn touch only behind aim assist; **look = whole right half** of the screen (buttons first); **DODGE disc** right of the thumb beside JUMP, always shown (not swimming), direction from the MOVE stick, neutral = backstep, desktop **Left Alt** + WASD; **Look speed + Swing turn speed sliders** scaling touch and mouse. Not picked: sprint lock, floating stick. Mockups: art/hud/round-7-sword-touch/{A-dodge,B-lunge,C-heavy,D-settings}.jpg (4 parallel codex runs)

_Moved from the legacy table in docs/tasks/ASKS.md on 2026-09-22._

Built: touch HEAVY disc (the AIM spot, same latch, charge ring) + DODGE disc (Left Alt on desktop; 3 m / 0.25 s toward the stick, backstep when centred; never off the pier), CoD-style lunge on every input (4 m / 5 m heavy, ±25°, stops 1.1 m short) with lock-on brackets (src/ui/LockOn.ts) and a touch camera ease, look over the whole right half (tap-fire only in the LOOK pad), Settings → Controls Look speed + Swing turn speed. Verified 390×844: dodge 3.01 m, lunge 4.2 → 1.73 m to a boar. Follow-ups: E34 (see-through bar), E35 (dodge / lunge feel + docs/plans/HUD-REFINEMENTS.md).
