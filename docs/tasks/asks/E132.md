# E132

**Status:** done (2026-09-25): the user's pick A is built. Commit `81a8c05`, live build `81a8c05-mugw0flt`. Sheet: `progress/245-e132-complete-screen.jpg`
- Built as designed below. The card is the shard-agnostic `src/ui/ShardComplete.ts` (+ `styles/complete.css`),
  fed by Driftwood's `src/game/quest/Complete.ts`. The finale hands over at the end of the hold (`Finale.ts`).
  main.ts's `frameGate` freezes the world while the card is up, and world audio is muted. `Progress.playS` (in
  `ws.progress.v1`, optional so old saves load) counts time in the world only.
- Verified on the iPhone 16 Pro profile (a local build of the commit):
  - the card at the hold's end, with the quest toasts under it; the frame counter stops while it is up;
  - KEEP EXPLORING brings back the HUD, and the chip reads "STILL TO FIND | SEA GLASS 35 M";
  - the BAG ▸ ACHIEVEMENTS row reopens the card;
  - a finished save without `seen:complete` gets "Driftwood Isle complete · open the menu to see your island".
- Not built: a "creatures defeated" total. There is no clean counter for it (only the capped crab and monkey
  achievement counts), so it is left out.

### Phase 1 board (2026-09-25): art/quest/round-2-complete-screen/board.jpg
- **A, full card:** a centred glass card over the dimmed reward view: title, a 3×2 stats grid (time, achievements, shards, sea glass, places, worn title), "still to find" chips, then KEEP EXPLORING and the two secondary buttons.
- **B, bottom sheet:** the planet-in-the-ring view and the title stay visible in the top half; a sheet from the bottom carries a one-row stats strip, a 2-column "still to find" list and the buttons in the thumb zone.
- **C, credits roll:** letterbox bars; the credits (a game by, Music: MiniMax-Music3, the SFX / Stability AI line, engine) roll over the view; one stats line and the buttons in the lower bar.
- Frames: `A.jpg` `B.jpg` `C.jpg`, and `today-hold.jpg` (the reward caption as it is now). Captured from the live build on an iPhone 16 Pro profile (402×874 @3×), UI injected as HTML/CSS. The stats are example values; the totals are the game's real ones.

## Phase 2 trigger design (built in 81a8c05)

- **When:** at the end of the reward view's 7 s hold (Finale.ts `reward > HOLD_S`). The game still owns the camera
  there (`player.carried`), the captain is dead and nothing else is aggro'd near the shrine, so the card never lands
  mid-fight. The finale keeps `carried` on and hands over to the card instead of releasing control. `seen:reward` is
  set, so the quest completes and the achievement toast plays under the card.
- **Pause:** while the card is up, the world freezes and the audio ducks like the pause menu (a `frameGate` term /
  the menu's hush). The music stays on the `chunk` sting / theme, and touch controls and the HUD are hidden.
- **Keep exploring:** the card closes, `carried` is released, the viewmodel and HUD come back, the clock runs on
  from golden hour. The quest chip then reads "Still to find · …" (the nearest unfound sea glass / place / treasure,
  with a distance and arrow), so the endless free play has a direction.
- **Skipped or never reached:** if `quest:driftwood-done` is set but `seen:complete` is not (for example an old
  save that finished before this shipped, or a reload during the hold), a toast offers "Driftwood complete · open
  the menu to see your island". It only fires out of combat, when no enemy is aggro'd within ~25 m.
- **Reopen:** a "Driftwood complete" row at the top of the menu's ACHIEVEMENTS tab reopens the same card at any time.
- **Save:** `seen:complete` is set in the shard's Flags (`ws.flags.v1`, per shard) the first time the card shows, so
  it auto-shows once only. Time played needs a new per-shard counter (Progress `ws.progress.v1` `playS`, ticked while
  `hud.entered`). It does not exist yet.
- **Next shard:** reload with `?chunk=nalati-grasslands`, as ENTER WORLD does for another shard. **Title screen:**
  `hud.exitToMenu()`, like the pause menu's "Exit to main menu".
**Ask:** 9 yes. But the Driftwood complete screen should have buttons to continue playing endlessly. Use a subagent for 9 and show me mockups of the complete screen. Also tell me in detail how this works. When can we pop up such a mega modal without it breaking gameplay mid gameplay? (E126 item 9: story ending — Driftwood complete screen + reasons to wander)


**2026-09-25, the user picked A** (full card over the dimmed view). Phase 2 (the build) goes to the same subagent.
