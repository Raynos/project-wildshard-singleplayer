# E179

**Status:** needs the user's iPhone (2026-09-26) — landed `807ee07f`: no code path navigates on ENTER WORLD / EXPLORE / a switch / a resident resume, even with a newer build waiting (headless repro, 0 navigations) → the reload was most likely iOS killing the page (memory). Now: Shards in memory = 1 by default; a parked shard frees its render targets + shadow maps (Nalati ~50, Pine ~64, Driftwood ~221 MB); a parked shard whose GL context died is rebuilt, not resumed; every intentional reload records why (ws.lastUnload) and Debug ▸ Loading & memory shows 'Last reload: …' (intentional / unexpectedly ended — likely iOS memory). Next iPhone reload: read that line
**Ask:** iPhone: pressing ENTER WORLD on a resident shard sometimes reloads the page (Pine resident + Nalati, switch → only Nalati left) — iOS memory kill or a code path? instrument + fix

