# E193

**Status:** done (2026-09-26) — every shard renders at a locked 30 on mobile: commit `6a7dc212`, live in `52f4967-muhy9dzk` (Jake's meter read "cap 30 fps" at 00:50)
**Ask:** Lock all shards to 30 fps on mobile.


## What changed

- `src/core/tier.ts` `frameCapFps()`: on a mobile device (a phone / tablet user agent, or the phone tier on any device)
  every shard is capped at 30; before, only Pine Hollow's phone tier was (PH-P1) and Driftwood / Nalati ran uncapped.
  No menu pick lifts it: the Debug ▸ Performance ▸ Frame cap row is desktop-only now. Only the perf probe's uncapped rows
  (`frameProbe`) and the test harness's `?fps=60` do. Desktop is unchanged (auto = the display's rate, 30 caps).
- Why (E189): the iPhone 17 Pro's GPU throttles ~2× within a minute or two, so an uncapped 60 sinks to an uneven 20–30.
- Checked (headless, a saved "Uncapped" pick): Driftwood / Nalati / Pine Hollow on an iPhone profile 30.0 fps drawn;
  desktop 60.0; desktop with `?tier=phone` 30.0; iPhone with the harness's `?fps=60` 60.0.
- Next (E189): cut the GPU cost so the hot phone holds that 30 in Driftwood's grass (ground cover, shadows).
