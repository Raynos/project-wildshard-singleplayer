# E167

**Status:** open (2026-09-25) — needs the user's iPhone (headless Chromium cannot show the iOS limit). Leftover of docs/plans/SHARD-CACHE.md (archived: project/archive/2026-09-25-shard-cache.md)
**Ask:** iPhone check for E155: with two shards resident (Debug ▸ Memory readout; Debug ▸ Developer on), does iOS keep the page alive? if it reloads / goes black, set Debug ▸ Shards in memory to 1

