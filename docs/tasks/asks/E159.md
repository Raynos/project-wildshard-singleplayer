# E159

**Status:** in flight (2026-09-25) — folded into the E155 build subagent's scope (docs/plans/SHARD-CACHE.md rows M4 cap → 2, M5 fast rebuild)

- 2026-09-25, the subagent: cap 2 on every tier; eviction frees the shard (context lost, no live Game / Scene / Physics left, heap drops); an evicted shard rebuilds in the page in 2.4–3.1 s to its title (Nalati; desktop / phone headless), no download, no audio re-decode. Waits on the E155 merge.
**Ask:** C: make a shard that was evicted from memory load fast (in-page rebuild, no download, reuse what the page already has); keep only TWO shards in memory instead of four

