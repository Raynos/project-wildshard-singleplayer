# E158

**Status:** done (2026-09-25) — landed `d6962f84`…`a9a60e58`, live as build `a9a60e5-muhadjvd`. Prod smoke (phone tier, Driftwood): the other two shards downloaded in the background in 23 s (Pine Hollow 26.3 MB / 42 files, Nalati 13.0 MB / 24 files, 0 failed), no page errors. Bench: a first switch Driftwood → Nalati / Pine Hollow went from 12.4 / 25.1 MB (phone) to ~4 KB. Off switch: pause ▸ Settings ▸ Debug ▸ Download in background (no URL switch). Runs on Wi-Fi and cellular (the user's pick); iOS Low Data Mode is not visible to the page
**Ask:** background prefetch: once a shard is playable, download the other shards' files into the SW cache so the first switch to a new shard skips the download phase and only loads

