# E31

**Status:** done (2026-09-22)
**Ask:** Change the review inbox password to the user's choice (the value itself stays out of git)

`REVIEW_PASSWORD` was replaced on Vercel for Production, Preview and Development, and in `.env.local`. HEAD (614f447) was
redeployed with `gh workflow run deploy` (run 35809099287) so `/api/inbox` reads the new value. Checked on production:
the new password gets 200, the old one gets 401, and `pnpm inbox:pull` works. A device still unlocked with the old
password is locked again on its next send and asks for the password in Settings → REVIEW.
