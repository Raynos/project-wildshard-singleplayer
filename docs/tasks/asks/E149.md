# E149

**Status:** done (2026-09-25): commit a4cb5598, live build a4cb559-mugytazr
**Ask:** T3 (Driftwood phone audit): the driftwood logs look like white styrofoam in the sun (Wreck.ts ~65) — fix it


**What landed:** one driftwood painter, `src/world/driftwood.ts` (`addDriftLog`), for every beached log on the island:
the wreck's four piles (`Wreck.ts`) and the dune-line logs all round the island (`GroundCover.ts`). The body tones go from
`#d2c6ae / #bfb197 / #e2d8c4` (near-white in the sun) to a warm silver-brown `#ab9d85 / #9d8e75 / #b4a894` (~20 %
darker). Each facet leans toward silver-grey (up-facing, sun-bleached) or brown (down-facing, weathered), ~1 facet in 8 is
a dark crack along the log, and the end caps are dark end grain. Still faceted and flat-shaded, same triangle count.
The Nalati logs (`nalati/dressing/statics.ts`) are another shard and were left alone.

**Evidence:** `progress/252-e149-driftwood-logs-before-after.jpg`: iPhone 390×844 @3×, UI hidden, the cave mouth and
the wreck beach at midday and golden hour, before (live `3f4b803-mugy7u8h`) and after. The Explore turntable
(`?explore=model&model=wreck`) was checked too: the piles read as wood there as well.
