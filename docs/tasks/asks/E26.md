# E26

**Status:** done (2026-09-22) — picks made, F1–F6 built, live in build 1e46950-mudeqkbx, verified end to end on production
**Ask:** "Ask me any questions or decisions to finalize this plan" — the feedback inbox (from E17), then "Go: build F1–F6 now"

The user's picks (the plan's Decisions table, D1–D8):
- Password, **no query string**: the feature is live for everyone but hidden behind Settings → REVIEW → password → UNLOCK.
- Once unlocked: a MENU **FEEDBACK** tab (composer only, **no history**) + a **Quick note (F8 / ✎)** switch.
- Desktop: F8 opens the quick bar; Tab expands it into the side sheet. Phone: ✎ NOTE disc under PAUSE **and** the tab.
- **Freehand pen** markup. Chips BUG / ART / FEEL / PERF / IDEA. Drain **on demand**.
- Password: generated, told to the user once in chat (it is in `.env.local` as `REVIEW_PASSWORD` and on Vercel).

Evidence:
- `a3f68da`: picks written into the plan.
- `2778db8`: `/api/inbox` + `pnpm inbox:pull` (F2, F5). The Blob store `wildshard-review-inbox` and `REVIEW_PASSWORD` are set on all three environments (F1).
- `1e46950`: the client (F3, F4). Headless checks: 1600×900 (quick bar, sheet, pen, offline queue, Settings REVIEW, FEEDBACK tab) and 390×844 touch (✎ disc with queued badge, bottom sheet).
- Production end to end: unlocked in Settings with the real password → F8 → "Note sent · ae74" → `pnpm inbox:pull` got the JSON + screenshot (test note moved to `.review/handled/`).
- F6: `.claude/skills/drain-inbox/SKILL.md`. The plan is archived at `project/archive/2026-09-22-feedback-inbox.md`. The leftover F7 (native) is ask **E30**.
