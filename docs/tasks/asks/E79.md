# E79

**Status:** in flight (2026-09-23, main session)
**Ask:** Sprint lock is not nice can you remove it permanently ?


Removed the R3 sprint lock (E71) outright: the latch, the SPRINT LOCK tag, the padlock glyph and its CSS. Pushing the
stick past the ring still sprints while held; letting go stops. HUD-REFINEMENTS R3 is marked removed for good.
