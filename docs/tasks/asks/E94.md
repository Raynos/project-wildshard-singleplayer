# E94

**Status:** open (2026-09-24)
**Ask:** Pine Hollow forest pops like Driftwood did: trees swap hi to lo cards at 55 m and lo trees cast no shadow on the phone (found during E90, not built)

Where: `src/world/Forest.ts` + `src/core/tier.ts` phone row — `treeHiDist: 55` (hi cards → lo cards), `loTreeShadows:
false` (lo trees cast none, while the cascade reaches 80 m), twigs pop at `treeTwigDist: 24`, far impostor at 130 m.
Same shape as the Driftwood fix in E90 (29c9e69): keep every tree that can shadow into view casting, push the visible
swap out of the shadow range / fade it. Measure the phone-tier cost first (Pine Hollow is the heavier shard).
