# E187

**Status:** done (2026-09-25)
**Ask:** Analyze how the Driftwood blow-by-blow PDF was written, make a skill/template/workflow for making more, and make new blow-by-blow PDFs for the Nalati Grasslands shard and the Pine Hollow remaster from their mega sessions; commit the PDFs

- How Driftwood's was made: hand-written HTML timeline in one `build.mjs` → Playwright Chromium `page.pdf()`, never
  committed (recovered from session bc119bdd's transcript).
- Skill: `~/.claude/skills/blow-by-blow/` (user-level, not in this repo) — `find_sessions.py` → `extract.py`
  (timeline + images + digest from one or more transcripts; finds files from deleted worktrees via path suffix and
  git history) → a model writes `story.mjs` → `build.mjs` (HTML → PDF + a ~3 MB `-share.pdf`).
- `docs/Nalati-Grasslands-blow-by-blow.pdf` — 34 pages, 102 images; sessions 4f59d3c7 + a0515af4.
- `docs/Pine-Hollow-Remaster-blow-by-blow.pdf` — 31 pages, 78 images; session d5e24aad.
- Both committed as the downsampled share copies (~3 MB); the full-resolution renders (14 / 25 MB) stay in the
  session scratchpad.
