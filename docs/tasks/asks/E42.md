# E42

**Status:** done (2026-09-23, HUD build agent)
**Ask:** build touch HUD layout E (art/hud/round-10-look-zone/E.jpg): separate LOOK pad and ATTACK zones; black HUD bar transparency 25% -> 33%; remaster the left movement stick (just an empty circle today) for better UX; keep the little bit of cyan colour


**Evidence:** commit `d57dc1a` (CI deploy run 35846799303 green); live build `e35905e-mudxvz01` (contains `d57dc1a`).
Proof shots `progress/187`–`192` (390×844: sword rest, hold-heavy charge, sprint stick, rifle FIRE + AIM + SWAP, swim
DIVE / SURFACE, live vs mockup E). Verified with real pointer input: ATTACK swings on down, a still hold charges then
releases the heavy, a drag from ATTACK turns the yaw, a LOOK-pad tap does not swing, dragging above the bar looks, a fast
triple tap stays slash → backhand → finisher. One look rate: 0.0095 rad/px on every surface (PAD_BOOST dropped).
Plan: `docs/plans/HUD-REFINEMENTS.md` ("E42: layout E"; R12 / R14 / R15 / R17 / R18 built, State `in progress`).
