# E136

**Status:** needs pick (2026-09-25). The deletions are done (the rocks commit waits for the Pine Hollow landing to push).
Jake picks on the two boards below: Nalati look v1 (painterly + old grass) and the procedural camp props.
**Ask:** (E134 follow-up) the dead code and the things I picked — delete all that. The old painterly look plus old grass: show before and after as an image, screenshots. WebGPU: leave it, I still need to figure that out. + Procedural nalati props vs 3d models, show me side by side

## Deleted (zero change to what players see)

| What | Commit | Lines | Evidence |
|---|---|---|---|
| Truly dead code: `src/boot/stream.ts` + 16 unreferenced exports (and the helpers they left unused) | fcd8857, b2a3e20 | −99 | repo-wide grep incl. scripts / dev-server `/src/` loads; gates green |
| E116 bushes: `?bush=` B canopy, C leaf cards, old lobes | 6999bcd | −379 / +27 | headless hash of 4 seeds × 170 bushes × phone + desktop, identical before / after |
| N23 edge (NALATI-FINISH B7): one bake, no `edge` setting / `.edge` files / `bakeVariant`; N19 `?horizonblend` | 8630da2 | ~−275 | terrain.bin / navmesh.bin byte-identical to the old `.edge` bake |
| E7 `?island`, E123 `?pshadow` rigs, E65 `?lighting` / `?sky=hdri` / `?post=cinematic` / `?matte` / inert `lut` setting | 78989f9 | ~−80 | Driftwood phone pack 959 → 549 KB (no unused HDRI); stale saved settings are dropped |
| E114 rocks: `?rocks=` A chiselled, C slabs, old icosahedron rocks | 5f117c10 | −407 / +95 | headless hash of rock geometry + Boulders / Wreck / Cove / GroundCover placements, identical |

Live: 96aa76e-mugvnre5 (dead code, bushes), 78989f9-mugvx0vn (edge, flags). Rocks: pending push.

Kept on purpose: `?nolut` (scripts/fit-lut.py), WebGPU (Jake: "leave that there"), `?boat=v1` (E113 needs pick),
`?bagbtn=0` (E124), `?paintedrange`, `_template.ts`, `spruceMask.ts`, `interact/validate.ts`.

## Boards for Jake's pick

`art/nalati-grasslands/round-11-variant-boards/` (README has the URLs and poses):
- `look-v1-vs-v2.jpg`: A old painterly look + old grass (`?look=v1`) vs B current. Deleting A removes ~415 lines plus
  `GrassPainterly.ts` (556).
- `props-proc-vs-glb.jpg`: A procedural camp props vs B 3D models. Deleting A removes ~150 lines. The churn model
  (row 4 B) reads as two brown bears.
