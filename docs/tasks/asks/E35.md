# E35

**Status:** done (2026-09-22) — `84951d0`, live in `84951d0-mudg6jz6` (CI run 35807955301); leftovers parked in docs/plans/HUD-REFINEMENTS.md (`0205775`)
**Ask:** (HUD agent) "What's next — a quick review / audit: what else can we do to improve touch controls compared to Call of Duty Mobile" → the user picked **dodge + lunge feel**; "the remaining recommendations should go into **tentative HUD refinements in docs/plans** … say Jake has not approved any of them, they are all just ideas"

Built (dodge + lunge feel): a dodge dips the eye 7 cm, leans into a sideways dodge and kicks the FOV 5°; a lunge kicks it
7° (`Player.fovKick`, the sword adds it); `audio.dodge()` / `audio.lunge()`; radial edge speed streaks
(`src/ui/SpeedLines.ts`); haptics on hit / kill / dodge / lunge via `navigator.vibrate` (Android only — iOS Safari has
none; Settings → Gameplay → Vibration, shown only where it works). Verified 390×844 `?touch=1` on Driftwood.

Parked, **not approved** (queue: docs/plans/HUD-REFINEMENTS.md, State `draft`): R1 swing on press, R2 hold LOOK pad =
heavy, R3 sprint lock, R4 floating stick, R5 layout editor, R6 look acceleration, R7 ADS sensitivity, R8 crouch / slide,
R9 gyro, R10 in-range pulse.

_Was row "E27" in the legacy docs/tasks/ASKS.md — that id is taken by another ask's file (docs/tasks/asks/E27.md), so this
ask was re-claimed as E35 at the E21 migration._
