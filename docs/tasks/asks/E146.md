# E146

**Status:** done (2026-09-25): the title deck's bottom padding is the iOS safe area PLUS a margin (phone portrait +20 px, desktop +26 px, landscape phone +14 px) instead of max(margin, safe area), so SETTINGS · SOUND sit clear of the home-swipe edge. src/ui/styles/menu.css
**Ask:** (title screen, iPhone home-screen app) The little buttons at the bottom are way too close to the edge of the screen — very hard to press, too close to the iOS drag edge to switch apps. Everything at the bottom of the screen needs ~20 px of padding / margin; everything has to move up. Nothing wrong with the design, just blank space at the bottom.

