# E14

**Status:** in flight (2026-09-22, session wildshard-singleplayer-8d)
**Ask:** "How do we **iterate quickly on models and graphics quality**? A **BUILD WORLD** entry next to ENTER WORLD → **god-mode freecam** (start from hxtnv/three-freecam) with a **Model Explorer** (build the models & assets) and a **Map Explorer** (build the map); **codex CLI (gpt-6-sol) mockups, parallel, lots**; mockups in a **named folder**, not loose in art/"

12 parallel codex runs → `art/build-world/` (title entry, model turntable / rig / catalog / A-B compare / phone, map freecam-place / top-down layers / scatter / sculpt / play-here, hub); three-freecam = npm `three-freecam` (MIT, 1.8 kB, `new FreeCam(camera, dom)`, `update(dt)`, `focus()`, `placeAt()`); next: user picks a direction → build

_Moved from the legacy table in docs/tasks/ASKS.md on 2026-09-22._
