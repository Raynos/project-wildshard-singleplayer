# N20

**Status:** done (2026-09-24) — the user's picks, locked in and the losers removed: terrain shadows OFF, terrain AO + bounce OFF (src/nalati/look/terrainLight.ts and its shader receivers deleted), model shading ON (always, no switch), yurts PROCEDURAL (the Blender yurt model + its loader branch deleted), camp people IMAGE-TO-3D ("Models 3D local ai model is best": the default, no switch; the Blender people files deleted; the procedural figures stay in the code for NALATI-FINISH B5's faces). No Look Lab switch is left; `?tshadow` `?tao` `?modelshade` `?yurts` `?people` are gone. NALATI-FINISH P1 ticked.
**Ask:** (leftover of NALATI-MERGE) Look Lab picks after playing: terrain shadows · terrain AO + bounce · model shading · Yurts (procedural / Blender) · Camp people (procedural / Blender / image-to-3D) — pause ▸ Settings ▸ Debug ▸ Look lab; the user: 'I need to still play it, keep them all in the look lab'

**Evidence:** commit 609f424, deploy run 36090024758, live build 609f424-mugebrf4. Boot check on the export (muted, Metal): Nalati, Driftwood, Pine Hollow boot with 0 page errors, 0 404s; the camp people are the image-to-3D SkinnedMesh (the procedural batch hidden once it loads).
