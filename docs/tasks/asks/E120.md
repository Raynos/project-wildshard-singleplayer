# E120

**Status:** done (2026-09-24)

**Measured** (a scripted jump + double jump at the apex, wading beside the starting pier, 4 spots along it; `peak − deck`): live build −0.36 / −0.36 / −0.38 / −0.44 m (can't get up). DOUBLE_JUMP 6.8 → 8.0: +0.03 / +0.03 / +0.01 / −0.04 (too tight). **6.8 → 8.6 (shipped): +0.25 / +0.25 / +0.24 / +0.18 m**. src/player/Player.ts.
**Not done:** lowering the pier. It needs scripts/bake-navmesh.mjs (another agent's uncommitted work was live in it) and a navmesh re-bake; the jump alone gets you up. On dry land the full double jump is now ~2.9 m (was ~2.2 m).
**Ask:** At the beginning level, the starting pier: make it so you can double jump on top. Increase the height of the double jump, and maybe make this pier a tiny bit lower, so you can double jump on top.

