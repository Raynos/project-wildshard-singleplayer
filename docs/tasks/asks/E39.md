# E39

**Status:** done (2026-09-23) — Jake: "e39 keep". Nothing to change; the rows stay (Render scale now lives in main menu ▸ Settings, E55).
**Ask:** Settings > Graphics rows (Render scale, Anti-aliasing): keep or drop? (left over from P4, the DBG pill removal)

P4 (`28286f2`) removed the DBG pill. The menu's Settings ▸ Graphics rows wrote to the pill's `ws.debug` key, so
the small-fixes agent kept them on their own key `ws.gfx.v1` ("Auto" = the settled defaults: phone DPR 1.5 + AA).
The user never asked for them to stay. Pick: **keep** (nothing to do → done) or **drop** (remove the two rows +
`ws.gfx.v1` from src/ui/Menu.ts / src/core/tier.ts; `?tier=` URL knobs stay).
