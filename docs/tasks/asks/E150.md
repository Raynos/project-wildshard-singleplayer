# E150

**Status:** done (2026-09-25) — commit `8de5b701`, live `f5138cc-mugz26ay` (carries it; checked on the live phone tier at the audit camera)
**Ask:** T5 (Driftwood phone audit): the waterfall is a soft blurred curtain that doesn't match the faceted style (Waterfall.ts) — fix it

**2026-09-25 (E150 agent):** `src/world/Waterfall.ts` rebuilt as a toon cascade. The W5 curtain stays behind `?waterfall=v1`
(`src/world/WaterfallV1.ts`).
- The sheet has three terraces, each a shelf and then a ballistic drop, and hugs the terrain. Every other column stands
  proud, so the sheet is pleated into flat facets, shaded in three toon bands against the sun.
- Down each drop the water changes colour in hard bands. There is a crisp white line at every brink, a scalloped foam
  band on every landing, and streak dashes scrolling down.
- At the foot: 12 boiling low-poly foam balls, 4 on each landing, and 8 spray chunks. Nine-sided dashed foam rings
  stay inside the plunge pool.
- Cost at the audit camera: 3 draws before and after. Triangles go from 208 (+26 points) to 830, and the scene
  totals from 1,326,083 to 1,326,705. GPU time for the waterfall alone is 0.035 vs 0.036 ms per frame at 780×1688
  (M5 Max), the same within noise.
- Evidence: `art/driftwood-isle/round-13-waterfall/`:
  - `board.jpg`: before / after, for the audit camera and a close camera, iPhone 390×844 @3×;
  - the four stills;
  - `waterfall-before-after.mp4`: 3 s, fixed camera, real-speed motion on a virtual clock.
