# E104

**Status:** needs pick (2026-09-24). Every model ran; 6 blind rounds were sent to Jake, and the keys are held until
he guesses. Scoreboard: `art/local-image/round-2-bakeoff/README.md`. Codex 9 · Qwen-2.1 turbo + mask 8 (localised
edits only) · Qwen-2.1 turbo 6 · SenseNova-U1.5 6 · Qwen-2.1 (40 steps) 6 · FLUX.2-klein-9B 5 · klein-4B 4 ·
Edit-2511 4 · PE rewriter 2. Open: Jake's guesses, and whether "Qwen-2.1 turbo + mask" becomes the local path for
localised mockup edits.

Bake-off on the same 10 jobs as E100 (`~/ml/imagegen/jobs.json`), blind A/B against codex:
- `Qwen/Qwen-Image-Edit-2511` (2025-12) + `lightx2v` Lightning 8-step, mflux
- `black-forest-labs/FLUX.2-klein-4B` (2026-01), mflux edit. The 9B is gated: Jake clicks "Agree" on
  https://huggingface.co/black-forest-labs/FLUX.2-klein-9B
- `mlx-community/SenseNova-U1.5-8B-MoT-8bit` (2026-08-19, the only Aug–Sep open editor found), MLX-Swift
  `xocialize/sensenova-u1-swift`

Jake (same day): also prep Qwen-2.1's turbo LoRA, region control and the official PE-I2I prompt rewriter. Text-to-image
one-shots are already covered by Qwen-2.1; the hunt is for edit models only. Qwen-2.1 variants to run on the same jobs:
turbo (6 steps), turbo + PE rewrite, and turbo + PE + mask. The mask variant covers only the localised jobs: the hover
pill, the quest chip and the feedback sheet. The mask rides along as the last condition image, and the output is
composited over the untouched frame outside it (`~/ml/imagegen/masks.py`, `box_masks.py`).

Download order (the link was at ~9 MB/s): turbo LoRA, PE-I2I, SenseNova and klein-4B first. Edit-2511 and klein-9B are
paused and resume afterwards. The `fetch-repo.sh` curl `--retry` truncation bug was fixed in weights `305ca16`.

Deep research (2026-09-24): Qwen-Image-2.1 (2026-09-20) is the #1 open image-edit model on LMArena (1367). Edit-2511
is at 1235 and FLUX.2-klein-9B at 1225. Newer candidates: SenseNova-U1.5 (Aug 19), JoyAI-Image-Edit-Plus (Jun 23),
HiDream-O1 (May). For 2.1 itself: the `Viggle/Qwen-Image-2.1-viggle-turbo` 6-step LoRA and the `PE-I2I` prompt
rewriter.
**Ask:** try Qwen-Image-Edit-2511 and FLUX.2 edit on the same mockups; deep-research subagent for image models released after Dec (ideally Aug/Sep 2026 only — we only want the newest models in our flows)

