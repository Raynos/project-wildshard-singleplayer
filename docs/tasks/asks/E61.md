# E61

**Status:** done (2026-09-23, main session) — build id below once live

**Done (uncommitted, iOS-lifecycle agent):**
- `src/ui/Resume.ts` + `src/ui/styles/resume.css` (new) + markup/inline script in `index.html`: the tiny resume screen —
  blurred still of the last frame, a small cyan shard mark, RESUMING, a hairline. On `?glreload` the inline script shows
  it from the first paint and hides the first-boot loader.
- `src/core/GpuRecovery.ts` rewritten on it: the screen goes up WHILE HIDDEN (visibilitychange / pagehide) with a still
  from `Game.snapshot(120)` (also kept in sessionStorage `wsResumeShot` for the reload); on show it drops two frames
  after the loop draws; a loss whose canvases are already wiped reloads at once (no wait for the restore event).
  The canvas-hiding trick (the black Jake saw) and the old "Restoring graphics" page (`gpu.css`) are gone.
- `src/core/Game.ts`: `snapshot(maxW)`. `src/main.ts`: `resuming` = `?glreload` → no title (`menuFirst` false), pause menu,
  boot plan's setup fraction drives the hairline (`resumeProgress`), `resumed` passed to installGpuRecovery.
- `scripts/check-css.mjs`: gpu.css → resume.css. `test/resume.test.ts`: index.html ↔ RESUME_HTML / flag / key.
- Measured (headless, Mac, warm cache): in-place return: screen gone ~220 ms after visible (incl. 160 ms fade).
  WebKit GPU kill, before (live 6e724bc…): first-boot loader at +1.7 s, title at +3.2 s, then a tap on ENTER WORLD.
  After: resume screen throughout, no loader, back in the world under the pause menu at +2.8 s, same spot.

**Left:** `scripts/e54-gpu-recovery.mjs` WebKit part — `myGpuProcesses()` (only kill our own GPU process) finds 0: the
WebKit GPU process is not a descendant of node (xpc). Target it another way (e.g. diff the WebKit.GPU pid set before /
after launching our browser), rerun the drill (Chrome + WebKit) and the iOS Simulator, then the four gates on a clean
export, commit only these hunks (main.ts / Game.ts carry other agents' WIP: private-index recipe), push, live build id.
**Ask:** app-switch return: a super lightweight tiny 'back to the action' loading screen (very different from the first-boot loading screen) — today switching back shows black / Restoring graphics and takes a while


Finished by the main session (2026-09-23): re-applied the src/main.ts hunk lost in the 16:05 truncation (World explorer
incident), fixed the drill's WebKit kill targeting (its GPU process hangs off launchd, not the script: the drill now kills
only GPU processes that appear while its own browser starts). Drill vs a local build: 10 / 10 PASS — Chromium in-place
restore (resume screen gone 236 ms after show), WebKit GPU kill → resume screen over the whole reload, no first-boot
loader, no title, same spot, back 4958 ms after the return (progress/209). tsc, oxlint, check-css, vitest 289, vite build
green on the exported tree.
