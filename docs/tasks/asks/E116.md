# E116

**Status:** needs pick (2026-09-24): the redo is three bushes built behind `?bush=a|b|c` (A leaf clump · B sculpted canopy · C leaf cards); board `art/bushes/round-1-directions/board.jpg`. The current bush stays the default until the user picks.
**Gaps (fixed):** three's IcosahedronGeometry is non-indexed (every face owns its corners); Bushes.ts jittered each copy on its own, so the lobes split into see-through cracks. mergeVertices before the jitter (src/world/Bushes.ts). The layout shifts a little (the rng draws fewer numbers).
**Ask:** Same thing with the hibiscus bush: the faces are not aligned, you can see through it in the gaps. And it's super low poly and just looks like dog shit. It also needs fixing. I don't know what the fix is, but it just needs to be done again.

**Redo (2026-09-24, bush subagent):** `src/world/bushKit.ts`, wired into `src/world/Bushes.ts` (the Explore catalog's
Hibiscus bush builds through it, so `&bush=` works on the turntable too). Phone tier, tris a bush / island (170 bushes,
built): now 93 / 15.9 k · A 352 / 59.8 k · B 427 / 72.6 k · C 229 / 38.9 k. Every look is still 1 draw call. A shares
lowPolyMaterial's program, while B and C each add one. Mockups first (local Qwen, `mockups.jpg`), then the builds. The
round's README has the table.

**Live:** commit 8ee071d, build `bb05911-mughgamt` (https://wildshard-singleplayer.vercel.app/?explore=model&model=bush&bush=a, `b`, `c`).
