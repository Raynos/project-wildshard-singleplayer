# E145

**Status:** done (2026-09-25): shown with vs without (art/shadows/round-1-orange-edge/: crops of the pier-deck post shadow + the beach sand shadow, and full phone frames); the user: "Remove it". src/world/stylize.ts `uToonEdge` defaults to 0 — no warm band round cast shadows; `?pedge=1` = the old rim. The facet terminator's warm band (where a face turns from the sun) is unchanged
**Ask:** Show me the shadow with and without the orange glow. The orange glow around the shadows is causing a lot of problems; maybe it should be permanently gone. It was a cute idea for style but it's not great. Show before and after.


**Live:** commit 8312b90, contained in live build `e9970c1-muh9w0ps` (checked 2026-09-25 at session exit).
