# E51

**Status:** done (2026-09-23) — commit 4a3b192, live build `4a3b192-muea9ump` (CI deploy run 35884876509 green)
**Ask:** build quest tracker G (art/quest/round-1-compact/G.jpg): one compact chip under the minimap 'GLYPH SHARDS 0/3 | SEA CAVE 230 M', minimap 80% size, heading readout under the minimap removed


**What landed (quest-HUD agent):**
- Minimap 80 %: 180 → 144 px desktop, 34vw → 27.2vw phone, same top-right position (`--ws-top` untouched); rim, ticks,
  "N", arrow, dots intact. The heading readout (`.ws-minimap-heading`) is gone: no DOM, no CSS, no per-frame text.
- The 4-row quest block is ONE chip under the minimap, right-aligned to its right edge (placed from the minimap's rect):
  ◆ `GLYPH SHARDS 1/3` | `SEA CAVE 284 M` ▲ (the arrow keeps its bearing). Short strings are quest data: `chip` on each
  step / the intro, `short` on each marker (`CHIP_MAX` 18, checked by `validateQuest`). Labels truncate with an
  ellipsis; the counter and the distance never do. Cyan pulse when the goal or its count changes. Same chip on desktop.
- Where the rest went: the chapter title ("THE SEALED RING"), the full objective and its sub-steps (the hint) are a
  quest card at the top of the menu's MAP tab (tap the minimap / M). The step / new-quest / complete toasts are unchanged.
- Boss bar width on the touch phone follows the smaller minimap (27.2vw).
- Screenshots: progress/195-e51-quest-chip-G-vs-live.jpg (G | live phone | map tab), 196 (phone), 197 (desktop 1600×900).
