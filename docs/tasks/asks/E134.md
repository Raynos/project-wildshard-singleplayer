# E134

**Status:** done (2026-09-25): answered at HEAD 683c765 (read-only); Jake said delete the picked losers + dead code, which is E136.
**Ask:** How much dead code and variants do we have in game where I've already choosen and we didn't delete the old ones ?

## Answer

About **690 lines** in `src/` (out of ~93k) still build the losing variants of looks Jake already picked. Another
~2,400 lines keep alternatives Jake never picked; an agent chose those defaults. Truly unreferenced code: ~80 lines.

### 1. Jake picked; the losers are still in src/ (~690 lines)

| Ask | Pick | Loser still reachable by | Lines |
|---|---|---|---|
| E116 | Bush A, leaf clump | `?bush=b\|c\|current`: `bushKit.ts:91-104,241-479` and `Bushes.ts:75-132` | ~310 |
| E114 | Rocks B, smooth painted | `?rocks=a\|c\|now`: `rockKit.ts:53-62,108-253,549-601` and branches in Boulders/Wreck/Cove/GroundCover. Removing the old path shifts later placements (Wreck and Cove burn RNG draws for it) | ~285 |
| N23 | Nalati slab edge on | `?edge=0` and the `edge` setting: a second terrain bake (`nalatiEdge.ts`) plus the debug menu row. Already planned as NALATI-FINISH B7 | ~40 |
| E7 | Blender cove | `?island=procedural` and the experimental settings row. The procedural cove itself stays (the Blender cove sits on it and falls back to it) | ~20 |
| E123 | Phone shadows `2c2k` | `?pshadow=old\|2k\|2c\|near` (`Sky.ts:32-41`) | ~10 |
| E65 | Toon lighting, stylized sky, clean post, LUT on, horizon matte on | `?lighting=standard`, `?sky=hdri`, `?post=cinematic`, `?nolut`, `?matte=0` plus settings plumbing. The HDRI sky and cinematic post stay for the other shards; only the Driftwood branches go | ~20 |
| N19 | Nalati horizon blend | `?horizonblend=0` | ~4 |

Already clean: E63 dodge T, E99 resume A, N20 Look Lab, E80 hover C, the SFX set.

### 2. An agent picked the default; no Jake pick found (~2,400 lines)

| Switch | Default | Alternative kept | Lines |
|---|---|---|---|
| `gpu` setting / `?gpu=` (E55) | WebGL | experimental WebGPU renderer, all of `src/gpu/` | ~1,535 |
| `?look=v1` (Nalati) | look v2 | the painterly v1 look: post chain, `Kuwahara.ts`, PaintGrade, `PaintedBackdrop.ts` | ~415 |
| `?grass=painterly` | v2 grass | `GrassPainterly.ts` (would go with look v1) | ~570 |
| `?paintedrange=1` | off | `PainterlyRange.ts` | ~175 |
| `?models=` (Nalati) | GLB props | procedural chest, woodpile, kazan, eagle, churn, saddle | ~150 |
| `?boat=v1` (E113, **still needs pick**) | v2 | `buildV1`. v1 caused the deck stripes (E123) | ~130 |
| `?bagbtn=0` (E124, **still needs pick**) | BAG button | one menu | ~3 |

E127 Q2 (GAME-NORMALIZATION N8, waiting on Jake) already proposes deleting most of the flag-only looks from both tables.

### 3. Truly dead code (knip, all entry points incl. dev pages, scripts, tests)

- ~80 lines: `src/boot/stream.ts` (28) plus 22 exports nothing references (~54: `buildBalbalCircleDressing`,
  `balbalRingSpots`, `OnePole`, `hVec2`, `setIslandMode`, `ROCK_LOOK_NAMES`, `BUSH_LOOK_NAMES` …).
- Borderline: `spruceMask.ts` (79, only a dev page uses it; comments claim a chunk uses it), `interact/validate.ts`
  (67, test-only; its doc says every table passes it), `chunks/_template.ts` (146, a scaffold).
- No `if (false)` branches and no commented-out code blocks.
- Side findings: the `lut` setting is inert (only `?nolut` does anything); `?kuwahara` can't run under the default
  look; `BlenderIsland.ts:4` still calls procedural the default island.
