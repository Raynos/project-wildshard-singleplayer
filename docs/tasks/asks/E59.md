# E59

**Status:** done (2026-09-23, main session) — build id below once live
**Ask:** DODGE button gets a cooldown, shown as a clock-sweep circle spinning on the disc until it's ready


Built: DODGE_COOLDOWN 0.6 → 0.8 s (Player.ts, also Left Alt); `player.dodgeCooldown` 1 → 0; the DODGE disc gets a dark
clock sweep unwinding from 12 o'clock plus a cyan ring filling round the rim, icon dimmed while it runs, a cyan flash when
it is back, a small shake for a tap before then (no queued dodge). Verified 390×844 (progress/211); gates green.
