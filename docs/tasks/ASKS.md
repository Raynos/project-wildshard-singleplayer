# The user's asks — one ledger, every request, its status, its evidence

Kept current by the parent at every commit and every time the user asks for something. Newest at the bottom;
a row never leaves — it moves from **open** to **done** (with the commit / evidence) or **dropped** (with the
user's words). If it is not in here, it was not asked, or the parent forgot — say so.

Status: **open** (nobody on it) · **in flight** (owner named) · **needs pick** (waiting on the user) · **done** · **dropped**.

Two Claude sessions work this checkout (herdr panes "Wildshard prototype 1" = load/boot/perf, "prototype 2" = phone UI/controls/menu). Each adds its own rows; prefix the ask # with the pane number when it matters.

## 2026-09-17 (evening, phone playtest)

| # | ask (the user's words, shortened) | status | owner / evidence |
|---|---|---|---|
| 1 | Main screen for the pine chunk is **not mobile friendly** | **done** | live build was stale (`5cc42cc`, no `@media`); redeployed HEAD; phone layout `98abf69` |
| 2 | Move the **reload/build pill top-right**, 3× smaller | **done** | `01cbb04` |
| 3 | Background world animation on the phone "looks broken"; **freeze it** | **done** | prototype 1 `d1ede6f` (attract camera frozen on the phone tier); superseded by #34 (menu shows hero stills, no world) |
| 4 | Menu takes too much space; **no chunk metadata**, just an Enter World button; shard list way smaller | **done** | `98abf69` phone intro; then replaced by the deck menu (#16) |
| 5 | **Kill double-tap zoom and text selection** (Safari) | **done** | `98abf69` viewport + touch-action; `0a2455f` touchmove/gesture block |
| 6 | **Mobile-friendly first-person view, fix the FOV** | **done** | `98abf69` Hor+ FOV (72° → ~94° vertical on portrait); crossbow pose/scale `881cc19` |
| 7 | **Touch controls** (there were only desktop controls) | **done** | `src/player/TouchControls.ts` `98abf69`, wired by prototype 1 `81c41e9` |
| 8 | Loading screen **bumps down on double-tap** | **done** | `0a2455f` (rubber-band blocked); `position:fixed` part reverted in `5b3d92d` (left a dead band under the bar in PWA mode) |
| 9 | Crossbow takes 100 % of the width — **whole crossbow in ~60 %** | **done** | `881cc19` (~64 %) |
| 10 | **Control bar** bottom 15 %: twin-stick, left move / right look; the four buttons just above it | **done** | `881cc19` |
| 11 | Three **codex mockups of the main menu** | **done** | `art/menu-A-cinematic.png`, `menu-B-list.png`, `menu-C-cards.png`; sent; user picked **C** |
| 12 | **Look UI needs to be faster** | **done** | `e852116` (0.54°/px, ×1.6 in the pad) |
| 13 | Move/look **only inside the bar** | **done** | `0c8dccb` |
| 14 | Crossbow "spazzed out" | **done** | `881cc19` — viewmodel lag spring diverged below ~15 fps (explicit Euler, k·dt² > 1); substepped |
| 15 | Need a **pause menu** + pause button + **Exit to main menu** | **done** (see #33) | `5b3d92d`; exit currently reloads → the user wants no reload (#33) |
| 16 | Build menu **C** in a subagent; carousel changes the hero image; placeholders: **Nalati grasslands** (coming soon), **Wind Waker–style low-poly island** (coming soon); only Pine Hollow enterable | **done** | `8db9a2c` (subagent); heroes `art/hero-*.png`; `src/chunks/placeholders.ts` |
| 17 | Carousel not snappy — **paginate**, always centred, never half-way | **done** | `5b3d92d` JS-driven track, one card per swipe |
| 18 | **Movement speed** triple → then "triple was silly, **double**" | **done** | 3× `5b3d92d`; 2× in the next commit (`Player.ts` 8.6 / 14.4 / 4.4 m/s) |
| 19 | Bottom bar **not touching the bottom** in PWA mode | **done** | `5b3d92d` — dropped `position: fixed` on html/body; confirmed gone in the 22:33 screenshot |
| 20 | Dynamic run speed not working — past the ring it **drops to walking** | **done** | `e13157d` — direction was divided by the unclamped finger distance |
| 21 | Stick ring **centred in the bar** | **done** | `e13157d` |
| 22 | "NEW BUILD · TAP TO UPDATE" **off the game view** — menu only | **done** | `e13157d` |
| 23 | Loading screen **regression** (bars gone, text misaligned) | **done** | `a0d1641` — carousel `.ws-track` collided with Loading.ts's `.ws-track` |
| 24 | **No world / no sounds / no fps meter on the main menu**; hero images only | **done** | prototype 1 owns main.ts (world paused + muted while the intro is up); Pine Hollow heroes: this session (#31) |
| 25 | **CSS sculpted per page** — unique class names per screen, fix it permanently | **done** | `087a862` `f80893d`: `hud.css` → `src/ui/styles/{base,game,menu,pause,touch,update}.css`, one prefix per screen, `scripts/check-css.mjs` on prebuild (fails on collisions; loading/perf warn-only until prototype 1 renames `ws-tagline`/`ws-fps`); also fixed the compass letters picking up the deck's `.ws-card` |
| 26 | **Tap the look pad to fire**, remove the FIRE button | **done** | `df60e4c` (tap < 12 px / < 300 ms in the LOOK pad fires) |
| 27 | **Aim is a toggle**, not a hold | **done** | `df60e4c` (AIM latches, cyan while on) |
| 28 | **Aim = iron sights**, not a zoom: no crosshair, crossbow centred; **3 codex mockups** → user picked **A** | **done** | `859379d` (bolt tip on the aim line, FOV 72 both ways, crosshair fades; portrait limbs 72 %) — `art/ads-A-centred-low.png`, `ads-B-eye-level.png`, `ads-C-peep-sight.png` sent (B's HUD was redrawn by codex — judge the pose only) |
| 29 | Exit to main menu **bumps to the loading screen** — should go to the chunk selector | **done** | `fd5d8de` (HUD re-shows the deck, no reload) + prototype 1 `9bda267` (world frozen + muted under it) |
| 30 | **Loading screen gone** (blank, build `b-mu6ezhob`) | **done** | prototype 1: `.ws-load` root rules restored in loading.css; live `b-mu6f75ka` |
| 31 | **Codex hero image for Pine Hollow** so the menu never shows the live world at 29 fps | **done** | `815d8e6` `art/hero-pine-hollow-{portrait,landscape}.png` → `src/chunks/thumbs/`, `heroPortrait/heroLandscape` on the ChunkDef; the menu shows it once #33 lands |
| 32 | **Show me the three (iron-sights) mockups** / **what's the current progress** | **done** | sent 22:46 |
| 33 | **The flow**: startup/reload → loading (always) → main menu = chunk selection → Enter → Pause → Exit → chunk selection **without** reload or loading screen | **done** | prototype 1: world pause/mute + `hud.onExitToMenu`; this session: HUD re-shows the intro, never reloads |
| 34 | Port trials-gauntlet's **asks process** so chat asks persist durably | **done** | this file; `AGENTS.md` bullet; `.claude/hooks/session-brief.sh` + `.claude/settings.json` SessionStart |
| 35 | "Current build is pretty badly damaged, I'll wait for a deploy" | **done** | `b-mu6f75ka` (loading fix + perf) then `b-mu6fmkqb` (CSS split, tap-to-fire, AIM toggle, 2× speed, Pine Hollow heroes on the def) |
| 36 | **Iron sights rebuilt to match mockup A** — 859379d rejected ("does nothing about aiming down the iron sights"): camera must look down the bolt axis, tip just below centre, string at the bottom, limbs mid-height | **done** | `9597753` — ADS pose solved at runtime from the bolt/nut geometry + FOV per aspect (`src/player/Crossbow.ts`, rotation 0, tip at NDC (0, −0.12), nut just in front of the near plane; limbs ±0.63 desktop, edge to edge portrait; bolts fly the eye→tip ray so the impact projects on the tip) |
| 37 | **Hoverboard mode + toggle button; walk speed back to 1×** ("double run speed feels silly now") | **done** | `c3632f2` (landed inside that commit — its message says iron sights; the hoverboard diff is in it): `Player.ts` 2.2 / 7.2 / 4.3 m/s again; hover = `H` / HOVER touch button (latched), 14 m/s cruise, 0.45 m ride height, ≤ 6° roll; `src/player/Hoverboard.ts` viewmodel |
| 38 | **New HUD from approved mockups K1 + P2** — compass band with cabin/animal markers + CABIN · 180 m readout (desktop + touch), round AIM / JUMP discs, vitals + bolts inside the control bar corners, no RELOAD button | **done** | `5d4b82a` compass band (HUD.ts + game.css; `hud.setAnimals()` / `HUDState.nearest` feed the paw — main.ts not yet wired, so no paw in play) · `cc1ad79` P2 touch layout (bar strips, discs, HOVER disc kept, PAUSE under the compass) |
| 39 | **Tracer bolts + bolts stay stuck in trees (debugging the sights)** — "a massive glowing red tracer" to see where the bolt goes; bolts stick in trees permanently for target practice | **done** | `03d00ac` — `TRACER_ON` in `src/player/Crossbow.ts` (`?tracer=0` off): red glow on the flying bolt, 8 px solid-red trail of the whole flight (no depth test, 6 s then fades), distance-scaled red ring at the impact; stuck bolts never despawn (cap 200), head 8 cm into the drawn bark, red nock dot; trunk hit test = full padded radius over the full trunk |
| 40 | **Rear peep sight for ADS** (mockup `art/ads-C-peep-sight.png`): a round iron sight the bolt goes through the middle of — "then I know exactly that the bolt should be going into the middle of this round iron sight" | **done** | `bc41246` — ring on a post in front of the nut, centre solved onto the eye→tip line (`adsPose.peepY/peepR`); ring vs tip within 0.003 NDC desktop + phone; tracer impact lands in the ring. Tracer is now a runtime setting: `getSetting('tracers')` (`src/ui/Settings.ts`, localStorage `ws.settings.v1`) read per shot, `?tracer=0/1` writes it once at load |

| K2 | One HOLO SURVEY HUD edit of base-phone; save art/hud-K2-holosurvey.png | **done** | art/hud-K2-holosurvey.png; built-in imagegen single generation |

| K3 | One built-in image edit: Cartographer HUD on base-phone; save art/hud-K3-cartographer.png | **done** | art/hud-K3-cartographer.png; single built-in imagegen edit |

| K1 | One Staging Glass HUD edit of base-phone; save art/hud-K1-stagingglass.png | **done** | art/hud-K1-stagingglass.png; one built-in imagegen edit |

| N2 | One built-in image edit: CONSOLE BAR on base-phone; save art/hud-N2-consolebar.png | **done** | art/hud-N2-consolebar.png; single built-in imagegen edit |

| N3 | One built-in HOLO BAR edit of base-phone; save art/hud-N3-holobar.png | **done** | art/hud-N3-holobar.png; one built-in imagegen edit |

| N1 | One built-in image edit: GRIP BAR HUD on base-phone; save art/hud-N1-gripbar.png | **done** | art/hud-N2-consolebar.png; single built-in imagegen edit |

| P1 | One built-in EDGES HUD edit of base-phone; save art/hud-P1-baredges.png | **done** | art/hud-P1-baredges.png; single built-in generation; outer-edge vitals/ammo |

| P2 | One built-in TOP CORNERS HUD edit; save art/hud-P2-barcorners.png | **done** | art/hud-P2-barcorners.png; single built-in generation |

| MB | One built-in terrain minimap screenshot edit; save art/minimap-B-terrain.png | **done** | art/minimap-B-terrain.png; single built-in imagegen edit |

| MINIMAP-A | One built-in radar minimap screenshot edit; save art/minimap-A-radar.png | **done** | art/minimap-A-radar.png; single built-in imagegen edit |

| MC | One built-in screenshot edit: holo ring minimap; save art/minimap-C-holo.png | **done** | art/minimap-C-holo.png; single built-in generation |

| MK1B | One built-in terrain minimap edit of K1 screenshot; save art/minimap-k1-B-terrain.png | **done** | art/minimap-k1-A-radar.png; single built-in imagegen edit |

| MK1A | One built-in edit of K1: circular radar minimap; save art/minimap-k1-A-radar.png | **done** | art/minimap-k1-A-radar.png; single built-in imagegen edit |

| MK1C | One built-in K1 screenshot edit: holo ring minimap; save art/minimap-k1-C-holo.png | **done** | art/minimap-k1-A-radar.png; single built-in imagegen edit |

| L1 | Load **5× faster on the iPhone**; the shaders step sat at "140 / 142" for 10–53 s; "the load time of all these shaders and programs is really bad" | **in flight** (2026-09-22 session: P3 + P5 agents; phone reading pending — only the user's iPhone can close this) | Round 1: `d584095` precompile everything (scene + shadow-depth + sky box + post chain, parallel link, live count; r186 PCFSoft→PCF recompile settled) · `3996b33` LINK_STATUS resolve · `5c6b8ef` textures uploaded in the step. Round 2: programs 103 → 75 (`d4c9657` `0b731ef` `114b4b3` `c4287fd` `05980ed` `714b6c2`), 0 at first frame; `9f6c3e2` SW static-cache migration (the 16 s cabins run was a cache wipe: +1 asset deploy now costs 0.83 MB not 33 MB). Desktop fresh context: shaders 3.5 s → 0.28 s, first frame 1.86 s → 0.07 s. |
| L2 | Loading bar is **not continuous — arbitrary chunks** | **done** | `da7c9c3` — steps weighted by the previous run's wall ms (localStorage per tier/cores), running step = max(sub-progress, elapsed/expected) < 1, republished every frame; 100 only at done(). Desktop trace: 232 paints / 1.2 s, 0 regressions, longest gap 214 ms. |
| L3 | **Pre-bake at deploy time** so the phone does no maths at launch | **done** (terrain, cards, clouds/planet/fur textures, sun/horizon) / open (PMREM, cabin geometry, KTX2) | `937c76d` terrain.bin (Heightfield lookups) · `0ad009e` branch cards headless (`scripts/bake-cards.mjs`) · `2bb4d4c` procedural textures (`bakedTexture()`, `scripts/bake-textures.mjs`) · `5519871` sky.json. Desktop fresh context per step: sky 89 · terrain 82 · cards 68 · grass 65 · cabins 94 · herds 16 (was 105) · shaders 277 · first frame 72; 2.4 s to the world incl. browser start. |
| MM1 | **Top-down circular minimap (B)** with red/yellow animal dots — art/minimap-k1-B-terrain.png | **done** | `b3b6568` src/ui/Minimap.ts + minimap.css (north-up, scrolls, fog of war, deer yellow / boar red + charge pulse); main.ts wiring (`new Minimap()` + `minimap.update(...)`) owned by the HUD session |

| BUTTONS-C | One built-in screenshot edit: lower crossbow and corner discs; save art/buttons-C-lowered.png | **done** | art/buttons-C-lowered.png; single built-in imagegen edit |

| BUTTONS-A | One built-in screenshot edit: edge-docked AIM/JUMP discs and right-edge HOVER pill; save art/buttons-A-edges.png | **done** | art/buttons-B-inbar.png; single built-in imagegen edit |

| BUTTONS-B | One built-in screenshot edit: compact AIM / HOVER / JUMP pills inside control bar; save art/buttons-B-inbar.png | **done** | art/buttons-B-inbar.png; single built-in imagegen edit |
| 40 | **Hunting feel** — "deer run away very fast and are very easy to scare, I just can't shoot them"; health bars above the deer; MMO-style MISS / damage numbers when I shoot at something | **done** (hunting-feel agent; main.ts wiring = `new Combat(game, animals, crossbow, game.camera)` + the 2-line Crossbow damage hook, see report) | `696f89b` — `DEER_TUNING` / `BOAR_TUNING` in `src/entities/AnimalManager.ts`: sight cone ±75° (52 m head-up / 26 m grazing) + hearing 5/11/26/48 m by player speed feed an awareness meter; alert = head up + 1.8–3.2 s freeze (the shot window), then bolt at 8.6 m/s only to 65–90 m, trot, look back, graze; hit → bolt at once; boar charges when hit ≤ 25 m or you ≤ 10 m; a bolting animal takes its herd (15 m). `src/ui/Combat.ts` + `combat.css`: pooled `ws-combat-hp` bars (≤ 6 on phone, within 60 m, hit < 4 s ago or under the crosshair), `ws-combat-float` numbers / HEADSHOT / KILL / MISS. Deer 60 hp, boar 100, body 32–40 (falloff past 40 m), head ×2.5. progress/071–073 |
| 41 | **Aim assist (touch) + pause-menu toggles** — "like PS4 Call of Duty / GTA 5 with a controller — it helps, it snaps, some stickiness … but don't make it trivial"; AIM ASSIST and TRACER BOLTS as toggles on the pause menu | **done** (aim-assist agent; main.ts wiring = `setAimTargets(animals.animals)` from `./player/AimTargets`, Crossbow should read `getSetting('tracers')`) | `4961546` + follow-up — `src/player/AimAssist.ts` (touch only, from `player.preUpdate`): friction ×0.6 hip / ×0.45 ADS inside a screen-space bubble (animal radius + 0.8 / 1.2 m ≈ 4.5° / 5.5° at 20 m, → ×1 at the edge), 180 ms ease-out snap onto the upper body when AIM latches (≤ 5.2° at 20 m, lands 0.4° short, not during a > 200 px/s flick), 65 % bearing-rate tracking inside 2.6° (≤ 45°/s, released by a drag away). Measured: 4.0° → 0.41° in 180 ms; 0.87°/px → 0.54°/px at the centre; walking deer 5.7°/s → sight follows 3.7°/s; switch off = 0.000° moved. `src/ui/Settings.ts` (localStorage ws.settings.v1), pause SETTINGS pills in `HUD.ts` / `pause.css`. progress/075–076 |
| 42 | **Hoverboard jump needs oomph**; **double jump** when walking | **done** | `66342aa` — 9.5 m/s ballistic launch, nose kick, landing compression; edge-triggered double jump |
| 43 | **Iron sights: add the round peep sight** (mockup C ring) so the bolt visibly goes through its centre | **done** | `bc41246` — ring solved onto the eye→tip line; impact lands in the ring |
| 44 | **Buttons mess up the FOV with the crossbow** — 3 codex mockups → user picked **A** (edge docks) | **done** | `art/buttons-{A,B,C}-*.png`; layout A `9c732fa` |
| 45 | **PAUSE and fps counter pushed to the very top** (×3) | **done** | `530bb35`, prototype 1 `da8384a` — both in the status-bar band |
| 46 | **HP/vitals out of the black bar** (thumbs cover it) → then "**half above, bolts back inside**" | **done** | `0673608` then `4615043` |
| 47 | **Crossbow bolts iron, not wood** | **done** | `4615043` — iron shaft in the bolt atlas |
| 48 | **Minimap 33 % bigger, compass two-thirds width floating left, minimap beside it** → then "**compass a bit lower and 25 % thinner**" | **done** | `4615043`, `62becba` |
| 49 | (parent's item) `setAimTargets(animals.animals)` is **not wired in main.ts** — aim assist is inert in production until it is | **done** | prototype 1 (main.ts): one import + one line after `animals` exists |


## 2026-09-18 (Driftwood Isle)

| # | ask (the user's words, shortened) | status | owner / evidence |
|---|---|---|---|
| D1 | "We are building Driftwood Island" — the low-poly Wind Waker–style island shard, currently a "not yet playable" teaser | **done** | shard live since 01:45 (b-mu6lg86q), plan complete at 04:23 (b-mu6r3wl0); docs/plans/DRIFTWOOD.md |
| D2 | **Mockups via parallel codex agents: the spawn point in three different low-poly art styles** | **done** | art/driftwood-spawn-{A-toon,B-faceted,C-painterly}.png (third-person round) — user: **B is closest**; Claude: B is also by far the easiest (no textures, flat-shaded procedural geometry, no outline pass) |
| D3 | **Mockups of two or three points of interest** | **done** | art/driftwood-poi-{1-lookout,2-wreck-cove,3-shrine}.png — user: "the points of interest are amazing" |
| D4 | Island gameplay is **different from Pine Hollow** — "more like Zelda Wind Waker"; then: **"this game engine is still first person"**, same player/HUD code as Pine Hollow | **done** | first round was third-person (wrong); second round regenerated first-person |
| D8 | **Portrait**, with the **same HUD as Pine Hollow** (PAUSE top, minimap, black touch bar at the bottom, no ammo counter) — user sent the real phone capture as the reference | **done** | art/driftwood-fp-{spawn,poi-1-lookout,poi-2-wreck-cove,poi-3-shrine,sword-wooden,sword-iron}.png (1024×1536, style B, HUD copied from the capture, no bolt counter) |
| D9 | **Full top-down map of the island** as a codex render (what the minimap zooms out to), POIs on it | **done** | art/driftwood-map-topdown.png (1254², north up, 5 labelled POIs, 4 edge jetties) |
| D5 | Equipment: **mockups of a basic low-poly wooden sword and a basic low-poly iron sword**; one swing animation each (no shoot/reload); **first implementation = wooden sword only** | **done** (mockups) | art/driftwood-fp-sword-{wooden,iron}.png; implementation open under D1 |
| D6 | Where does **equipment / inventory** go in the UI — "forget about equipment … I'll deal with inventory and equipment later" | **dropped** (deferred by the user) | — |
| D7 | "We already implemented the boar — do the same boar in the low-poly art style, and start there" | **done** | boar-agent: style 'lowpoly' in AnimalFactory / species (78c0afa, 41acbbc); progress/077–083 |

| P1 | **DPR 1.5 + AA on by default on phones** ("game changer for the crossbow") | **done** | `b-mu6i9vn4`; user confirmed 1.5 + AA stays the phone default |
| P2 | **Deer less skittish** — "I need to be able to get close and shoot them" | **done** | `6bd73ad` / `b-mu6iff3u`: sight 30/14 m, hears a walker at 12 m, 4–7 s freeze, run 6.0 (< sprint 7.2), stops at 35–50 m |
| P3 | Crossbow shader programs 8 → 4 with the look unchanged (load agent's patch) | **done** | `3ce85f6` (+ `f2f335f` LOAD-PERF row), live `f2f335f`: crossbow 8 → 4 programs (one Physical + vertex colour for stock / iron / brass / cord / leather / peep, bolt single-pass, depth clearer fog-free), total 79 → 77 (two were shared with other viewmodels); prod anisotropy kept; progress/141–142. Follow-up `7bc91dd` (+ `dee5422` row), live `dee5422`: pbr hands (−3) + rifle (−1) share the crossbow program → Pine Hollow tier=phone 73 programs; progress/144–145. Next: fogless depth clearers in Sword / Hoverboard / Rifle / Hands (−1) |
| P4 | Remove the DBG pill (tier / dpr / aa / meter) once the graphics defaults are settled | **done** | `28286f2` (live `28286f2-mud4s65a`): DBG pill + its tier.ts localStorage overrides removed; phone defaults (DPR 1.5 + AA) and `?tier=` URL knobs kept |
| P5 | Cold first-launch bytes 30 MB → ~10 MB (baked PMREM instead of the 4 MB HDR, model textures to the phone tier, KTX2 for GPU memory) | **in flight** (2026-09-22, cold-bytes agent) | Pine Hollow tier=phone cold **37.47 → 10.00 MB**, Driftwood 1.62 MB (live `dd839c6`): `1c4d87b` prop textures q85 · `1374a41` sky HDR → 0.3 MB gain-mapped pair + .phone copies · `50b9326` SW skips hero images · `81fdb22` phone tier WebP for every texture set / prop / LOD GLB; terrain layers stay 1024 (512 visibly soft). Left: per-shard manifest, Driftwood clouds/planet bake (request-neutral), bench-load abort; Pine Hollow request pack → E4. Was: docs/plans/LOAD-PERF.md §P1; the SW cache migration (9f6c3e2) already makes it a one-time cost |
| P6 | Shard 3 biome (shard 2 = Driftwood Isle, D1) | **open** | user: "I'll tell later" |
| X1 | Black screen on iOS app switch | **closed — accepted** | iOS restores a suspended standalone app before any JS runs; overlay / mirror / hidden canvas / manifest colour / wake lock / keep-alive audio made no difference; modal removed in `fa47487` |
| X2 | 30 fps in Low Power Mode | **closed — accepted** | iOS caps rAF and timers at 30 Hz; `?loop=timer` tried and removed |
| V1 | **Deer/boar variants + rarity** — "white deer; black boar with bigger tusks; mix up sizes — bigger boars, smaller and bigger deer; a rarity system with rarer boars and rarer deer — choose some fun stuff" | **done** | `78c0afa` — species registry (`src/entities/species/{registry,loft,deer,boar}.ts`, AnimalFactory globs the folder; bear/elk = one new file). Deer: hind, stag, **white hind/stag** (8 %), **Great stag** ×1.25 90 hp (8 %), **Piebald** rare (3 %), **Ghost stag** legendary ×1.35 130 hp cyan glow (1 %). Boar: boar, **sow** ×0.85 70 hp, **Black boar** tusks ×1.6 (10 %), **Big boar** ×1.25 140 hp (8 %), **Scarback** rare ×1.3 tusks ×1.8 180 hp (3 %), **Old Ironhide** legendary ×1.5 tusks ×2.2 300 hp, 40 % less body damage, relentless (1 %). One legendary alive per kind. Health bar shows the variant name; rare/legendary = white ring on the minimap; Pine Hollow gets a black/scarback sounder deep under the canopy. DEER_TUNING untouched (multipliers only) |
| V2 | **Bears: black + brown, aggressive** — "black bear and brown bear — aggressive enemies, they will come for you" | **done** | `ea10b2b` — `src/entities/species/bear.ts` (registry): **Black bear** common 220 hp ×0.95–1.05 (a third with a cream chest blaze), **Brown bear** uncommon 320 hp ×1.15–1.3 shoulder hump, body damage ×0.85, **Old Blackpaw** rare ×1.25 330 hp, **Grizzled Sow** rare ×1.45 480 hp. Walk 1.0 · stalk 2.5 · charge **9 m/s** (you sprint 7.2 — only the hoverboard outruns one); charge damage 35 / 45 / 42 / 55. HUNTERS (`HuntTuning.stalk`, new `stalk` AI state in AnimalManager): inside 40 m (or a bolt landing within 80 m) → alert 1–2 s → stalk toward you huffing → charge from 14 m; after a hit or a timed-out charge it keeps pressing (re-charge 1.5 s) until you are 60 m away; a hit makes it charge from anywhere, never flee (a black bear under 20 % may break off 50 %; brown / olds never). Pine Hollow: a black den (2) NW corner + a brown den (1) NE, far off the trails. Sounds bear_growl / bear_roar / bear_hurt; red pulsing minimap dot. Verified: onCharge 35 every ~2 s, vitals 100 → 65 → 30. progress/090–094 |
| D10 | **"Go ahead and implement the whole thing, incrementally, screenshots along the way; get me walking around quickly"** — world first | **done** | every checkpoint C0–C16 built and deployed; see D28/D31 |
| D11 | **One subagent each: boar (low-poly), sword (wooden), world** | **done** | boar 78c0afa/41acbbc · sword be6b454/7e579fb · world 3a83028 → b9cd524 |
| D12 | World order: **fill the world with water + just the pier** → then **a little boat by the pier** → then **the beach** (what you see from the pier) → then the rest of the island piece by piece | **done** | world-agent: pier 3a83028 → boat 54e85c5 → beach d845c26 → hut/palms/lookout/wreck/shrine/bushes/jetties/paths/bridge |
| D13 | **Swimming mechanism** (subagent) — it's all water | **done** | swim-agent 4817360, 4397077, 92781d5 |
| D14 | **Diving** (second subagent, after swimming): in water JUMP becomes **DIVE**; holding DIVE goes down, a **SURFACE** button appears beside it that goes up; no other vertical control for now | **done** | dive-agent 56cb2e1, bc163c9: DIVE / SURFACE discs, neutral buoyancy, underwater look, seabed |

| M1 | **Full map**: tap the minimap → whole chunk, drag to pan, pinch/wheel zoom, fog of war, cabins + pond as POIs, no animal markers; CLOSE / Esc / M | **done** | `019d9b9` + phone tap fix `232547a`; user: "map works" |
| D15 | "Race to the fastest checkpoint: **sword + pier**, even half broken; mark the world **super experimental**"; then keep building everything | **done** | checkpoint deployed 01:45 (b-mu6lg86q, D19); everything after it built through D31 |
| D16 | "What else should we be building?" — Claude's list from the mockups: **gulls**, **ringed planet**, **palms**, **thatched hut**, fences/steps/signposts, **lookout + rope bridge**, **wreck cove** (glow inside → iron sword pickup), **ring shrine**, full-map screen with POI labels, boar hit-stun for melee, boar senses retuned for open beach, something to dive for, ocean-edge boundary, hero/thumbnail in style B, island audio (waves/gulls/planks/sand/splash) | **done** | all of it shipped through the plan rows C7–C16 (D28, D30) |
| D17 | **Swimming = no weapon, hands only**; simple swim-stroke hand animation; **different hand styles per shard** (Pine Hollow PBR vs Driftwood low-poly); **white gloves at all times** to dodge hand-rendering difficulty | **done** | src/player/Hands.ts 92781d5 — white gloves, pbr / lowpoly per shard |

## 2026-09-18 (Pine Hollow gameplay: one menu, four tabs, shard achievements)

| # | ask (the user's words, shortened) | status | owner / evidence |
|---|---|---|---|
| G1 | "I need some kind of gameplay in Pine Hollow — we hunt, but there's no achievements, no progression, no quests" → **shard-specific achievements with shard-specific (funny) titles** | **done** | the in-game menu (G2), shard achievements → titles (G4), legendary skin drops (G5/G6); Pine Hollow now has goals: 6 achievements, 2 legendary hunts |
| G2 | **Pause menu becomes just "Menu"** with four tabs: **Map · Inventory · Achievements · Settings**. Tapping the minimap opens the menu on the **Map** tab; the pause button opens it on the **Settings** tab (Resume / Exit / switches live there) | **done** | built by the (update) row below: `8a6defc` — Menu.ts four tabs, minimap → Map (`main.ts` `fullMap.bindMinimap`), pause/Esc → Settings; flipped 2026-09-22 after a code check |
| G3 | **Mockups of all four tabs** (portrait phone, the glass HUD identity) | **done** | art/menu-tab-{map,inventory,achievements,settings}.png (1024×1536, four parallel codex runs) |
| G4 | Achievement list: **kill 5 deer, 5 boar, 3 elk, 2 bear**; reward = a **funny title** each. Titles would show under the name in multiplayer; single-player only shows them on the Achievements tab | **done** | src/game/achievements.ts (Pine Hollow: deer 5 / boar 5 / elk 3 / bear 2 + Ghost stag / Old Ironhide); elk (cd6462a) and bear (ea10b2b) species landed with kinds 'elk' / 'bear', so every row counts |
| G5 | **Ghost stag drops a skinned crossbow, Old Ironhide drops a skinned AR-15**; picking it up just swaps the skin for now. **Mockups of what "cool" means** → then: "**implement them all** — rifle A and crossbow A are the skins that drop; the other two live in the code, no enemy drops them" | **done** | mockups art/skin-*.png; code `36b6c52` — src/player/Skins.ts (Ghost Stag / Hollow Ash / Ironhide / Scarback Furnace), SkinLocker persists; progress/099-skin-*.png; `?skin=<id>` previews |
| G6 | The legendary drop is **not the carcass-harvest UI** — "a sexy unique item drops on the floor, big, and you walk over it to get it" (the floating-pickup presentation) | **done** | `36b6c52` + `21dea8f` — WeaponPickup tier 'rare' (purple) where the legendary fell, the skinned weapon inside at 1.6× / 1.35×; `?drop=<id>` spawns one ahead |
| G8 | "Achievements menu can't be scrolled with touch events" | **done** | `21dea8f` — the three scrolling panels carry `data-scroll` (index.html swallows touchmove elsewhere) |
| W1 | **AR-15 rifle + weapon swap** — a second weapon, semi-auto, iron sights like the crossbow; `1` / `2` / `Q`, touch SWAP pill; then: the player does **not** start with it — "you gotta find it": a floating orb pickup in a cabin (art/pickup-A-bubble.png; blue orb = items, purple = rare skins) | **done** (rifle agent) | `b4edc3b` — src/player/Rifle.ts (one lit program, hitscan, damageFor × 0.55, 30 + 90, 1.6 s reload, flash / brass / kick / tracer), Weapons.ts (kit manager, holster blend, lock / unlock), WeaponPickup.ts (`ItemPickup`, tier colour, Fresnel orb + motes + light pool; cabin 1 by the door wall, `[E] Take AR-15`), HUD ammo strip generic (ROUNDS 27 / 30 + 60 + weapon tag), rifle / reload / swap sounds; `?weapon=rifle` unlocks at load. Screenshots: scratchpad rifle-*.png (hip, ADS, burst 25 / 30, reload 30 / 30 + 85, phone SWAP, pickup orb, pickup burst) |
| G2 | (update) user: "those mockups look great — **implement that**" | **done** | `8a6defc` (+ `e1c3636` build fix) — src/ui/Menu.ts + gmenu.css, src/game/{achievements,Progress,Inventory}.ts, Map.ts embedded, HUD pause → menu; progress/098-menu-*-tab.png; live |
| G7 | "Use herdr — the agent on the new island does the deploy" | **done** | handed to wildshard-proto-3 via `herdr agent prompt` (deploy from a clean `git archive HEAD` export) |
| D18 | **Plan doc in docs/plans** with everything decided: gulls, planet, sailboat, palms, hut, fences, steps, signpost, rope bridge, wreck cove + ring shrine ("go nuts"), sword combo (2–3 swings, three-hit combo, no block/dodge — strafe or eat it), iron sword hovering pickup, diving decorative only, swim forever / no drowning, same chunk-edge force field underwater, style-B hero art, loading screen, audio, SUPER EXPERIMENTAL tag; "build the whole plan overnight" | **done** (doc) / **in flight** (build) | docs/plans/DRIFTWOOD.md |

| PICKUP-A | One built-in cabin screenshot edit: holo-bubble AR-15 pickup; save art/pickup-A-bubble.png | **done** | art/pickup-A-bubble.png; single built-in imagegen edit |

| PICKUP-B | Single built-in image edit: cabin AR-15 sigil-ring pickup; save art/pickup-B-ring.png | **done** | art/pickup-B-ring.png; one built-in generation |

| PICKUP-C | One built-in cabin screenshot edit: diegetic AR-15 on crate, ammo and lantern; save art/pickup-C-diegetic.png | **done** | art/pickup-C-diegetic.png; single built-in generation |
| D19 | "**LET ME IN**" — deploy checkpoint C0/C1 | **done** | `4a7c034` built from a clean export, live at https://wildshard-singleplayer.vercel.app (build b-mu6lg86q): ocean + pier + wooden sword + swimming |
| D20 | World/sword agents as **checkpoint agents** (world v0.0.1 / sword v0.1: commit the minimal, deploy, then keep building) | **done** | both committed WIP on request; world-agent continues boat → beach → island |
| D21 | Use **herdr** to see what the other two sessions are doing (menus, floor pickups + pickup mockups, enemy variants / new enemy types) and reuse: floor-pickup for the iron sword; **low-poly bears** on the island; **add three new enemy types** that fit the style to the plan (+ mockups) — "go nuts" | **done** | herdr used for handoffs + coordination; bears on the island (loot-agent f425ce7); three enemy types (D26/D30) |
| D22 | "**It's stuck loading**" on the iPhone (step 6, download frozen) → "deploy the error state", "**add an uncaught exception modal**", "need source maps?" | **done** | `c985098` src/ui/ErrorModal.ts (message, stack, build/URL/UA, RELOAD / COPY / DISMISS) + BOOT FAILED foot line; the modal caught it: `e[0].index` = `mergeGeometries([])` — the SW served a stale same-size `terrain.bin` bake (flat sea floor → nothing above water → empty palm merge) → `37c27ce` never migrate /assets/baked/**, esbuild keepNames + hidden source maps; `5b798c8` purge cached bakes on activate + network-first. Live b-mu6nepgr |
| D23 | "Aiming the sword doesn't make sense — top 3 melee ideas for the AIM button" → Claude: HEAVY (hold-charge overhead), KICK/SHOVE stagger, LOCK-ON; recommended HEAVY | **done** | combo-agent 56bc97e: HEAVY on the AIM disc (hold-charge overhead, stagger); label HEAVY 7787f9a |
| D24 | Deploys 2–5: boat, beach, boulders, hut, palms, island minimap, hands, sword sounds (b-mu6m6qsv, b-mu6mg7yl); lookout + wreck + error modal (b-mu6mvkt2); shrine, jetties, bushes (b-mu6n8rri); SW purge (b-mu6nepgr) | **done** | `8c8e8b6` `b13ccf0` `c985098` `37c27ce` `5b798c8` — three restores after sibling sessions committed stale shared-index blobs (0181106, 2109f7e, 96f0272); parent now commits only via a private index |
| D25 | "**You can't just walk up a cliff** — the hoverboard can glide up, but cliffs/rocks/terrain must stop you running up arbitrary walls or inclines" | **done** | dive-agent 56cb2e1: SLOPE_WALK 0.72 / SLOPE_SLIDE 0.6, hoverboard exempt; progress/129 |
| D26 | **Three new enemy types** that fit the style (+ mockups) → Claude's picks: Reef Crab (tidepools, sidesteps), Coconut Monkey (palm troops, throws coconuts), Drowned Sailor (rises in the wreck at night, guards the iron sword); plus low-poly boar (done) and low-poly bear (loot-agent) | **done** | mockups art/driftwood-enemy-*.png; species live 8557c9c (D30) |
| D27 | Readable crash stacks on the phone → `b1ba49a` minifyIdentifiers off + real build id in the modal | **done** | live b-mu6nlj77 |
| D28 | Plan rows landed and deployed: C2 hands `92781d5`, C3 boat, C4 beach, C5 boar + fauna `f425ce7`, C6 dive/surface + seabed `56cb2e1`, C7 paths/steps/fences/signposts `5dfec83`, C8 rope bridge `b9cd524`, C9 wreck cove, C10 ring shrine, C11 iron sword pickup `f425ce7`, C12 gulls + C13 planet `2924d8b`, C14 island audio `3b7fa18`, sword combo + HEAVY + hit-stun `56bc97e`, slope limit `56cb2e1`, phone perf `fa3857e` | **done** | live b-mu6pqy9v; parent wiring commits `7787f9a` `eab7aa3` `af1bd55` `5d03c17` `663592b` |
| D29 | C15 hero art: the deck card / hero stills are now in-engine style-B captures from the pier (planet, gulls, boars, stairs), replacing the toon placeholder | **done** | src/chunks/thumbs/driftwood-isle{,-portrait,-landscape}.jpg |
| D30 | C16 enemies: reef crab, coconut monkey, drowned sailor + wreck-cove / shrine dressing (tidepools, cave glow, glyphs, fireflies) | **done** | enemies-agent 8557c9c + wiring c7f090a; live b-mu6r3wl0 |
| D30 | (update) C16 enemies + cove / shrine dressing | **done** | `8557c9c` (enemies-agent: crab / monkey / sailor species, Enemies.ts, Cove.ts, shrine glyphs + fireflies, 8 sounds) + `c7f090a` (wiring); live b-mu6r3wl0. Known gaps: no day/night so the sailor is wreck-only; harvest yields nothing for the new kinds |
| D31 | **The whole Driftwood plan (docs/plans/DRIFTWOOD.md C0–C16) is built and deployed** — the user plays the full shard tomorrow | **done** | 25 production deploys tonight; last b-mu6r3wl0. Not done: nothing from the plan; open ideas for later: day/night clock, harvest loot for crab/monkey/sailor, sword icon in the Inventory kit, a bake committed for the shard (currently regenerated at build) |
| D32 | "Summarize the session — what was the magic?" → write it into docs for Matthew | **done** | docs/HOW-DRIFTWOOD-GOT-BUILT.md |
| D33 | **A trailer for Project Wildshard** from the prototype, towards the vision (sources/citadel.mp4); then "2 min is way too long — 15 s and 30 s editor's cuts" | **done** | progress/wildshard-trailer{,-15s,-30s}.mp4 (1600×900 masters); live 720p: /trailer-15.mp4, /trailer-30.mp4; pipeline scripts/trailer/ (deterministic frame-stepped capture via a gated render loop, title cards, synth score) |
| D34 | "**A plan for our first original music** — 30 s for the trailer clip, also used in game" | **done** (plan) | docs/plans/MUSIC.md — the score as code: a D-Lydian theme, 7 WebAudio patches, an adaptive layer mixer (calm / alert / combat / underwater / stings), the same render for the trailer (OfflineAudioContext → WAV) and the title screen; hit points mapped to edl-30 |
| D35 | "**When the plan is done build it** and show me the 15 s and 30 s trailer with original music" | **done** | music-agent `e7f727c` `e4e81b8` `b277be2` (src/audio/Music.ts + score/wildshard-theme.ts, 7 WebAudio patches, adaptive mixer, OfflineAudioContext render, MIDI export, Settings MUSIC slider); parent `12b60eb` wires it in play (menu / calm / alert / combat / underwater / stings); trailers re-cut with the score: /trailer-15.mp4, /trailer-30.mp4; live b-mu6zc9vx |
| D36 | "**Make me a really cool PDF of this chat** for Matthew — all my steering prompts, all the mockups, all the progress screenshots, blow by blow; a big PDF is fine" | **done** | docs/Driftwood-Isle-blow-by-blow.pdf (23 pages, 13 MB; built by scripts-free HTML → Chromium PDF, source in the session scratchpad) |
| D37 | "Take the **prepare-to-exit** skill from trials-gauntlet and adapt it for this repo" | **done** | .claude/skills/prepare-to-exit/SKILL.md — private-index commits pinned to a captured base, gates on the exported tree, worktree sync, browser-session close, the ASKS/plans queue, the BYE / OOPS banner; session-brief hook now lists lettered ask ids (D…, G…, V…) and skips closed rows; 15 stale D-rows flipped to done |
| D38 | (leftover from C16) **Day / night clock** for Driftwood — the drowned sailor walks the cove beach at night; fireflies / shrine glyphs at dusk (`EnemyWorld.night`, `shrine.setDusk` exist, nothing drives them) | **open** | needs an owner; src/entities/Enemies.ts + src/world/Shrine.ts + Sky.ts |
| D39 | (leftover from C16) **Harvest loot for the new enemies** — crab / monkey / sailor carcasses harvest as "Nothing" (`harvestOf` in src/game/Inventory.ts) | **done** | `e1ebf19` + tests `a73c0b2` (CI green): crab / monkey / sailor carcasses harvest real loot into the Inventory. Was: small; also the enemies' minimap dots use the species `aggressive` flag already |
| D40 | (leftover from C11) **A sword glyph** for the Inventory kit — the wooden / iron sword rows reuse the crossbow icon (`src/ui/icons.ts`) | **done** | `f17a1e9` (live `f17a1e9-mud5c9fx`): `sword` glyph in src/ui/icons.ts for the wooden + iron sword rows; sword rows read "Melee" instead of "0 / 30"; progress/143-d40-sword-glyph-inventory.png |
| D41 | **Listen to the music** — the theme was verified spectrally only (onsets, levels, loop seam); the user's ears decide: keep / retune / commission from the MIDI export (scripts/music/wildshard-theme.mid) | **done** (picked 2026-09-22) | the user: "It's super basic, we need better music, how do we generate music with top tier local open weight models?" → E5 (MUSIC v2) |
| D42 | (music plan "later") second theme per biome (the centre chunk / citadel), a diegetic shrine hum of the motif's first interval, a composer pass from the MIDI | **folded into E5** (2026-09-22) | the user picked: second theme = Driftwood Isle, shrine hum always by proximity — now rows 4 + 7 of MUSIC v2 (model-generated, not synth) |
| D43 | HEAD `34efff4` is three docs/hook commits past the live build b-mu6zc9vx (`12b60eb`) — nothing player-facing changed; next deploy from a clean export of HEAD whenever the next feature lands. This session owned production deploys (herdr handoff G7); it ends with this checkpoint | **done** | superseded by CD (D46): every push to `main` deploys via GitHub Actions; live build `4e14b5c` = HEAD as of 2026-09-20 21:27 UTC |

## 2026-09-20 (collaborators, continuous deployment, strict gates)

| # | Ask (the user's words, shortened) | Status | Evidence / where to resume |
|---|---|---|---|
| D44 | **Who are the GitHub collaborators on the prototype; invite Matt-Esch if missing** | **done** | `project-wildshard` + `-meta`: Matt-Esch write; `-singleplayer`: invite sent 2026-09-18, pending his accept — nothing to re-send |
| D45 | **Add Matt to Vercel so his agent can deploy** | **done** (by D46) | Hobby scope has no seats and Vercel's git integration rejects non-member authors — solved by CI deploying with a project-scoped token instead; he never needs a Vercel login |
| D46 | **Continuous deployment on push to main, gated on lint / tests / TypeScript** | **done** | `.github/workflows/deploy.yml` (`1e83fe9`…`82dd4b2`): typecheck → oxlint → css → vite build → `vercel deploy --prod`; secrets `VERCEL_TOKEN` (project-scoped `vcp_`), `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`; runs 35537337401 / 35538391506 / 35538733287 green, ~1 min push-to-live |
| D47 | **oxlint strictest config + tsconfig as strict as possible, loop until clean** | **done** | `c3f199f` tsconfig (24 flags, 2253 → 0), `9fd9fe1` scripts, `2bfae0a` `.oxlintrc.json` (type-aware, 7 categories error, 53,661 → 0), `d409964` Inventory. 0 `any`, 0 `!`, 0 ts-ignore, 9 reasoned per-line disables |
| D48 | (found by D47) **`no-unsafe-type-assertion` is still off** — 267 plain `as X` casts (75 `as THREE.*`, 14 `as HTMLElement`, 19 `as Float32Array`) and 37 pre-existing `as unknown as` remain; tsc cannot catch a wrong cast | **dropped** | user 2026-09-22: "whatever, I don't care" — rule stays off. Was: second sweep: turn the rule on, replace casts with `instanceof` guards at setup / typed `querySelector` wrappers / a once-narrowing `materialOf(mesh)`; hot loops must stay allocation-free. Subagent job, same behaviour-neutral rules |
| D49 | **Tests** — none exist; the CI gate has no test step | **done** | `b81fee6` (build `b81fee6-mud4xn1l`, CI run 35781396525): vitest, 93 tests in test/ (rng/noise determinism, boot plan invariants, Inventory + harvestOf, achievements/titles, Settings, species + chunk registries), ~0.4 s; deploy.yml Test step between Lint and CSS; docs/RUNNING.md Tests. Was: user: "I will deal with tests later" → 2026-09-22: do it. When added: `pnpm test` as a step between Lint and CSS check in deploy.yml |
| D50 | (found while sweeping) the **elk harvest WIP** in `Inventory.ts` (every elk drops antlers) was dropped, not committed — variant ids are cow / bull / big-bull / pale / imperial and HEAD's `/bull\|imperial/` already covers the three racked ones | **done** | `4e14b5c` commit body; if whoever wrote it meant something else, resume from `8e7d068` |
| D51 | (accident) **"Continue with GitHub" on vercel.com created a new Vercel account `jake-8460` / `jakesoundxyzs-projects`** (the raynos account is Google-login only) | **dropped** | user 2026-09-22: "don't care" — account left as is. Was: delete it (empty, 2026-09-20) or leave it; leaving it means GitHub-login on Vercel keeps landing there |
| D52 | **Port prepare-to-exit from trials-gauntlet, adapt to this repo** | **done** | already ported 2026-09-19 (`34efff4`); re-cut 2026-09-20 for CD: push = deploy, four gates on the exported tree, watch the CI run, `version.json` = HEAD is a BYE precondition; AGENTS.md Deploy + Version-control sections rewritten to match |

## 2026-09-22 (batch: load, cleanup, tests, loot, music)

| # | Ask (the user's words, shortened) | Status | Evidence / where to resume |
|---|---|---|---|
| E1 | "Do **L1, P3, P4, P5, D49, G2, D39, D40, D42**; mark D48 / D51 don't-care; D41 I'll listen later; check they're not already done; subagents as necessary" | **in flight** | G2 was already done (`8a6defc`) → flipped; D48 / D51 dropped; agents: crossbow-programs (P3), cold-bytes (P5), small-fixes (P4 D39 D40), tests (D49), music (D42) |
| E2 | "Any plans in docs/plans? **Archive the finished ones in ./project/archive**; AGENTS.md / README.md: plans carry a **state** (in progress, finished, archived…)" | **done** | `9b10c39`, live 9b10c39-mud4pmj6. DRIFTWOOD finished → `project/archive/2026-09-18-driftwood.md`; LOAD-PERF (in progress), PLAY-PERF (blocked), MUSIC (in progress) stay live with a State line; AGENTS.md → Plans (draft / in progress / blocked / finished / archived / dropped), README pointer, session brief prints each live plan's State, prepare-to-exit archives finished plans |
| E3 | "Read the game demos gauntlet trials and figure out how to make the **iOS and Android binaries** for this game" → the user: gauntlet already built its binaries; its store plan is about Ubisoft, and Wildshard is new IP; **include the Vercel update-server (OTA) stuff** | **done** | docs/plans/NATIVE-APPS.md (draft): port gauntlet branch `docs/native-mobile-publishing` (Capacitor 8.5.2 shells, native saves, signed OTA via @capgo/capacitor-updater on a separate `wildshard-updates` Vercel project); IP-scrub phases skipped. Waiting on the go + N1 app ID/name, N2 accounts (0 signing identities on this Mac), N4 min OS, N5 closed-test start. Plan live in `cf88e29` (build cf88e29-mud4t6px) → picks taken 2026-09-22, built as E23 |
| E5 | "Ask me any questions necessary about **docs/plans/MUSIC.md** so we can build it" | **in flight** (2026-09-22, this session) | picks: revenue-capped licences OK (ACE-Step 1.5 / MiniMax Music 3 / Stable Audio Open), the model composes, **three styles (piano-ambient / orchestral / folk) + an in-game switch**, ~3–5 MB per style lazy, Driftwood 2nd theme, shrine hum by proximity → docs/plans/MUSIC.md v2 |
| E6 | "I need the **right click for aim on desktop to toggle** aim mode because I'm playing on a MacBook touch pad" | **done** | `c2bf470`, live c2bf470-mud56ydk: crossbow + rifle ADS and the sword heavy: RMB click toggles instead of hold (the touch AIM disc already latches); sprint / pause / holster drop the toggle. Its commit also flipped the P3 row back to in flight by mistake (index built one commit behind); restored in the next commit |
| E4 | "How do we finish **PLAY-PERF & LOAD-PERF**?" → goal: **build both plans until finished and archived** | **in flight** (2026-09-22, this session + play-perf / load-cpu agents) | finish lines are the two plans' State lines; P3 landed `3ce85f6`, P5 (bytes, SW, baked sky) stays with the E1 cold-bytes agent; the last step is two iPhone readings only the user can take (load time, in-play meter) |
| E8 | "You are the **model agent** — the **graphical fidelity of all the models in Driftwood**" (E7 remaster) | **in flight** (2026-09-22, session wildshard-singleplayer-8d) | owns model geometry + paint: src/world/{Boat,Pier,Hut,Lookout,RopeBridge,Wreck,Shrine,Palms,Boulders,Cove,Props,Trailside,Gulls}.ts, src/entities/species/{boar,bear,crab,monkey,sailor,loft}.ts, src/entities/lowpoly.ts, dev/models.html; the per-POI gaps come from E7's audit (docs/plans/DRIFTWOOD-REMASTER.md) |
| E9 | "Update the game so **Driftwood is first and Pine Hollow second**, and mark Pine Hollow **experimental**" | **done** | `5231859`, live 5231859-mud6cq4l: src/chunks/registry.ts: DEFAULT_CHUNK = driftwood-isle, CHUNKS order Driftwood → Pine Hollow; Pine Hollow biome/blurb say EXPERIMENTAL; Driftwood's stale "SUPER EXPERIMENTAL / ocean and a pier so far" dropped. Side effect: a bare URL (and `pnpm bench:ci` without `?chunk=`) now loads Driftwood |
| E10 | (HUD agent) "**Remove the AIM button for swords and melee weapons** for now" | **done** | TouchControls.ts polls `weapons.current.id` each frame: a sword (`sword` / `sword-iron`) held → the layer gets `.melee`, touch.css hides the AIM disc, a latched ADS is released on the way in. Verified 390×844 `?touch=1`: Driftwood sword → disc `display:none`, Pine Hollow crossbow → `flex`. Side effect: the sword **heavy has no touch button** until E11's ATTACK disc lands |
| E11 | (HUD agent) "Use a subagent to **research the AIM and movement mechanisms of Call of Duty Mobile**; see how that might apply to our **driftwood sword gameplay**" → "How do we improve the twin stick touch controls … ask me any questions" → "show me the HUD / UI codex mockups (parallel)" | **in flight** (2026-09-22, HUD agent) | research relayed in chat. The user's picks: **tap LOOK pad = swing + a HEAVY disc** in the old AIM spot (tap charge / tap release); **CODM lunge** (≤ 4 m, ±25° cone → ~1.4 m in ~0.13 s, heavy 5 m, walkable ground only) on every input, camera turn touch only behind aim assist; **look = whole right half** of the screen (buttons first), but a **tap swings / fires only inside the LOOK pad**; **DODGE disc** right of the thumb beside JUMP, always shown (not swimming), direction from the MOVE stick, neutral = backstep, desktop **Left Alt** + WASD; **Look speed + Swing turn speed sliders** scaling touch and mouse. Not picked: sprint lock, floating stick. Mockups: art/hud/round-7-sword-touch/{A-dodge,B-lunge,C-heavy,D-settings}.jpg (4 parallel codex runs) — the user: "mockups look good, ship the new HUD stuff"; building |
| E13 | "The **experimental label for Pine Hollow** should be **way larger, more obvious** lol" | **done** | `9d15efa`, live 39ad6c4-mud8alol: `ChunkDef.experimental` → the title deck stamps a hazard-taped amber EXPERIMENTAL band across the card image (sized off `--card-w`: ~36 px desktop, ~26 px phone) and the ENTER hint reads "Experimental shard — rough edges ahead"; the 10 px "· EXPERIMENTAL" biome suffix is gone |
| E14 | "How do we **iterate quickly on models and graphics quality**? A **BUILD WORLD** entry next to ENTER WORLD → **god-mode freecam** (start from hxtnv/three-freecam) with a **Model Explorer** (build the models & assets) and a **Map Explorer** (build the map); **codex CLI (gpt-6-sol) mockups, parallel, lots**; mockups in a **named folder**, not loose in art/" | **in flight** (2026-09-22, session wildshard-singleplayer-8d) | 12 parallel codex runs → `art/build-world/` (title entry, model turntable / rig / catalog / A-B compare / phone, map freecam-place / top-down layers / scatter / sculpt / play-here, hub); three-freecam = npm `three-freecam` (MIT, 1.8 kB, `new FreeCam(camera, dom)`, `update(dt)`, `focus()`, `placeAt()`); next: user picks a direction → build |
| E21 | "What **rules can we set up in AGENTS.md for 10 agents committing to the same main branch**? Prior art in game-demos trials gauntlet, the final-fantasy gauntlet demo, and kami-kakushi" | **needs pick** (2026-09-22) | research relayed in chat: kami-kakushi's sweep-guard hook, push mutex (push-main.sh, leave-local), tree-claim lanes, destructive-git block; FF15's gitlock; one file per ask. Waiting on which rules to adopt |
| E19 | "Why would **git push hang**, are the last 5 commits heavy?" → "**kill all the git pushes** and use **herdr** to coordinate" | **done** (2026-09-22) | not heavy (5 commits ≈ 12 MB, two 2 MB PNGs in progress/): a ~9–118 KB/s uplink shared by 6 concurrent pushes. Killed all; herdr-told 10 agents to stop pushing; w3:p1 pushed 8df5524 (5m50s alone), 39ad6c4, 58e4bdb — live 58e4bdb-mud8fi4k. On exit handed off to `lockf -k -t 900 .git/push.lock git push origin main` (one push at a time). Rule for AGENTS.md → E21 (multi-agent git rules) |
| E15 | "What's our **physics engine and collision system** on Wildshard?" | **done** | answered in chat 2026-09-22: no physics library (three.js only) — hand-rolled kinematic controller in `Player.ts` (gravity, heightfield ground, platform callbacks, circle-vs-trunk + circle-vs-OBB push-out); weapons = rays / swept segments vs animal sphere+capsule and `heightAt` |
| E20 | "Do a **research for new physics systems** we could use to improve the engine **instead of hand-rolling absolutely everything**" (+ "bonus points for notes from the trials gauntlet's physics research plan") | **done** | research → `39ad6c4` draft `docs/plans/PHYSICS.md`: Rapier 3D pick, crashcat runner-up, navcat for animals; gauntlet plan parked / 2D, only its method ports |
| E22 | "Rapier sounds good — **make a mega plan for Rapier physics**: add the physics, update the baseline, collisions for **both worlds**, collisions for **all models / weapons / items / enemies**; **commit to the engine, not switchable**" | **needs pick** | mega plan written: `docs/plans/PHYSICS.md` (P0 baseline → P9 re-baseline, 10 phases, no switch); waiting on go + 3 picks |
| E19b | (from E19) ~17:20 2026-09-22 someone wrote the worktree `docs/tasks/ASKS.md` with `>` (2 lines left), which wiped every **uncommitted** row. E15 / E20 / E22 were re-added in da31e24. Still missing from HEAD: E7 (Driftwood audit & 10× remaster, in flight), E12 (codex B-lunge portrait mockup), E16 "Vercel-hosted feedback system?" (done, answered in chat), E17 (feedback inbox plan + codex mockups), E18 (document the mockup workflow in AGENTS.md). Owners were told over herdr to re-add them. **Open until each lost row is back in HEAD** | **done** | the 2-line write was E21's session (this commit's author): a python one-liner opened ASKS.md for writing before reading it (`open(p, "w").write(fix(open(p).read()))`). E7 / E12 / E12-IMG / E11-C / E16 / E17 / E18 recovered verbatim from the session transcripts (latest version of each row) and re-added |
| E16 | "Do we have any **Vercel-hosted feedback system** like in game demos gauntlet trials?" | **done** | answered in chat 2026-09-22: no — Wildshard has no `api/`, no `@vercel/blob`, no `?review=1`. Gauntlet's is `api/inbox.ts` (password-gated Vercel function → private Vercel Blob, `REVIEW_PASSWORD`), `src/ui/inbox.ts` (✎ NOTE sheet: words + auto context + canvas JPEG, offline queue), `scripts/inbox-pull.ts` (`pnpm inbox:pull` → `.review/inbox/`), `.claude/skills/drain-inbox` → E17 |
| E17 | "Make a **plan** on how to build it (the gauntlet-style Vercel feedback inbox), make **codex CLI mockups** of what it would look like in UI" | **done** | `24ff355` + `a396197`, live 1da15cd-mudaqlfr: docs/plans/FEEDBACK-INBOX.md (draft: F1–F7 build order, picks D1–D5 wait on the user); six mockups `art/feedback/round-1-inbox/` A-desktop-sheet, B-phone-sheet, C-menu-tab, D-annotate, E-phone-sent, F-desktop-quick (.jpg; 6 parallel codex image_gen edits of live 1600×900 + 390×844 captures, none re-rolled) |
| E18 | "**Document** the mockup / art / concept art / image gen workflow in **AGENTS.md** :)" | **done** | `24ff355` + `a396197`, live 1da15cd-mudaqlfr: AGENTS.md § Mockups: folder rule, start from a live capture, the `codex exec` recipe (one image per run, parallel, copy your own image_gen output, not the newest), COMMON + SCREEN prompt blocks, look at every image, several variants |
| E7 | "You are the ultimate research agent — **audit & review this game against its README.md & BRIEF.md**; focus on **Driftwood**: we want to **remaster it and 10× its quality and polish**" → "yes we will need a full remaster" | **in flight** (2026-09-22, this session) | audit done (no BRIEF.md → docs/SUBAGENT-BRIEF.md + sources/): code/gameplay, stylized-island research, live captures vs art/ → **docs/plans/DRIFTWOOD-REMASTER.md** (`in progress`): 10 bugs (B1 sword reach = Infinity, B2 "Gored" toast + bolts, B5 eye-fan hit test through walls, B6 title hero is the AI mockup since 4e14b5c …) + tracks 0 fixes / L look / W water / M models (E8) / C combat feel / A adventure / S sound; picks D1–D5 open. (`3bb9fd4` is an empty commit — the row + plan land in the next one) |
| E12 | Generate **one 1024×1536 portrait mobile screenshot mockup** of Driftwood beach crab melee lunge (layout B), preserving the supplied world and HUD, and save it as `art/touch-sword-B-lunge.png` | **folded into E11** (2026-09-22) | a row written by one of the HUD agent's four parallel codex image runs (E11), not a user ask; the image is `art/hud/round-7-sword-touch/B-lunge.jpg` |
| E12-IMG | "Use image generation to make one 1024×1536 portrait Wildshard sword PAUSE MENU Settings screenshot, save as art/touch-sword-D-settings.png" | **folded into E11** (2026-09-22) | a row written by one of the HUD agent's four parallel codex image runs (E11), not a user ask; the image is `art/hud/round-7-sword-touch/D-settings.jpg` |
| E11-C | One portrait 1024×1536 imagegen mockup of Driftwood sword HEAVY charge HUD; save `art/touch-sword-C-heavy.png` | **folded into E11** (2026-09-22) | a row written by one of the HUD agent's four parallel codex image runs (E11), not a user ask; the image is `art/hud/round-7-sword-touch/C-heavy.jpg` |
| E23 | "Build the plan, build the native apps. com.jakeverbaten.wildshard-singleplayer is fine" (picks: Android id `com.jakeverbaten.wildshard_singleplayer`, no accounts yet, OTA promote = CI on manual trigger, iOS 17 / Android 10) | **done** | docs/plans/NATIVE-APPS.md (State `blocked` on E24): Capacitor 8.5.2 `ios/` + `android/`, `pnpm build:native`, native saves mirror + lifecycle (`src/native/`), signed per-file OTA on Vercel `wildshard-updates` (`gh workflow run ota-promote`); iOS XCUITest (`scripts/native-ios-ui.sh`) and Android emulator E2E (`scripts/native-android-e2e.mjs`) pass. Fixed on the way, web too: ENTER WORLD below the screen on landscape phones, Android pointer-lock error modal, closed pause menu in the a11y tree |
| E24 | (from E23) **Needs you** to put the apps in the stores: enrol Apple Developer ($99/yr) + Play Console ($25); create the `VERCEL_UPDATES_TOKEN` secret (dashboard token for `wildshard-updates`, then `gh secret set VERCEL_UPDATES_TOKEN`); back up `~/.config/wildshard/ota-signing.pem` offline; recruit 12 Play closed testers | **needs you** | docs/plans/NATIVE-APPS.md N-C / N-D; after enrolment the next agent wires signing + fastlane (TestFlight / Play internal) |
| E25 | "why is the CPU maxxed why is headless chrome using so much CPU ? … learnings from game demos scaffold" + "tell the trials gauntlet loop to stop doing whatever its doing with android" (picks: agent-browser idle 5 min, trials → Metal + serial, written rule max 3, stop neither ACE-Step nor emulator) | **done** (2026-09-22) | Cause: trials `clip.ts` ×3 on SwiftShader (~5 cores) + trials `native/gate.ts android` emulator `rockhop_api36` on `swiftshader_indirect` (~5 cores) + open agent-browser game tabs (~1.5 cores each). `~/.agent-browser/config.json` `idleTimeout: "5m"` (verified `AGENT_BROWSER_IDLE_TIMEOUT_MS=300000`); trials session messaged to kill the emulator and run clips on Metal one at a time; AGENTS.md render-lane rule. |
