# Nine Dragon grapple lab → partial shard parity audit

2026-09-26 · E204 / F2. This is a preservation check before any archive or deletion. The only remaining Nine Dragon lab source under `src/dev/nd-lab/` is `grapple/` (18 source files including the Blender recipe); the clean-room page and earlier look/viewmodel labs were removed in `7a339ed2` / `33c629af`. The former lab scene is **not** equivalent to the live shard yet. Keep it available.

## What the live partial shard now does

`src/chunks/nine-dragon-stack/grapple/Traversal.ts` claims LOCK on a visible hook, checks line of sight and a safe floor with the shared Rapier queries, uses JUMP to fire, plays the left-arm grapple clips, draws a basic filament, moves the capsule across the Well with the CharacterMotor, and closes the temporary upper safety guard. The square-to-street and Well-rim crossings were exercised in portrait browser QA. The 19-leg fragment walk gate passed with zero stuck and zero boundary escapes. The final portrait trailer review cut uses that real crossing.

## Lab inventory and parity

| Lab source | Useful content | Live counterpart | Assessment |
| --- | --- | --- | --- |
| `main.ts` | Two-take playback, seek, captures and bench controls | Game loop; `scripts/steam-trailer/` capture | Lab harness only. Keep until the behavior below is ported and compared. |
| `sequence.ts` | Fire → fly → bite → taut → zip → release/vault/land; miss → snap/reel/dock; camera and arm timing | `grapple/Traversal.ts`, `vm/fpArms.ts` | **Partial.** Live zip and existing aim/fire/hold clips work; bite, vault/settle and the complete miss/reel sequence are absent. |
| `line.ts` | Fixed-step rope dynamics and a pixel-width animated filament | `grapple/Traversal.ts` cylinder tracer | **Missing.** Current line is straight and basic; no slack, tension pulse or whip. |
| `fx.ts` | Sparks, muzzle, bite and dock flashes | No equivalent grapple effect in the live shard | **Missing.** |
| `hook.ts` | TRELLIS dragon casting, textured brass and lock/bite glow | Procedural brass hooks in `world/well-rim.ts` and `world/well-bridges.ts` | **Partial.** Target placement works; sculpt, gold outline and bite response have not been ported. The lab GLB stays in `public/assets/nine-dragon/lab/grapple/`. |
| `feizhua.ts`, `vm-material.ts`, `blender/fei_zhua.py` | Wrist launcher/claw shape, materials and source build | `vm/fpArms.ts`, `vm/materials.ts`, `public/assets/nine-dragon/viewmodel/fp-rig.glb` | New rig supersedes the lab presentation. Verify claw opening/flight/readability beside the lab before retiring the lab model or Blender recipe. |
| `post.ts` | Bloom, fog, ink and grade for the lab shot | `look/post.ts`, `look/render/`, `look/style.ts` | Shard-wide look is implemented independently and has been visually reviewed in dome rounds. Check grapple-specific flashes in that pipeline when ported. |
| `well.ts` | Small staged Well and shot set points | `world/well*.ts`, `world/build.ts`, `layout.ts` | Superseded by the actual partial shard and its colliders; the lab's staged camera path is still useful as a motion reference. |
| `neon.ts`, `wetstone.ts` | Signs, neon spill and reflective stone for the staged shot | `look/neonsigns.ts`, `look/signs.ts`, `look/render/reflect.ts`, world materials | The fragment has production equivalents. No independent grapple action remains here. |
| `world/geo.ts`, `world/material.ts`, `world/rng.ts`, `world/batch.ts`, `world/grammar.ts`, `world/pieces.ts` | Lab scene kit, Jiehua facades, seeded dressing and batching | `world/facade/`, `look/facadeMaterial.ts`, `world/build.ts` | Ported/superseded for the built fragment. Do a visual comparison before archive; the lab has more geography than the partial shard by design. |

## Archive gate

The asset directory `public/assets/nine-dragon/lab/` also remains **live**: `def.ts`, `world/build.ts`, `world/props3d.ts`, `world/crowd.ts`, `world/jian.ts` and the look code still load its textures, walker/sitter/guard and organic GLBs. `grade-lut-cleanroom.bin` is also the current production grade despite its name. F10 must relocate these assets, update every reference, check the generated byte/version manifests and rerun the clean export gate before that directory can be archived. A path containing `lab` is not evidence that the asset is unused.

1. Add one visible hook sculpt/lock/bite checkpoint, preserving the phone draw and triangle budgets; compare it to the lab and the approved Well mockup.
2. Add rope/filament dynamics and fire/bite/landing effects as one focused visual checkpoint. Review a short portrait capture at every step.
3. Add the miss/reel path and prove it restores the arm and guard without a soft lock. Add enemy yank only after its combat behavior and target rules are reviewed; it is specified in the plan but not built in the lab's two takes.
4. Compare all three sequences on device or a faithful portrait capture, run the physics and clean-export gates, and verify every lab-only asset has a production location or a documented reason to retire it. Only then archive the lab source and its remaining assets in a small, explicit commit.

The lab GLBs are study assets. The three **placed** TRELLIS GLBs in the fragment have already been registered in the shared Model Explorer via `src/chunks/nine-dragon-stack/index.ts`. The lab-only dragon hook has no Model Explorer entry because its sculpt is not yet placed in the live shard; register it through the same world registry when it is placed.
