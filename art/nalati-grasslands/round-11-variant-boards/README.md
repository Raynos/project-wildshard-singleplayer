# Nalati round 11: variant boards (E136)

Two agent-chosen defaults with the losing variant still in `src/`. Captured on the live build `eb88654-mugvg4kx` as an
iPhone 16 Pro (390×844 @3×, `?touch&tier=phone`), frozen at 16:13 (`clock=0`), `weather=clear`. Each pose is the same
on both sides.

Base URL: `https://wildshard-singleplayer.vercel.app/?chunk=nalati-grasslands&mute=1&nolock=1&skipintro=1&weather=clear&clock=0&perf=0&tier=phone&touch`

| File | A | B (what players get) |
|---|---|---|
| `look-v1-vs-v2.jpg` | old painterly look + old painterly grass (`&look=v1`) | current look v2 |
| `props-proc-vs-glb.jpg` | procedural camp props (`&models=0`; this also turns rocks, balbals and creatures procedural) | the 3D models |

Poses (x, z, yaw, pitch):
- Look: vista (0, 58, 0, 0); camp (68, 198, −2.2455, −0.06); grass (40, 205, 0.4, −0.42).
- Props: kazan + eagle (90, 202, 2.2143, −0.2); woodpile (99.5, 207.5, 2.0344, −0.33); chest (83, 204, 2.2318, −0.35);
  churns (92, 217, π, −0.42); saddles (79, 211, 2.1815, −0.3).

The single shots are the board cells, shrunk to 1266 px tall.

Note: the churn model (`kumis-churn`, props row 4 B) reads as two brown bears from 5 m.
